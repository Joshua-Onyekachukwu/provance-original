# Schema Migration Runbook: 0003–0021 (Dashboard SQL Editor)

How to apply `supabase/migrations/0003_admin_ops.sql` through
`0021_scan_attempts.sql` to the live Supabase project using the **dashboard
SQL Editor** (pg-meta), in dependency order, with a verification checklist
per migration. The founder-facing checklist below is generated by
`node scripts/validate-migrations.mjs --runbook` (see the markers) — one
click-to-open SQL Editor link per pending migration, plus the full combined
paste block.

<!-- BEGIN MIGRATION MANIFEST — canonical file set + order; regenerated by `node scripts/validate-migrations.mjs --runbook` or `node scripts/check-migration-convergence.mjs --fix`; do not hand-edit between markers -->
| # | Migration file |
| - | -------------- |
| 1 | `0001_waitlist_auth.sql` |
| 2 | `0002_scans.sql` |
| 3 | `0003_admin_ops.sql` |
| 4 | `0004_profiles.sql` |
| 5 | `0005_organization.sql` |
| 6 | `0006_feature_flags.sql` |
| 7 | `0007_incidents.sql` |
| 8 | `0008_audit_logs.sql` |
| 9 | `0009_scan_processing.sql` |
| 10 | `0010_user_sessions.sql` |
| 11 | `0011_notifications.sql` |
| 12 | `0012_profiles_team.sql` |
| 13 | `0013_scan_dedup.sql` |
| 14 | `0014_crash_reports.sql` |
| 15 | `0015_invite_token_hash.sql` |
| 16 | `0016_role_scopes.sql` |
| 17 | `0017_user_sessions_seed.sql` |
| 18 | `0018_better_auth.sql` |
| 19 | `0019_scan_idempotency.sql` |
| 20 | `0020_api_usage.sql` |
| 21 | `0021_scan_attempts.sql` |
| 22 | `0022_vu_ledger.sql` |
| 23 | `0023_scan_vu_meter.sql` |
| 24 | `0024_vu_rollover.sql` |
<!-- END MIGRATION MANIFEST -->

<!-- BEGIN FOUNDER CHECKLIST — regenerated by `node scripts/validate-migrations.mjs --runbook`; do not hand-edit between markers -->
## ⚡ Founder checklist — one paste converges the schema

**Project:** `dmhrwdcuwtgscwlaagsa` · **Current state:** 5 applied / 16 pending (1 seed-only) — re-probe anytime with `npm run validate:migrations`.

Each row opens the SQL Editor **pre-filled** with that migration: click, **Run**, check the verify count, tick the box. Or skip the clicks and paste the **full combined block** at the bottom once — every file is idempotent and the order is dependency-correct.

| Done | Migration | File | Unlocks | Verify (expect) | SQL Editor |
| ---- | --------- | ---- | ------- | --------------- | ---------- |
| [ ] | 0005 | `0005_organization.sql` | org API + invites + member role/team + team scoping + admin org views | 4 tables | [Open →](https://supabase.com/dashboard/project/dmhrwdcuwtgscwlaagsa/sql/new?query=--%20Organization%20workspace%20schema%3A%20organizations%2C%20teams%2C%20members%2C%20invites.%0A--%0A--%20NOTE%3A%20this%20is%20a%20NEW%20migration%20(0005)%2C%20not%20an%20edit%20to%200002_scans.sql%20%E2%80%94%20that%0A--%20migration%20is%20scans-only%20and%20is%20already%20applied%20to%20the%20remote%20Supabase%0A--%20project%2C%20so%20editing%20it%20would%20create%20drift.%20See%0A--%20docs%2Fengineering%2FORGANIZATION_API_CONTRACT.md%20for%20the%20full%20contract.%0A%0Acreate%20extension%20if%20not%20exists%20pgcrypto%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20Organizations%0A--%20---------------------------------------------------------------------------%0A%0Acreate%20table%20if%20not%20exists%20public.organizations%20(%0A%20%20id%20uuid%20primary%20key%20default%20gen_random_uuid()%2C%0A%20%20name%20text%20not%20null%2C%0A%20%20slug%20text%20unique%2C%0A%20%20plan%20text%20not%20null%20default%20'pro'%2C%0A%20%20seats%20integer%20not%20null%20default%201%2C%0A%20%20storage_limit_gb%20numeric%20not%20null%20default%2050%2C%0A%20%20storage_used_gb%20numeric%20not%20null%20default%200%2C%0A%20%20scan_count%20integer%20not%20null%20default%200%2C%0A%20%20created_at%20timestamptz%20not%20null%20default%20now()%2C%0A%20%20updated_at%20timestamptz%20not%20null%20default%20now()%0A)%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20Teams%20(scoped%20to%20an%20organization)%0A--%20---------------------------------------------------------------------------%0A%0Acreate%20table%20if%20not%20exists%20public.teams%20(%0A%20%20id%20uuid%20primary%20key%20default%20gen_random_uuid()%2C%0A%20%20organization_id%20uuid%20not%20null%20references%20public.organizations(id)%20on%20delete%20cascade%2C%0A%20%20name%20text%20not%20null%2C%0A%20%20description%20text%2C%0A%20%20created_at%20timestamptz%20not%20null%20default%20now()%0A)%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20Members%20(profile%20user%20%3C-%3E%20organization%20join)%0A--%20---------------------------------------------------------------------------%0A%0Acreate%20table%20if%20not%20exists%20public.organization_members%20(%0A%20%20organization_id%20uuid%20not%20null%20references%20public.organizations(id)%20on%20delete%20cascade%2C%0A%20%20user_id%20uuid%20not%20null%20references%20auth.users(id)%20on%20delete%20cascade%2C%0A%20%20role%20text%20not%20null%20default%20'member'%0A%20%20%20%20check%20(role%20in%20('owner'%2C%20'admin'%2C%20'member'))%2C%0A%20%20team_id%20uuid%20references%20public.teams(id)%20on%20delete%20set%20null%2C%0A%20%20status%20text%20not%20null%20default%20'active'%0A%20%20%20%20check%20(status%20in%20('active'%2C%20'invited'))%2C%0A%20%20created_at%20timestamptz%20not%20null%20default%20now()%2C%0A%20%20updated_at%20timestamptz%20not%20null%20default%20now()%2C%0A%20%20primary%20key%20(organization_id%2C%20user_id)%0A)%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20Invites%0A--%20---------------------------------------------------------------------------%0A%0Acreate%20table%20if%20not%20exists%20public.organization_invites%20(%0A%20%20id%20uuid%20primary%20key%20default%20gen_random_uuid()%2C%0A%20%20organization_id%20uuid%20not%20null%20references%20public.organizations(id)%20on%20delete%20cascade%2C%0A%20%20email%20text%20not%20null%2C%0A%20%20role%20text%20not%20null%20default%20'member'%0A%20%20%20%20check%20(role%20in%20('admin'%2C%20'member'))%2C%0A%20%20team_id%20uuid%20references%20public.teams(id)%20on%20delete%20set%20null%2C%0A%20%20token%20text%20not%20null%20unique%20default%20gen_random_uuid()%3A%3Atext%2C%0A%20%20status%20text%20not%20null%20default%20'pending'%0A%20%20%20%20check%20(status%20in%20('pending'%2C%20'accepted'%2C%20'cancelled'%2C%20'expired'))%2C%0A%20%20invited_by%20uuid%20references%20auth.users(id)%20on%20delete%20set%20null%2C%0A%20%20expires_at%20timestamptz%20not%20null%20default%20(now()%20%2B%20interval%20'7%20days')%2C%0A%20%20created_at%20timestamptz%20not%20null%20default%20now()%0A)%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20Indexes%20for%20the%20hot%20org-scoped%20queries%20(FKs%20are%20not%20auto-indexed%20in%20PG)%0A--%20---------------------------------------------------------------------------%0A%0Acreate%20index%20if%20not%20exists%20teams_organization_id_idx%20on%20public.teams%20(organization_id)%3B%0Acreate%20index%20if%20not%20exists%20organization_invites_org_status_idx%0A%20%20on%20public.organization_invites%20(organization_id%2C%20status)%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20Row%20level%20security%0A--%0A--%20The%20backend%20writes%20through%20the%20service-role%20admin%20client%20(RLS%20bypassed)%2C%0A--%20so%20these%20policies%20protect%20direct%20Supabase%20access%3A%20any%20member%20of%20an%0A--%20organization%20can%20read%20its%20org%2C%20teams%2C%20members%2C%20and%20invites.%20Writes%20are%0A--%20intentionally%20backend-only%20(no%20direct%20insert%2Fupdate%2Fdelete%20policies).%0A--%20---------------------------------------------------------------------------%0A%0Aalter%20table%20public.organizations%20enable%20row%20level%20security%3B%0Aalter%20table%20public.teams%20enable%20row%20level%20security%3B%0Aalter%20table%20public.organization_members%20enable%20row%20level%20security%3B%0Aalter%20table%20public.organization_invites%20enable%20row%20level%20security%3B%0A%0Adrop%20policy%20if%20exists%20%22Organization%20is%20viewable%20by%20members%22%20on%20public.organizations%3B%0Acreate%20policy%20%22Organization%20is%20viewable%20by%20members%22%0Aon%20public.organizations%0Afor%20select%0Ato%20authenticated%0Ausing%20(%0A%20%20exists%20(%0A%20%20%20%20select%201%20from%20public.organization_members%20m%0A%20%20%20%20where%20m.organization_id%20%3D%20organizations.id%20and%20m.user_id%20%3D%20auth.uid()%0A%20%20)%0A)%3B%0A%0Adrop%20policy%20if%20exists%20%22Teams%20are%20viewable%20by%20members%22%20on%20public.teams%3B%0Acreate%20policy%20%22Teams%20are%20viewable%20by%20members%22%0Aon%20public.teams%0Afor%20select%0Ato%20authenticated%0Ausing%20(%0A%20%20exists%20(%0A%20%20%20%20select%201%20from%20public.organization_members%20m%0A%20%20%20%20where%20m.organization_id%20%3D%20teams.organization_id%20and%20m.user_id%20%3D%20auth.uid()%0A%20%20)%0A)%3B%0A%0Adrop%20policy%20if%20exists%20%22Members%20are%20viewable%20by%20org%20members%22%20on%20public.organization_members%3B%0Acreate%20policy%20%22Members%20are%20viewable%20by%20org%20members%22%0Aon%20public.organization_members%0Afor%20select%0Ato%20authenticated%0Ausing%20(%0A%20%20exists%20(%0A%20%20%20%20select%201%20from%20public.organization_members%20m%0A%20%20%20%20where%20m.organization_id%20%3D%20organization_members.organization_id%0A%20%20%20%20%20%20and%20m.user_id%20%3D%20auth.uid()%0A%20%20)%0A)%3B%0A%0Adrop%20policy%20if%20exists%20%22Invites%20are%20viewable%20by%20org%20members%22%20on%20public.organization_invites%3B%0Acreate%20policy%20%22Invites%20are%20viewable%20by%20org%20members%22%0Aon%20public.organization_invites%0Afor%20select%0Ato%20authenticated%0Ausing%20(%0A%20%20exists%20(%0A%20%20%20%20select%201%20from%20public.organization_members%20m%0A%20%20%20%20where%20m.organization_id%20%3D%20organization_invites.organization_id%0A%20%20%20%20%20%20and%20m.user_id%20%3D%20auth.uid()%0A%20%20)%0A)%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20Updated-at%20triggers%0A--%20---------------------------------------------------------------------------%0A%0Adrop%20trigger%20if%20exists%20organizations_set_updated_at%20on%20public.organizations%3B%0Acreate%20trigger%20organizations_set_updated_at%0Abefore%20update%20on%20public.organizations%0Afor%20each%20row%0Aexecute%20function%20public.set_updated_at()%3B%0A%0Adrop%20trigger%20if%20exists%20organization_members_set_updated_at%20on%20public.organization_members%3B%0Acreate%20trigger%20organization_members_set_updated_at%0Abefore%20update%20on%20public.organization_members%0Afor%20each%20row%0Aexecute%20function%20public.set_updated_at()%3B) |
| [ ] | 0007 | `0007_incidents.sql` | admin monitoring incidents section | 5 incident rows | [Open →](https://supabase.com/dashboard/project/dmhrwdcuwtgscwlaagsa/sql/new?query=--%20Admin%20monitoring%20incidents.%20Seeded%20rows%20mirror%20the%20frontend%20mockMonitoring%0A--%20incidents%20so%20the%20real%20path%20is%20immediately%20useful%3B%20the%20seed%20uses%20relative%0A--%20timestamps%20computed%20at%20migration%20time.%0A%0Acreate%20table%20if%20not%20exists%20public.admin_incidents%20(%0A%20%20id%20text%20primary%20key%2C%0A%20%20title%20text%20not%20null%2C%0A%20%20severity%20text%20not%20null%0A%20%20%20%20check%20(severity%20in%20('critical'%2C%20'major'%2C%20'minor'))%2C%0A%20%20status%20text%20not%20null%0A%20%20%20%20check%20(status%20in%20('investigating'%2C%20'monitoring'%2C%20'resolved'))%2C%0A%20%20started_at%20timestamptz%20not%20null%2C%0A%20%20resolved_at%20timestamptz%2C%0A%20%20duration_hours%20numeric%2C%0A%20%20services%20text%5B%5D%20not%20null%20default%20'%7B%7D'%2C%0A%20%20summary%20text%20not%20null%20default%20''%2C%0A%20%20created_at%20timestamptz%20not%20null%20default%20now()%0A)%3B%0A%0Ainsert%20into%20public.admin_incidents%0A%20%20(id%2C%20title%2C%20severity%2C%20status%2C%20started_at%2C%20resolved_at%2C%20duration_hours%2C%20services%2C%20summary)%0Avalues%0A%20%20(%0A%20%20%20%20'inc_001'%2C%0A%20%20%20%20'Scan%20worker%20partial%20outage'%2C%0A%20%20%20%20'major'%2C%0A%20%20%20%20'resolved'%2C%0A%20%20%20%20now()%20-%20interval%20'6%20days%202%20hours'%2C%0A%20%20%20%20now()%20-%20interval%20'5%20days%209%20hours'%2C%0A%20%20%20%207%2C%0A%20%20%20%20array%5B'Scan%20Worker'%5D%2C%0A%20%20%20%20'A%20memory%20leak%20in%20the%20fingerprint%20model%20worker%20stalled%20processing%20for%20roughly%20a%20third%20of%20the%20queue.%20A%20rollback%20to%20the%20previous%20model%20release%20restored%20throughput.'%0A%20%20)%2C%0A%20%20(%0A%20%20%20%20'inc_002'%2C%0A%20%20%20%20'Elevated%20API%20latency'%2C%0A%20%20%20%20'minor'%2C%0A%20%20%20%20'resolved'%2C%0A%20%20%20%20now()%20-%20interval%20'12%20days%204%20hours'%2C%0A%20%20%20%20now()%20-%20interval%20'12%20days%2011%20hours'%2C%0A%20%20%20%207%2C%0A%20%20%20%20array%5B'API%20Gateway'%5D%2C%0A%20%20%20%20'Autoscaling%20lag%20under%20a%20waitlist%20invite%20burst%20pushed%20p95%20latency%20above%20target%20for%20seven%20hours.%20Autoscaling%20thresholds%20were%20retuned.'%0A%20%20)%2C%0A%20%20(%0A%20%20%20%20'inc_003'%2C%0A%20%20%20%20'Storage%20upload%20errors'%2C%0A%20%20%20%20'major'%2C%0A%20%20%20%20'resolved'%2C%0A%20%20%20%20now()%20-%20interval%20'19%20days%206%20hours'%2C%0A%20%20%20%20now()%20-%20interval%20'19%20days%209%20hours'%2C%0A%20%20%20%203%2C%0A%20%20%20%20array%5B'Object%20Storage%20(R2)'%5D%2C%0A%20%20%20%20'Signed%20upload%20URLs%20expired%20early%20under%20load%2C%20rejecting%20a%20subset%20of%20media%20uploads.%20The%20signing%20window%20was%20extended%20and%20the%20worker%20retries%20added.'%0A%20%20)%2C%0A%20%20(%0A%20%20%20%20'inc_004'%2C%0A%20%20%20%20'Scan%20worker%20memory%20pressure'%2C%0A%20%20%20%20'major'%2C%0A%20%20%20%20'investigating'%2C%0A%20%20%20%20now()%20-%20interval%20'4%20hours'%2C%0A%20%20%20%20null%2C%0A%20%20%20%20null%2C%0A%20%20%20%20array%5B'Scan%20Worker'%5D%2C%0A%20%20%20%20'Resident%20memory%20on%20the%20worker%20pool%20is%20trending%20upward%20since%20the%20model%20update.%20Monitoring%20is%20active%20while%20a%20candidate%20fix%20is%20validated.'%0A%20%20)%2C%0A%20%20(%0A%20%20%20%20'inc_005'%2C%0A%20%20%20%20'Database%20connection%20pool%20exhaustion'%2C%0A%20%20%20%20'critical'%2C%0A%20%20%20%20'resolved'%2C%0A%20%20%20%20now()%20-%20interval%20'25%20days%203%20hours'%2C%0A%20%20%20%20now()%20-%20interval%20'25%20days%205%20hours'%2C%0A%20%20%20%202%2C%0A%20%20%20%20array%5B'Postgres%20(Neon)'%5D%2C%0A%20%20%20%20'A%20runaway%20query%20pattern%20exhausted%20the%20connection%20pool%2C%20causing%20intermittent%20timeouts.%20The%20query%20was%20optimized%20and%20pool%20limits%20raised.'%0A%20%20)%0Aon%20conflict%20(id)%20do%20nothing%3B%0A%0A--%20Incident%20severity%20%2B%20status%20lookups%20are%20the%20hot%20path%20on%20the%20monitoring%20page.%0Acreate%20index%20if%20not%20exists%20admin_incidents_status_idx%20on%20public.admin_incidents%20(status)%3B) |
| [ ] | 0008 | `0008_audit_logs.sql` | admin audit logs (GET /v1/admin/audit-logs) | 15 audit rows | [Open →](https://supabase.com/dashboard/project/dmhrwdcuwtgscwlaagsa/sql/new?query=--%20Admin%20audit%20log.%20Written%20by%20backend%20services%20(service-role%20only)%20and%20read%0A--%20by%20the%20admin%20Audit%20Logs%20endpoint%20(GET%20%2Fadmin%2Faudit-logs).%20Seeded%20rows%20mirror%0A--%20the%20frontend%20mockAuditEvents%20shape%20(%7Bid%2C%20actor_email%2C%20action%2C%20severity%2C%0A--%20resource_type%2C%20resource_id%2C%20created_at%7D)%20so%20the%20real%20path%20is%20immediately%0A--%20useful%3B%20the%20seed%20uses%20relative%20timestamps%20computed%20at%20migration%20time.%0A--%0A--%20RLS%20is%20enabled%20with%20no%20public%20policies%3A%20the%20Supabase%20service%20role%20bypasses%0A--%20RLS%2C%20so%20only%20the%20backend%20can%20read%2Fwrite%20the%20trail.%20The%20auth.audit_log_entries%0A--%20table%20is%20left%20untouched%20%E2%80%94%20this%20is%20the%20application-level%20audit%20trail.%0A%0Acreate%20table%20if%20not%20exists%20public.audit_logs%20(%0A%20%20id%20text%20primary%20key%20default%20gen_random_uuid()%3A%3Atext%2C%0A%20%20actor_email%20text%20not%20null%2C%0A%20%20action%20text%20not%20null%2C%0A%20%20severity%20text%20not%20null%20default%20'low'%0A%20%20%20%20check%20(severity%20in%20('critical'%2C%20'high'%2C%20'medium'%2C%20'low'))%2C%0A%20%20entity_type%20text%2C%0A%20%20entity_id%20text%2C%0A%20%20details%20jsonb%2C%0A%20%20created_at%20timestamptz%20not%20null%20default%20now()%0A)%3B%0A%0Aalter%20table%20public.audit_logs%20enable%20row%20level%20security%3B%0A%0Ainsert%20into%20public.audit_logs%0A%20%20(id%2C%20actor_email%2C%20action%2C%20severity%2C%20entity_type%2C%20entity_id%2C%20created_at)%0Avalues%0A%20%20('audit_0001'%2C%20'founder.admin%40provance.local'%2C%20'user.invited'%2C%20%20%20'medium'%2C%20'user'%2C%20'usr_002'%2C%20now()%20-%20interval%20'0%20hours')%2C%0A%20%20('audit_0002'%2C%20'aisha.khan%40provance.local'%2C%20%20%20'scan.submitted'%2C%20%20'medium'%2C%20'scan'%2C%20'scan_0042'%2C%20now()%20-%20interval%20'2%20hours')%2C%0A%20%20('audit_0003'%2C%20'aisha.khan%40provance.local'%2C%20%20%20'scan.completed'%2C%20%20'low'%2C%20%20%20%20'scan'%2C%20'scan_0042'%2C%20now()%20-%20interval%20'4%20hours')%2C%0A%20%20('audit_0004'%2C%20'founder.admin%40provance.local'%2C%20'role.changed'%2C%20%20%20'high'%2C%20%20%20'role'%2C%20'role_analyst'%2C%20now()%20-%20interval%20'8%20hours')%2C%0A%20%20('audit_0005'%2C%20'david.okafor%40provance.local'%2C%20'report.exported'%2C%20'low'%2C%20%20%20'report'%2C%20'rpt_0042'%2C%20now()%20-%20interval%20'10%20hours')%2C%0A%20%20('audit_0006'%2C%20'system'%2C%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20'scan.failed'%2C%20%20%20%20%20'high'%2C%20%20%20'scan'%2C%20'scan_0041'%2C%20now()%20-%20interval%20'1%20day%202%20hours')%2C%0A%20%20('audit_0007'%2C%20'founder.admin%40provance.local'%2C%20'waitlist.approved'%2C%20'medium'%2C%20'waitlist_application'%2C%20'wl_0017'%2C%20now()%20-%20interval%20'1%20day%208%20hours')%2C%0A%20%20('audit_0008'%2C%20'lucia.mendes%40provance.local'%2C%20'team.member_added'%2C%20'medium'%2C%20'team'%2C%20'team_legal'%2C%20now()%20-%20interval%20'2%20days%201%20hour')%2C%0A%20%20('audit_0009'%2C%20'founder.admin%40provance.local'%2C%20'feature_flag.toggled'%2C%20'high'%2C%20'feature_flag'%2C%20'ff_deep_scan'%2C%20now()%20-%20interval%20'2%20days%206%20hours')%2C%0A%20%20('audit_0010'%2C%20'david.okafor%40provance.local'%2C%20'api_key.created'%2C%20'medium'%2C%20'api_key'%2C%20'key_0012'%2C%20now()%20-%20interval%20'3%20days%203%20hours')%2C%0A%20%20('audit_0011'%2C%20'founder.admin%40provance.local'%2C%20'settings.updated'%2C%20'low'%2C%20%20'settings'%2C%20'retention'%2C%20now()%20-%20interval%20'3%20days%209%20hours')%2C%0A%20%20('audit_0012'%2C%20'aisha.khan%40provance.local'%2C%20%20%20'report.viewed'%2C%20%20%20'low'%2C%20%20%20%20'report'%2C%20'rpt_0038'%2C%20now()%20-%20interval%20'4%20days%202%20hours')%2C%0A%20%20('audit_0013'%2C%20'lucia.mendes%40provance.local'%2C%20'scan.submitted'%2C%20%20'medium'%2C%20'scan'%2C%20'scan_0039'%2C%20now()%20-%20interval%20'5%20days%205%20hours')%2C%0A%20%20('audit_0014'%2C%20'founder.admin%40provance.local'%2C%20'org.created'%2C%20%20%20%20'medium'%2C%20'organization'%2C%20'org_0001'%2C%20now()%20-%20interval%20'6%20days%201%20hour')%2C%0A%20%20('audit_0015'%2C%20'system'%2C%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20'waitlist.rejected'%2C%20'high'%2C%20'waitlist_application'%2C%20'wl_0009'%2C%20now()%20-%20interval%20'6%20days%208%20hours')%0Aon%20conflict%20(id)%20do%20nothing%3B%0A%0A--%20The%20endpoint%20orders%20by%20created_at%20desc%20and%20the%20page%20filters%20by%20action%20%2F%0A--%20severity%20%2F%20actor%2C%20so%20those%20are%20the%20hot%20paths.%0Acreate%20index%20if%20not%20exists%20audit_logs_created_at_idx%20on%20public.audit_logs%20(created_at%20desc)%3B%0Acreate%20index%20if%20not%20exists%20audit_logs_action_idx%20on%20public.audit_logs%20(action)%3B%0Acreate%20index%20if%20not%20exists%20audit_logs_severity_idx%20on%20public.audit_logs%20(severity)%3B) |
| [ ] | 0009 | `0009_scan_processing.sql` | **scan round-trip hard gate** (POST /v1/scans) | 3 columns | [Open →](https://supabase.com/dashboard/project/dmhrwdcuwtgscwlaagsa/sql/new?query=--%200009%3A%20scan%20processing%20metadata.%0A--%0A--%20The%20mock%20scan%20rows%20the%20frontend%20consumes%20carry%20processing_mode%2C%20team_id%2C%20and%0A--%20completed_at.%20The%20real%20scans%20table%20needs%20them%20so%20the%20API%20boundary%20can%20emit%0A--%20the%20same%20row%20dialect%20for%20queue%2Fledger%2Freport%20surfaces.%0A%0Aalter%20table%20public.scans%0A%20%20add%20column%20if%20not%20exists%20processing_mode%20text%20not%20null%20default%20'standard'%2C%0A%20%20add%20column%20if%20not%20exists%20team_id%20uuid%2C%0A%20%20add%20column%20if%20not%20exists%20completed_at%20timestamptz%3B%0A%0A--%20The%20queue%20snapshot%20and%20admin%20analytics%20aggregate%20on%20status%3B%20keep%20the%20common%0A--%20filters%20indexed.%0Acreate%20index%20if%20not%20exists%20scans_status_idx%20on%20public.scans%20(status)%3B%0Acreate%20index%20if%20not%20exists%20scans_user_status_idx%20on%20public.scans%20(user_id%2C%20status)%3B) |
| [ ] | 0010 | `0010_user_sessions.sql` | active-sessions surface + readiness hard gate | 2 tables | [Open →](https://supabase.com/dashboard/project/dmhrwdcuwtgscwlaagsa/sql/new?query=--%20User%20session%20ledger%20and%20per-user%20security%20settings.%0A--%0A--%20Written%20by%20the%20backend%20security%20service%20(service-role%20only)%3A%20every%0A--%20sign-in%20%2F%20session%20refresh%20upserts%20a%20row%20keyed%20by%20(user_id%2C%20auth_session_id)%0A--%20where%20auth_session_id%20is%20the%20%60sid%60%20claim%20of%20the%20Supabase%20access-token%20JWT%0A--%20(the%20same%20id%20GoTrue%20uses%20in%20auth.sessions).%20Only%20a%20SHA-256%20hash%20of%20the%0A--%20refresh%20token%20is%20stored%20%E2%80%94%20never%20the%20raw%20token%20%E2%80%94%20so%20a%20DB%20leak%20cannot%20replay%0A--%20a%20session%20credential%3B%20revocation%20goes%20through%20the%20GoTrue%20admin%20API%20by%0A--%20session%20id%20instead.%0A--%0A--%20RLS%20is%20enabled%20with%20no%20public%20policies%3A%20the%20service%20role%20bypasses%20RLS%2C%20so%0A--%20only%20the%20backend%20can%20read%2Fwrite%20the%20ledger.%20The%20anon%2Fauthenticated%20keys%0A--%20cannot%20touch%20it.%0A--%0A--%20The%20security%20endpoints%20degrade%20gracefully%20when%20this%20migration%20is%20not%20yet%0A--%20applied%20(settings%20default%2C%20sessions%20render%20empty)%20so%20a%20fresh%20DB%20never%0A--%20blocks%20sign-in%20%E2%80%94%20consistent%20with%20the%20billing%20plan%20fallback.%0A%0Acreate%20table%20if%20not%20exists%20public.user_sessions%20(%0A%20%20id%20text%20primary%20key%20default%20gen_random_uuid()%3A%3Atext%2C%0A%20%20user_id%20uuid%20not%20null%20references%20auth.users%20(id)%20on%20delete%20cascade%2C%0A%20%20--%20The%20%60sid%60%20claim%20of%20the%20Supabase%20access%20token%20%E2%80%94%20the%20durable%20key%20that%0A%20%20--%20survives%20refresh-token%20rotation%20(a%20refresh%20keeps%20the%20same%20auth%20session).%0A%20%20auth_session_id%20text%20not%20null%2C%0A%20%20--%20SHA-256%20hex%20of%20the%20current%20refresh%20token.%20Used%20to%20clean%20the%20ledger%20row%0A%20%20--%20on%20sign-out%2C%20where%20only%20the%20cookie%20refresh%20token%20is%20available.%0A%20%20refresh_token_hash%20text%2C%0A%20%20device%20text%20not%20null%20default%20'Unknown%20device'%2C%0A%20%20ip_address%20text%2C%0A%20%20location%20text%2C%0A%20%20created_at%20timestamptz%20not%20null%20default%20now()%2C%0A%20%20last_active_at%20timestamptz%20not%20null%20default%20now()%2C%0A%20%20unique%20(user_id%2C%20auth_session_id)%0A)%3B%0A%0Aalter%20table%20public.user_sessions%20enable%20row%20level%20security%3B%0A%0Acreate%20index%20if%20not%20exists%20user_sessions_user_last_active_idx%0A%20%20on%20public.user_sessions%20(user_id%2C%20last_active_at%20desc)%3B%0A%0A--%20Per-user%20sign-in%20controls%20(2FA%20flag%2C%20session%20timeout%2C%20notify%20preferences).%0A--%20Mirrors%20the%20frontend%20mockSecuritySettings.signInControls%20contract.%0Acreate%20table%20if%20not%20exists%20public.user_security_settings%20(%0A%20%20user_id%20uuid%20primary%20key%20references%20auth.users%20(id)%20on%20delete%20cascade%2C%0A%20%20two_factor_enabled%20boolean%20not%20null%20default%20false%2C%0A%20%20session_timeout_minutes%20integer%20not%20null%20default%2060%2C%0A%20%20notify_on_new_device%20boolean%20not%20null%20default%20false%2C%0A%20%20notify_on_password_change%20boolean%20not%20null%20default%20false%2C%0A%20%20updated_at%20timestamptz%20not%20null%20default%20now()%0A)%3B%0A%0Aalter%20table%20public.user_security_settings%20enable%20row%20level%20security%3B) |
| [ ] | 0011 | `0011_notifications.sql` | notifications API + bell | 1 table | [Open →](https://supabase.com/dashboard/project/dmhrwdcuwtgscwlaagsa/sql/new?query=--%20User-scoped%20notifications%20for%20the%20in-app%20bell%20and%20notification%20center.%0A--%0A--%20Written%20by%20the%20backend%20notifications%20service%20(service-role%20only)%3A%20rows%20are%0A--%20keyed%20by%20user_id%20so%20every%20query%20is%20scoped%20to%20the%20signed-in%20user%20%E2%80%94%20the%20same%0A--%20identity%20pattern%20as%20auth_audit_events%20(actor_email)%20but%20with%20a%20proper%20FK%20so%0A--%20the%20dashboard%20%2B%20bell%20surfaces%20can%20join%20and%20filter%20by%20user.%0A--%0A--%20The%20column%20set%20mirrors%20the%20frontend%20mockNotifications%20contract%3A%0A--%20%20%20%7B%20id%2C%20category%2C%20title%2C%20description%2C%20read%2C%20link%2C%20created_at%20%7D%0A--%20%60read%60%20is%20stored%20as%20is_read%20(read%20is%20a%20SQL-reserved-ish%20keyword%20in%20some%0A--%20tooling)%20and%20mapped%20back%20in%20the%20service%20response.%0A--%0A--%20RLS%20is%20enabled%20with%20no%20public%20policies%3A%20the%20service%20role%20bypasses%20RLS%2C%20so%0A--%20only%20the%20backend%20can%20read%2Fwrite.%20The%20anon%2Fauthenticated%20keys%20cannot%20touch%0A--%20it%20%E2%80%94%20consistent%20with%20user_sessions%20and%20the%20other%20backend-owned%20tables.%0A--%0A--%20The%20notifications%20endpoints%20degrade%20gracefully%20when%20this%20migration%20is%20not%0A--%20yet%20applied%20(list%20returns%20empty%2C%20mark-read%20is%20a%20no-op)%20so%20a%20fresh%20DB%20never%0A--%20blocks%20the%20app%20shell.%0A%0Acreate%20table%20if%20not%20exists%20public.notifications%20(%0A%20%20id%20text%20primary%20key%20default%20gen_random_uuid()%3A%3Atext%2C%0A%20%20user_id%20uuid%20not%20null%20references%20auth.users%20(id)%20on%20delete%20cascade%2C%0A%20%20category%20text%20not%20null%20default%20'system'%2C%0A%20%20title%20text%20not%20null%2C%0A%20%20description%20text%2C%0A%20%20--%20%60read%60%20is%20a%20keyword%20in%20a%20few%20SQL%20dialects%3B%20is_read%20is%20unambiguous.%0A%20%20is_read%20boolean%20not%20null%20default%20false%2C%0A%20%20--%20Optional%20deep%20link%20(e.g.%20%2Fapp%2Freports%2Frpt_001)%20the%20bell%2Fpage%20navigate%20to.%0A%20%20link%20text%2C%0A%20%20created_at%20timestamptz%20not%20null%20default%20now()%0A)%3B%0A%0Aalter%20table%20public.notifications%20enable%20row%20level%20security%3B%0A%0Acreate%20index%20if%20not%20exists%20notifications_user_created_idx%0A%20%20on%20public.notifications%20(user_id%2C%20created_at%20desc)%3B) |
| [ ] | 0012 | `0012_profiles_team.sql` | team scoping on profiles (admin team filter) | 1 column | [Open →](https://supabase.com/dashboard/project/dmhrwdcuwtgscwlaagsa/sql/new?query=--%200012%3A%20team%20assignment%20on%20the%20user%20directory%20(profiles).%0A--%0A--%20The%20admin%20Users%20page%20renders%20each%20user's%20team%20badge%20and%20drives%20its%20team%0A--%20filter%20from%20the%20user%20row's%20team_id%20(mirroring%20mockUsers.team_id).%20The%0A--%20organization_members%20table%20is%20the%20relational%20source%20of%20truth%20for%20team%0A--%20assignment%2C%20but%20the%20user-directory%20query%20path%20reads%20a%20flat%20profile%20row%20%E2%80%94%0A--%20so%20profiles%20gains%20a%20denormalized%20team_id%2C%20backfilled%20from%20the%20first%0A--%20active%20membership%20per%20user.%20New%20assignments%20are%20written%20by%20the%20org%0A--%20service%20(updateMemberTeam)%20which%20keeps%20both%20columns%20in%20sync.%0A%0Aalter%20table%20public.profiles%0A%20%20add%20column%20if%20not%20exists%20team_id%20uuid%20references%20public.teams(id)%20on%20delete%20set%20null%3B%0A%0Acreate%20index%20if%20not%20exists%20profiles_team_id_idx%20on%20public.profiles%20(team_id)%3B%0A%0A--%20Backfill%3A%20first%20active%20membership's%20team%20per%20user%20(created_at%20asc).%0Aupdate%20public.profiles%20p%0Aset%20team_id%20%3D%20m.team_id%0Afrom%20(%0A%20%20select%20distinct%20on%20(user_id)%20user_id%2C%20team_id%0A%20%20from%20public.organization_members%0A%20%20where%20team_id%20is%20not%20null%20and%20status%20%3D%20'active'%0A%20%20order%20by%20user_id%2C%20created_at%20asc%0A)%20m%0Awhere%20m.user_id%20%3D%20p.user_id%0A%20%20and%20p.team_id%20is%20null%3B) |
| [ ] | 0013 | `0013_scan_dedup.sql` | scan dedup (file_hash_sha256) | 1 column | [Open →](https://supabase.com/dashboard/project/dmhrwdcuwtgscwlaagsa/sql/new?query=--%200013%3A%20scan%20deduplication.%0A--%0A--%20Hash-based%20dedup%3A%20when%20a%20user%20submits%20a%20scan%20whose%20SHA-256%20matches%20a%20prior%0A--%20completed%20scan%20of%20theirs%2C%20the%20backend%20skips%20reprocessing%20and%20reuses%20the%0A--%20prior%20result%20payload%20(see%20scans.service.ts%20submitScan).%20The%20hash%20is%20written%0A--%20at%20submit%20time%20so%20the%20lookup%20is%20an%20indexed%20equality%2C%20not%20a%20payload%20scan.%0A%0Aalter%20table%20public.scans%0A%20%20add%20column%20if%20not%20exists%20file_hash_sha256%20text%3B%0A%0A--%20The%20dedup%20lookup%20filters%20by%20user%20%2B%20hash%20on%20completed%20rows%20only.%0Acreate%20index%20if%20not%20exists%20scans_user_hash_complete_idx%0A%20%20on%20public.scans%20(user_id%2C%20file_hash_sha256)%0A%20%20where%20status%20%3D%20'complete'%3B) |
| [ ] | 0014 | `0014_crash_reports.sql` | crash reports (POST /v1/telemetry/errors) | 1 table | [Open →](https://supabase.com/dashboard/project/dmhrwdcuwtgscwlaagsa/sql/new?query=--%200014%3A%20client%20crash%20reports%20(pre-Sentry%20telemetry).%0A--%0A--%20Backs%20POST%20%2Fv1%2Ftelemetry%2Ferrors%2C%20where%20the%20frontend%20flushes%20its%20buffered%0A--%20render-error%20records%20(src%2Flib%2Ftelemetry.js).%20client_id%20is%20the%20client's%0A--%20%60cr-%E2%80%A6%60%20record%20id%20and%20the%20primary%20key%2C%20so%20a%20retried%20flush%20upserts%20in%20place%0A--%20instead%20of%20duplicating%20the%20same%20crash.%0A%0Acreate%20table%20if%20not%20exists%20public.crash_reports%20(%0A%20%20client_id%20text%20primary%20key%2C%0A%20%20type%20text%20not%20null%20default%20'render_error'%2C%0A%20%20message%20text%20not%20null%20default%20''%2C%0A%20%20stack%20text%2C%0A%20%20component_stack%20text%2C%0A%20%20route%20text%2C%0A%20%20user_agent%20text%2C%0A%20%20user_id%20text%2C%0A%20%20email%20text%2C%0A%20%20meta%20jsonb%20not%20null%20default%20'%7B%7D'%2C%0A%20%20reported_at%20timestamptz%2C%0A%20%20received_at%20timestamptz%20not%20null%20default%20now()%0A)%3B%0A%0A--%20Triage%20ordering%20for%20a%20future%20admin%20crash%20surface%20(newest%20first).%0Acreate%20index%20if%20not%20exists%20crash_reports_reported_at_idx%0A%20%20on%20public.crash_reports%20(reported_at%20desc)%3B) |
| [ ] | 0015 | `0015_invite_token_hash.sql` | hardened invite tokens (token_hash) | 1 column | [Open →](https://supabase.com/dashboard/project/dmhrwdcuwtgscwlaagsa/sql/new?query=--%200015%3A%20org%20invite%20token%20hardening%20%E2%80%94%20store%20only%20a%20SHA-256%20hash.%0A--%0A--%20organization_invites.token%20previously%20held%20the%20raw%20invite%20token%2C%20so%20a%0A--%20leaked%20invites%20table%20exposed%20usable%20acceptance%20tokens.%20The%20app%20now%20writes%0A--%20token_hash%20(sha256%20hex%20of%20the%20raw%20token)%20and%20matches%20acceptance%20by%20hash%3B%0A--%20the%20raw%20token%20leaves%20the%20backend%20only%20in%20the%20inviteMember%20response%2C%20so%20it%0A--%20travels%20via%20the%20share%2Femail%20link%20and%20is%20never%20persisted.%0A%0Aalter%20table%20public.organization_invites%0A%20%20add%20column%20if%20not%20exists%20token_hash%20text%3B%0A%0A--%20Backfill%20existing%20pending%20invites%20from%20their%20raw%20tokens%20so%20they%20stay%0A--%20acceptable%20after%20this%20deploy%20(raw%20values%20are%20hashed%20in%20place%20and%20never%0A--%20re-issued).%0Aupdate%20public.organization_invites%0Aset%20token_hash%20%3D%20encode(sha256(token%3A%3Abytea)%2C%20'hex')%0Awhere%20token_hash%20is%20null%20and%20token%20is%20not%20null%3B%0A%0A--%20One%20accepted%20token%20per%20invite%20%E2%80%94%20partial%20so%20nulls%20never%20collide.%0Acreate%20unique%20index%20if%20not%20exists%20organization_invites_token_hash_idx%0A%20%20on%20public.organization_invites%20(token_hash)%0A%20%20where%20token_hash%20is%20not%20null%3B%0A%0A--%20Deprecate%20the%20raw%20column%3A%20neutralize%20the%20default%20%2B%20not-null%20so%20future%20rows%0A--%20store%20no%20raw%20token%20bytes.%20The%20column%20itself%20is%20dropped%20in%20a%20follow-up%0A--%20migration%20once%20this%20deploy%20is%20confirmed%20live.%0Aalter%20table%20public.organization_invites%0A%20%20alter%20column%20token%20drop%20default%2C%0A%20%20alter%20column%20token%20drop%20not%20null%3B) |
| [ ] | 0016 | `0016_role_scopes.sql` | role scopes (admin roles page) | 1 table | [Open →](https://supabase.com/dashboard/project/dmhrwdcuwtgscwlaagsa/sql/new?query=--%20Role%20scope%20grants.%20The%20RBAC%20role%20matrix%20(ADMIN_ROLES%20in%20the%20backend%20roles%0A--%20module)%20declares%20the%20default%20scope%20grants%20as%20product%20config%3B%20this%20table%0A--%20holds%20the%20*overrides*%20saved%20through%20PATCH%20%2Fadmin%2Froles%2F%3AroleId%2Fscopes%20so%0A--%20permission%20edits%20survive%20restarts.%20The%20list%20endpoint%20merges%20these%20rows%20over%0A--%20the%20defaults%20(DB%20wins)%2C%20which%20keeps%20new%20scope%20keys%20working%20the%20moment%20they%0A--%20ship%20without%20a%20migration.%0A--%0A--%20RLS%20is%20enabled%20with%20no%20public%20policies%3A%20the%20Supabase%20service%20role%20bypasses%0A--%20RLS%2C%20so%20only%20the%20backend%20can%20read%2Fwrite%20the%20grants%20(same%20as%20audit_logs).%0A%0Acreate%20table%20if%20not%20exists%20public.role_scopes%20(%0A%20%20role_id%20text%20not%20null%2C%0A%20%20scope_key%20text%20not%20null%2C%0A%20%20enabled%20boolean%20not%20null%20default%20true%2C%0A%20%20updated_at%20timestamptz%20not%20null%20default%20now()%2C%0A%20%20primary%20key%20(role_id%2C%20scope_key)%0A)%3B%0A%0Aalter%20table%20public.role_scopes%20enable%20row%20level%20security%3B%0A%0Acreate%20index%20if%20not%20exists%20role_scopes_role_idx%20on%20public.role_scopes%20(role_id)%3B) |
| [ ] | 0017 | `0017_user_sessions_seed.sql` | dev-account session seed | 3 rows (conditional) | [Open →](https://supabase.com/dashboard/project/dmhrwdcuwtgscwlaagsa/sql/new?query=--%200017%3A%20seed%20demo%20rows%20for%20the%20user_sessions%20ledger.%0A--%0A--%20The%20security%20service%20writes%20ledger%20rows%20on%20every%20real%20sign-in%2C%20so%20the%0A--%20table%20self-populates%20in%20normal%20use.%20This%20seed%20exists%20so%20a%20freshly-migrated%0A--%20environment%20(or%20one%20where%20nobody%20has%20signed%20in%20yet)%20still%20renders%20the%0A--%20Security%20page's%20%22active%20sessions%22%20surface%20with%20plausible%20data%20%E2%80%94%20the%20same%0A--%20relative-timestamp%20pattern%20as%20the%20incidents%20(0007)%20and%20audit-log%20(0008)%0A--%20seeds.%0A--%0A--%20It%20targets%20the%20dev%20test%20account%20(founder.admin%40provance.local%2C%20the%20owner%0A--%20the%20seed-org%20script%20creates)%20and%20is%20a%20strict%20no-op%20otherwise%3A%20the%20DO%20block%0A--%20bails%20when%20that%20auth%20user%20does%20not%20exist%2C%20so%20a%20production%20DB%20with%20no%20such%0A--%20account%20is%20untouched%2C%20and%20it%20skips%20when%20the%20user%20already%20has%20ledger%20rows%20so%0A--%20it%20never%20fights%20real%20sessions.%0A--%0A--%20auth_session_id%20values%20are%20UUID-format%20demo%20ids%2C%20distinct%20from%20any%20real%0A--%20GoTrue%20session%20id%20(real%20ones%20never%20collide%20with%20the%20'00000000-0000-%E2%80%A6'%0A--%20prefix).%20The%20refresh_token_hash%20columns%20are%20SHA-256%20hashes%20of%20dummy%20refresh%0A--%20values%20%E2%80%94%20never%20raw%20tokens%2C%20matching%20the%20ledger's%20at-rest%20doctrine.%20Note%3A%0A--%20revoking%20a%20*demo*%20row%20via%20the%20Security%20page%20will%20fail%20the%20GoTrue%20admin%20call%0A--%20(the%20fake%20session%20id%20does%20not%20exist%20there)%2C%20so%20the%20UI%20surfaces%20an%20error%0A--%20toast%3B%20the%20ledger%20row%20itself%20is%20only%20removed%20by%20the%20real%20sign-in%2Fsign-out%0A--%20lifecycle.%0A%0Ado%20%24%24%0Adeclare%0A%20%20v_user_id%20uuid%3B%0Abegin%0A%20%20select%20id%20into%20v_user_id%0A%20%20from%20auth.users%0A%20%20where%20email%20%3D%20'founder.admin%40provance.local'%0A%20%20limit%201%3B%0A%0A%20%20if%20v_user_id%20is%20not%20null%20and%20not%20exists%20(%0A%20%20%20%20select%201%20from%20public.user_sessions%20where%20user_id%20%3D%20v_user_id%0A%20%20)%20then%0A%20%20%20%20insert%20into%20public.user_sessions%0A%20%20%20%20%20%20(user_id%2C%20auth_session_id%2C%20refresh_token_hash%2C%20device%2C%20ip_address%2C%20location%2C%20created_at%2C%20last_active_at)%0A%20%20%20%20values%0A%20%20%20%20%20%20(%0A%20%20%20%20%20%20%20%20v_user_id%2C%0A%20%20%20%20%20%20%20%20'00000000-0000-4000-8000-00000000d001'%2C%0A%20%20%20%20%20%20%20%20encode(sha256('demo-refresh-current'%3A%3Abytea)%2C%20'hex')%2C%0A%20%20%20%20%20%20%20%20'Chrome%20126%20%C2%B7%20macOS'%2C%0A%20%20%20%20%20%20%20%20'41.90.98.12'%2C%0A%20%20%20%20%20%20%20%20'Lagos%2C%20NG'%2C%0A%20%20%20%20%20%20%20%20now()%20-%20interval%20'2%20hours'%2C%0A%20%20%20%20%20%20%20%20now()%20-%20interval%20'2%20hours'%0A%20%20%20%20%20%20)%2C%0A%20%20%20%20%20%20(%0A%20%20%20%20%20%20%20%20v_user_id%2C%0A%20%20%20%20%20%20%20%20'00000000-0000-4000-8000-00000000d002'%2C%0A%20%20%20%20%20%20%20%20encode(sha256('demo-refresh-laptop'%3A%3Abytea)%2C%20'hex')%2C%0A%20%20%20%20%20%20%20%20'Firefox%20128%20%C2%B7%20Windows'%2C%0A%20%20%20%20%20%20%20%20'102.89.33.7'%2C%0A%20%20%20%20%20%20%20%20'Abuja%2C%20NG'%2C%0A%20%20%20%20%20%20%20%20now()%20-%20interval%20'3%20days'%2C%0A%20%20%20%20%20%20%20%20now()%20-%20interval%20'1%20day'%0A%20%20%20%20%20%20)%2C%0A%20%20%20%20%20%20(%0A%20%20%20%20%20%20%20%20v_user_id%2C%0A%20%20%20%20%20%20%20%20'00000000-0000-4000-8000-00000000d003'%2C%0A%20%20%20%20%20%20%20%20encode(sha256('demo-refresh-mobile'%3A%3Abytea)%2C%20'hex')%2C%0A%20%20%20%20%20%20%20%20'Safari%2017%20%C2%B7%20iOS'%2C%0A%20%20%20%20%20%20%20%20'154.120.70.4'%2C%0A%20%20%20%20%20%20%20%20'Nairobi%2C%20KE'%2C%0A%20%20%20%20%20%20%20%20now()%20-%20interval%20'6%20days'%2C%0A%20%20%20%20%20%20%20%20now()%20-%20interval%20'5%20days'%0A%20%20%20%20%20%20)%3B%0A%20%20end%20if%3B%0Aend%20%24%24%3B) |
| [ ] | 0018 | `0018_better_auth.sql` | better-auth tables (informational — live auth runs on GoTrue) | 12 tables | [Open →](https://supabase.com/dashboard/project/dmhrwdcuwtgscwlaagsa/sql/new?query=--%20%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%0A--%200018_better_auth.sql%20%E2%80%94%20Better%20Auth%20schema%20(parallel%20auth%20provider)%0A--%0A--%20Tables%20required%20by%20the%20Better%20Auth%20instance%20in%0A--%20backend%2Fsrc%2Fauth%2Fbetter-auth.config.ts%2C%20mounted%20at%20%2Fapi%2Fauth.%0A--%0A--%20%20%20core%20%20%20%20%20%20%20%E2%86%92%20user%2C%20session%2C%20account%2C%20verification%0A--%20%20%20twoFactor%20%20%E2%86%92%20twoFactor%20(%2B%20user.twoFactorEnabled)%20%20%20%20%20%20%20%20%20%5Bbetter-auth%2Fplugins%5D%0A--%20%20%20organization%20%E2%86%92%20organization%2C%20member%2C%20invitation%2C%20team%2C%20%20%5Bbetter-auth%2Fplugins%5D%0A--%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20teamMember%2C%20role%0A--%20%20%20apiKey%20%20%20%20%20%E2%86%92%20apiKey%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%5B%40better-auth%2Fapi-key%5D%0A--%0A--%20Field%20lists%20were%20extracted%20from%20the%20installed%20packages%0A--%20(better-auth%401.6.26%2C%20%40better-auth%2Fapi-key%401.6.26).%20Once%20DATABASE_URL%20is%0A--%20set%2C%20reconcile%20with%20the%20canonical%20generator%3A%0A--%0A--%20%20%20cd%20backend%20%26%26%20npx%20%40better-auth%2Fcli%20generate%20--config%20src%2Fauth%2Fbetter-auth.config.ts%0A--%0A--%20The%20generated%20SQL%20is%20the%20source%20of%20truth%3B%20this%20file%20is%20the%20reproducible%0A--%20dashboard-SQL-editor%20equivalent%20(idempotent%20%E2%80%94%20safe%20to%20re-run).%0A--%0A--%20NOTE%3A%20%60user%60%20is%20quoted%20because%20it%20is%20a%20reserved%20keyword%20in%20Postgres%3B%20the%0A--%20better-auth%20pg%20adapter%20quotes%20identifiers%20the%20same%20way.%0A--%20%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%3D%0A%0A--%20---------------------------------------------------------------------------%0A--%20Core%3A%20user%0A--%20---------------------------------------------------------------------------%0Acreate%20table%20if%20not%20exists%20%22user%22%20(%0A%20%20id%20%20%20%20%20%20%20%20%20%20%20%20text%20primary%20key%2C%0A%20%20name%20%20%20%20%20%20%20%20%20%20text%20not%20null%2C%0A%20%20email%20%20%20%20%20%20%20%20%20text%20not%20null%20unique%2C%0A%20%20emailVerified%20boolean%20not%20null%20default%20false%2C%0A%20%20image%20%20%20%20%20%20%20%20%20text%2C%0A%20%20createdAt%20%20%20%20%20timestamptz%20not%20null%2C%0A%20%20updatedAt%20%20%20%20%20timestamptz%20not%20null%2C%0A%20%20--%20added%20by%20the%20twoFactor%20plugin%0A%20%20twoFactorEnabled%20boolean%0A)%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20Core%3A%20session%0A--%20---------------------------------------------------------------------------%0Acreate%20table%20if%20not%20exists%20%22session%22%20(%0A%20%20id%20%20%20%20%20%20%20%20text%20primary%20key%2C%0A%20%20expiresAt%20timestamptz%20not%20null%2C%0A%20%20token%20%20%20%20%20text%20not%20null%20unique%2C%0A%20%20createdAt%20timestamptz%20not%20null%2C%0A%20%20updatedAt%20timestamptz%20not%20null%2C%0A%20%20ipAddress%20text%2C%0A%20%20userAgent%20text%2C%0A%20%20userId%20%20%20%20text%20not%20null%20references%20%22user%22%20(id)%20on%20delete%20cascade%0A)%3B%0A%0Acreate%20index%20if%20not%20exists%20session_user_id_idx%20on%20%22session%22%20(userId)%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20Core%3A%20account%0A--%20---------------------------------------------------------------------------%0Acreate%20table%20if%20not%20exists%20%22account%22%20(%0A%20%20id%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20text%20primary%20key%2C%0A%20%20accountId%20%20%20%20%20%20%20%20%20%20%20%20%20text%20not%20null%2C%0A%20%20providerId%20%20%20%20%20%20%20%20%20%20%20%20text%20not%20null%2C%0A%20%20userId%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20text%20not%20null%20references%20%22user%22%20(id)%20on%20delete%20cascade%2C%0A%20%20accessToken%20%20%20%20%20%20%20%20%20%20%20text%2C%0A%20%20refreshToken%20%20%20%20%20%20%20%20%20%20text%2C%0A%20%20idToken%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20text%2C%0A%20%20accessTokenExpiresAt%20%20timestamptz%2C%0A%20%20refreshTokenExpiresAt%20timestamptz%2C%0A%20%20scope%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20text%2C%0A%20%20password%20%20%20%20%20%20%20%20%20%20%20%20%20%20text%2C%0A%20%20createdAt%20%20%20%20%20%20%20%20%20%20%20%20%20timestamptz%20not%20null%2C%0A%20%20updatedAt%20%20%20%20%20%20%20%20%20%20%20%20%20timestamptz%20not%20null%0A)%3B%0A%0Acreate%20index%20if%20not%20exists%20account_user_id_idx%20on%20%22account%22%20(userId)%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20Core%3A%20verification%0A--%20---------------------------------------------------------------------------%0Acreate%20table%20if%20not%20exists%20%22verification%22%20(%0A%20%20id%20%20%20%20%20%20%20%20%20text%20primary%20key%2C%0A%20%20identifier%20text%20not%20null%2C%0A%20%20value%20%20%20%20%20%20text%20not%20null%2C%0A%20%20expiresAt%20%20timestamptz%20not%20null%2C%0A%20%20createdAt%20%20timestamptz%2C%0A%20%20updatedAt%20%20timestamptz%0A)%3B%0A%0Acreate%20index%20if%20not%20exists%20verification_identifier_idx%20on%20%22verification%22%20(identifier)%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20twoFactor%20plugin%0A--%20---------------------------------------------------------------------------%0Acreate%20table%20if%20not%20exists%20%22twoFactor%22%20(%0A%20%20id%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20text%20primary%20key%2C%0A%20%20userId%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20text%20not%20null%20references%20%22user%22%20(id)%20on%20delete%20cascade%2C%0A%20%20secret%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20text%20not%20null%2C%0A%20%20backupCodes%20%20%20%20%20%20%20%20%20%20%20%20%20text%2C%0A%20%20verified%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20boolean%20not%20null%20default%20false%2C%0A%20%20failedVerificationCount%20integer%20not%20null%20default%200%2C%0A%20%20lockedUntil%20%20%20%20%20%20%20%20%20%20%20%20%20timestamptz%0A)%3B%0A%0Acreate%20unique%20index%20if%20not%20exists%20two_factor_user_id_idx%20on%20%22twoFactor%22%20(userId)%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20organization%20plugin%20%E2%80%94%20organization%0A--%20---------------------------------------------------------------------------%0Acreate%20table%20if%20not%20exists%20%22organization%22%20(%0A%20%20id%20%20%20%20%20%20%20%20text%20primary%20key%2C%0A%20%20name%20%20%20%20%20%20text%20not%20null%2C%0A%20%20slug%20%20%20%20%20%20text%20not%20null%20unique%2C%0A%20%20logo%20%20%20%20%20%20text%2C%0A%20%20metadata%20%20jsonb%2C%0A%20%20createdAt%20timestamptz%20not%20null%0A)%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20organization%20plugin%20%E2%80%94%20team%0A--%20---------------------------------------------------------------------------%0Acreate%20table%20if%20not%20exists%20%22team%22%20(%0A%20%20id%20%20%20%20%20%20%20%20%20%20%20%20%20text%20primary%20key%2C%0A%20%20name%20%20%20%20%20%20%20%20%20%20%20text%20not%20null%2C%0A%20%20organizationId%20text%20not%20null%20references%20%22organization%22%20(id)%20on%20delete%20cascade%2C%0A%20%20createdAt%20%20%20%20%20%20timestamptz%20not%20null%2C%0A%20%20updatedAt%20%20%20%20%20%20timestamptz%0A)%3B%0A%0Acreate%20index%20if%20not%20exists%20team_organization_id_idx%20on%20%22team%22%20(organizationId)%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20organization%20plugin%20%E2%80%94%20member%0A--%20---------------------------------------------------------------------------%0Acreate%20table%20if%20not%20exists%20%22member%22%20(%0A%20%20id%20%20%20%20%20%20%20%20%20%20%20%20%20text%20primary%20key%2C%0A%20%20organizationId%20text%20not%20null%20references%20%22organization%22%20(id)%20on%20delete%20cascade%2C%0A%20%20userId%20%20%20%20%20%20%20%20%20text%20not%20null%20references%20%22user%22%20(id)%20on%20delete%20cascade%2C%0A%20%20role%20%20%20%20%20%20%20%20%20%20%20text%20not%20null%2C%0A%20%20createdAt%20%20%20%20%20%20timestamptz%20not%20null%20default%20now()%0A)%3B%0A%0Acreate%20unique%20index%20if%20not%20exists%20member_org_user_idx%20on%20%22member%22%20(organizationId%2C%20userId)%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20organization%20plugin%20%E2%80%94%20teamMember%0A--%20---------------------------------------------------------------------------%0Acreate%20table%20if%20not%20exists%20%22teamMember%22%20(%0A%20%20id%20%20%20%20%20%20%20%20text%20primary%20key%2C%0A%20%20teamId%20%20%20%20text%20not%20null%20references%20%22team%22%20(id)%20on%20delete%20cascade%2C%0A%20%20userId%20%20%20%20text%20not%20null%20references%20%22user%22%20(id)%20on%20delete%20cascade%2C%0A%20%20createdAt%20timestamptz%20not%20null%20default%20now()%0A)%3B%0A%0Acreate%20unique%20index%20if%20not%20exists%20team_member_team_user_idx%20on%20%22teamMember%22%20(teamId%2C%20userId)%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20organization%20plugin%20%E2%80%94%20invitation%0A--%20---------------------------------------------------------------------------%0Acreate%20table%20if%20not%20exists%20%22invitation%22%20(%0A%20%20id%20%20%20%20%20%20%20%20%20%20%20%20%20text%20primary%20key%2C%0A%20%20organizationId%20text%20not%20null%20references%20%22organization%22%20(id)%20on%20delete%20cascade%2C%0A%20%20email%20%20%20%20%20%20%20%20%20%20text%20not%20null%2C%0A%20%20role%20%20%20%20%20%20%20%20%20%20%20text%20not%20null%2C%0A%20%20status%20%20%20%20%20%20%20%20%20text%20not%20null%20default%20'pending'%0A%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20check%20(status%20in%20('pending'%2C%20'accepted'%2C%20'rejected'%2C%20'canceled'))%2C%0A%20%20teamId%20%20%20%20%20%20%20%20%20text%20references%20%22team%22%20(id)%20on%20delete%20set%20null%2C%0A%20%20inviterId%20%20%20%20%20%20text%20not%20null%2C%0A%20%20expiresAt%20%20%20%20%20%20timestamptz%20not%20null%2C%0A%20%20createdAt%20%20%20%20%20%20timestamptz%20not%20null%20default%20now()%0A)%3B%0A%0Acreate%20index%20if%20not%20exists%20invitation_org_status_idx%20on%20%22invitation%22%20(organizationId%2C%20status)%3B%0Acreate%20index%20if%20not%20exists%20invitation_email_idx%20on%20%22invitation%22%20(email)%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20organization%20plugin%20%E2%80%94%20role%20(custom%20role%20%E2%86%92%20permission%20map)%0A--%20---------------------------------------------------------------------------%0Acreate%20table%20if%20not%20exists%20%22role%22%20(%0A%20%20id%20%20%20%20%20%20%20%20%20%20%20%20%20text%20primary%20key%2C%0A%20%20organizationId%20text%20not%20null%20references%20%22organization%22%20(id)%20on%20delete%20cascade%2C%0A%20%20role%20%20%20%20%20%20%20%20%20%20%20text%20not%20null%2C%0A%20%20permission%20%20%20%20%20jsonb%20not%20null%20default%20'%7B%7D'%2C%0A%20%20createdAt%20%20%20%20%20%20timestamptz%20not%20null%20default%20now()%2C%0A%20%20updatedAt%20%20%20%20%20%20timestamptz%0A)%3B%0A%0Acreate%20unique%20index%20if%20not%20exists%20role_org_role_idx%20on%20%22role%22%20(organizationId%2C%20role)%3B%0A%0A--%20---------------------------------------------------------------------------%0A--%20apiKey%20plugin%20(%40better-auth%2Fapi-key%2C%20references%3A%20'user')%0A--%20---------------------------------------------------------------------------%0Acreate%20table%20if%20not%20exists%20%22apiKey%22%20(%0A%20%20id%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20text%20primary%20key%2C%0A%20%20configId%20%20%20%20%20%20%20%20%20%20%20%20%20text%20not%20null%2C%0A%20%20name%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20text%2C%0A%20%20start%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20text%2C%0A%20%20prefix%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20text%2C%0A%20%20key%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20text%20not%20null%20unique%2C%0A%20%20--%20polymorphic%20owner%3A%20the%20userId%20(references%3A%20'user')%20or%20organizationId%0A%20%20--%20(references%3A%20'organization').%20No%20FK%20%E2%80%94%20the%20referenced%20entity%20varies.%0A%20%20referenceId%20%20%20%20%20%20%20%20%20%20text%20not%20null%2C%0A%20%20refillInterval%20%20%20%20%20%20%20integer%2C%0A%20%20refillAmount%20%20%20%20%20%20%20%20%20integer%2C%0A%20%20lastRefillAt%20%20%20%20%20%20%20%20%20timestamptz%2C%0A%20%20enabled%20%20%20%20%20%20%20%20%20%20%20%20%20%20boolean%20not%20null%20default%20true%2C%0A%20%20rateLimitEnabled%20%20%20%20%20boolean%20not%20null%20default%20false%2C%0A%20%20rateLimitTimeWindow%20%20integer%2C%0A%20%20rateLimitMax%20%20%20%20%20%20%20%20%20integer%2C%0A%20%20requestCount%20%20%20%20%20%20%20%20%20integer%20not%20null%20default%200%2C%0A%20%20remaining%20%20%20%20%20%20%20%20%20%20%20%20integer%2C%0A%20%20lastRequest%20%20%20%20%20%20%20%20%20%20timestamptz%2C%0A%20%20expiresAt%20%20%20%20%20%20%20%20%20%20%20%20timestamptz%2C%0A%20%20createdAt%20%20%20%20%20%20%20%20%20%20%20%20timestamptz%20not%20null%2C%0A%20%20updatedAt%20%20%20%20%20%20%20%20%20%20%20%20timestamptz%20not%20null%2C%0A%20%20metadata%20%20%20%20%20%20%20%20%20%20%20%20%20jsonb%2C%0A%20%20permissions%20%20%20%20%20%20%20%20%20%20jsonb%0A)%3B%0A%0Acreate%20index%20if%20not%20exists%20api_key_reference_id_idx%20on%20%22apiKey%22%20(referenceId)%3B) |
| [ ] | 0019 | `0019_scan_idempotency.sql` | idempotent POST /scans (idempotency_key) | 1 column | [Open →](https://supabase.com/dashboard/project/dmhrwdcuwtgscwlaagsa/sql/new?query=--%200019%3A%20scan%20initiate%20idempotency.%0A--%0A--%20POST%20%2Fscans%20accepts%20an%20Idempotency-Key%20header%20so%20a%20client%20retry%20(network%0A--%20blip%2C%20double-submit)%20cannot%20create%20duplicate%20scan%20records.%20The%20key%20is%0A--%20stored%20per%20user%2C%20and%20the%20partial%20unique%20index%20scopes%20the%20guarantee%20to%20the%0A--%20awaiting_upload%20window%20%E2%80%94%20once%20a%20scan%20leaves%20that%20state%20(submitted%2C%0A--%20completed%2C%20failed)%2C%20the%20same%20key%20starts%20a%20fresh%20record%20instead%20of%20silently%0A--%20reusing%20a%20submitted%20one.%0A%0Aalter%20table%20public.scans%0A%20%20add%20column%20if%20not%20exists%20idempotency_key%20text%3B%0A%0Acreate%20unique%20index%20if%20not%20exists%20scans_user_idempotency_awaiting_idx%0A%20%20on%20public.scans%20(user_id%2C%20idempotency_key)%0A%20%20where%20status%20%3D%20'awaiting_upload'%3B) |
| [ ] | 0020 | `0020_api_usage.sql` | API usage (billing meters) | 1 table | [Open →](https://supabase.com/dashboard/project/dmhrwdcuwtgscwlaagsa/sql/new?query=--%20Per-user%20monthly%20API-call%20metering%20for%20the%20Billing%20page's%20apiCalls%20meter.%0A--%0A--%20The%20billing%20service%20reads%20this%20table%20(service-role%20only)%20to%20report%0A--%20apiCallsUsed%2FapiCallsLimit%20on%20GET%20%2Fv1%2Fbilling%3B%20the%20per-plan%20call%20limit%20comes%0A--%20from%20the%20plan%20catalog%20in%20backend%2Fsrc%2Fbilling%2Fbilling.service.ts%20(not%20the%0A--%20table)%20so%20limits%20stay%20code-configurable%20per%20plan.%0A--%0A--%20One%20row%20per%20(user_id%2C%20period_month)%20%E2%80%94%20the%20calendar%20month%20in%20UTC%2C%20stored%20as%0A--%20'YYYY-MM'.%20A%20row%20is%20created%20lazily%20on%20first%20write%3B%20reads%20treat%20a%20missing%20row%0A--%20as%20zero%20usage%2C%20so%20a%20fresh%20DB%20never%20breaks%20the%20billing%20payload.%0A--%0A--%20RLS%20is%20enabled%20with%20no%20public%20policies%3A%20the%20service%20role%20bypasses%20RLS%2C%20so%0A--%20only%20the%20backend%20can%20read%2Fwrite.%20The%20anon%2Fauthenticated%20keys%20cannot%20touch%0A--%20it%20%E2%80%94%20consistent%20with%20user_sessions%20and%20the%20other%20backend-owned%20tables.%0A%0Acreate%20table%20if%20not%20exists%20public.api_usage%20(%0A%20%20id%20text%20primary%20key%20default%20gen_random_uuid()%3A%3Atext%2C%0A%20%20user_id%20uuid%20not%20null%20references%20auth.users%20(id)%20on%20delete%20cascade%2C%0A%20%20--%20'YYYY-MM'%20(UTC)%20%E2%80%94%20the%20billing%20cycle%20the%20usage%20belongs%20to.%0A%20%20period_month%20text%20not%20null%2C%0A%20%20calls%20integer%20not%20null%20default%200%2C%0A%20%20created_at%20timestamptz%20not%20null%20default%20now()%2C%0A%20%20updated_at%20timestamptz%20not%20null%20default%20now()%2C%0A%20%20unique%20(user_id%2C%20period_month)%0A)%3B%0A%0Aalter%20table%20public.api_usage%20enable%20row%20level%20security%3B%0A%0Acreate%20index%20if%20not%20exists%20api_usage_user_period_idx%0A%20%20on%20public.api_usage%20(user_id%2C%20period_month)%3B) |
| [ ] | 0021 | `0021_scan_attempts.sql` | scan attempt telemetry (attempts_made / max_attempts) | 2 columns | [Open →](https://supabase.com/dashboard/project/dmhrwdcuwtgscwlaagsa/sql/new?query=--%200021%3A%20scan%20retry%20telemetry.%0A--%0A--%20The%20worker%20(BullMQ%20path)%20retries%20a%20failing%20scan%20up%20to%20%60attempts%3A%203%60%20with%0A--%20exponential%20backoff%20before%20landing%20it%20in%20the%20terminal%20'failed'%20state.%20This%0A--%20migration%20records%20how%20many%20attempts%20the%20worker%20actually%20burned%20so%20the%20admin%0A--%20Jobs%20page%20can%20show%20operators%20the%20real%20retry%20count%20instead%20of%20a%20neutral%0A--%20default%3A%20scans.attempts_made%20%3D%20attempts%20burned%2C%20scans.max_attempts%20%3D%20the%0A--%20configured%20retry%20budget%20at%20enqueue%20time%20(3%20for%20BullMQ%2C%201%20for%20the%20inline%0A--%20no-retry%20path).%0A--%0A--%20Both%20columns%20default%20to%20a%20sane%20fallback%20so%20rows%20written%20before%20this%0A--%20migration%20(or%20by%20paths%20that%20do%20not%20report%20attempts)%20render%201%2F3%20rather%20than%0A--%20null%3A%20a%20failed%20scan%20with%20no%20telemetry%20means%20%22failed%20on%20its%20first%20attempt%22.%0A%0Aalter%20table%20public.scans%0A%20%20add%20column%20if%20not%20exists%20attempts_made%20integer%20not%20null%20default%201%3B%0A%0Aalter%20table%20public.scans%0A%20%20add%20column%20if%20not%20exists%20max_attempts%20integer%20not%20null%20default%203%3B) |

**After the paste** — one command: `cd backend && npm run validate:migrations` (expect `applied: 20 · missing: 0 · skipped: 1`), then `curl http://localhost:4000/v1/health/readiness` (expect `"status": "ready"`). Or skip the dashboard entirely once `DATABASE_URL` is set in `backend/.env.local`: `npm run apply:migrations -- --verify`.

<details><summary>📋 Full combined block — paste once (all pending migrations, dependency order)</summary>

```sql
-- =====================================================================
-- MIGRATION 0005 · 0005_organization.sql
-- =====================================================================
-- Organization workspace schema: organizations, teams, members, invites.
--
-- NOTE: this is a NEW migration (0005), not an edit to 0002_scans.sql — that
-- migration is scans-only and is already applied to the remote Supabase
-- project, so editing it would create drift. See
-- docs/engineering/ORGANIZATION_API_CONTRACT.md for the full contract.

create extension if not exists pgcrypto;

-- ---------------------------------------------------------------------------
-- Organizations
-- ---------------------------------------------------------------------------

create table if not exists public.organizations (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  slug text unique,
  plan text not null default 'pro',
  seats integer not null default 1,
  storage_limit_gb numeric not null default 50,
  storage_used_gb numeric not null default 0,
  scan_count integer not null default 0,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- ---------------------------------------------------------------------------
-- Teams (scoped to an organization)
-- ---------------------------------------------------------------------------

create table if not exists public.teams (
  id uuid primary key default gen_random_uuid(),
  organization_id uuid not null references public.organizations(id) on delete cascade,
  name text not null,
  description text,
  created_at timestamptz not null default now()
);

-- ---------------------------------------------------------------------------
-- Members (profile user <-> organization join)
-- ---------------------------------------------------------------------------

create table if not exists public.organization_members (
  organization_id uuid not null references public.organizations(id) on delete cascade,
  user_id uuid not null references auth.users(id) on delete cascade,
  role text not null default 'member'
    check (role in ('owner', 'admin', 'member')),
  team_id uuid references public.teams(id) on delete set null,
  status text not null default 'active'
    check (status in ('active', 'invited')),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (organization_id, user_id)
);

-- ---------------------------------------------------------------------------
-- Invites
-- ---------------------------------------------------------------------------

create table if not exists public.organization_invites (
  id uuid primary key default gen_random_uuid(),
  organization_id uuid not null references public.organizations(id) on delete cascade,
  email text not null,
  role text not null default 'member'
    check (role in ('admin', 'member')),
  team_id uuid references public.teams(id) on delete set null,
  token text not null unique default gen_random_uuid()::text,
  status text not null default 'pending'
    check (status in ('pending', 'accepted', 'cancelled', 'expired')),
  invited_by uuid references auth.users(id) on delete set null,
  expires_at timestamptz not null default (now() + interval '7 days'),
  created_at timestamptz not null default now()
);

-- ---------------------------------------------------------------------------
-- Indexes for the hot org-scoped queries (FKs are not auto-indexed in PG)
-- ---------------------------------------------------------------------------

create index if not exists teams_organization_id_idx on public.teams (organization_id);
create index if not exists organization_invites_org_status_idx
  on public.organization_invites (organization_id, status);

-- ---------------------------------------------------------------------------
-- Row level security
--
-- The backend writes through the service-role admin client (RLS bypassed),
-- so these policies protect direct Supabase access: any member of an
-- organization can read its org, teams, members, and invites. Writes are
-- intentionally backend-only (no direct insert/update/delete policies).
-- ---------------------------------------------------------------------------

alter table public.organizations enable row level security;
alter table public.teams enable row level security;
alter table public.organization_members enable row level security;
alter table public.organization_invites enable row level security;

drop policy if exists "Organization is viewable by members" on public.organizations;
create policy "Organization is viewable by members"
on public.organizations
for select
to authenticated
using (
  exists (
    select 1 from public.organization_members m
    where m.organization_id = organizations.id and m.user_id = auth.uid()
  )
);

drop policy if exists "Teams are viewable by members" on public.teams;
create policy "Teams are viewable by members"
on public.teams
for select
to authenticated
using (
  exists (
    select 1 from public.organization_members m
    where m.organization_id = teams.organization_id and m.user_id = auth.uid()
  )
);

drop policy if exists "Members are viewable by org members" on public.organization_members;
create policy "Members are viewable by org members"
on public.organization_members
for select
to authenticated
using (
  exists (
    select 1 from public.organization_members m
    where m.organization_id = organization_members.organization_id
      and m.user_id = auth.uid()
  )
);

drop policy if exists "Invites are viewable by org members" on public.organization_invites;
create policy "Invites are viewable by org members"
on public.organization_invites
for select
to authenticated
using (
  exists (
    select 1 from public.organization_members m
    where m.organization_id = organization_invites.organization_id
      and m.user_id = auth.uid()
  )
);

-- ---------------------------------------------------------------------------
-- Updated-at triggers
-- ---------------------------------------------------------------------------

drop trigger if exists organizations_set_updated_at on public.organizations;
create trigger organizations_set_updated_at
before update on public.organizations
for each row
execute function public.set_updated_at();

drop trigger if exists organization_members_set_updated_at on public.organization_members;
create trigger organization_members_set_updated_at
before update on public.organization_members
for each row
execute function public.set_updated_at();

-- =====================================================================
-- MIGRATION 0007 · 0007_incidents.sql
-- =====================================================================
-- Admin monitoring incidents. Seeded rows mirror the frontend mockMonitoring
-- incidents so the real path is immediately useful; the seed uses relative
-- timestamps computed at migration time.

create table if not exists public.admin_incidents (
  id text primary key,
  title text not null,
  severity text not null
    check (severity in ('critical', 'major', 'minor')),
  status text not null
    check (status in ('investigating', 'monitoring', 'resolved')),
  started_at timestamptz not null,
  resolved_at timestamptz,
  duration_hours numeric,
  services text[] not null default '{}',
  summary text not null default '',
  created_at timestamptz not null default now()
);

insert into public.admin_incidents
  (id, title, severity, status, started_at, resolved_at, duration_hours, services, summary)
values
  (
    'inc_001',
    'Scan worker partial outage',
    'major',
    'resolved',
    now() - interval '6 days 2 hours',
    now() - interval '5 days 9 hours',
    7,
    array['Scan Worker'],
    'A memory leak in the fingerprint model worker stalled processing for roughly a third of the queue. A rollback to the previous model release restored throughput.'
  ),
  (
    'inc_002',
    'Elevated API latency',
    'minor',
    'resolved',
    now() - interval '12 days 4 hours',
    now() - interval '12 days 11 hours',
    7,
    array['API Gateway'],
    'Autoscaling lag under a waitlist invite burst pushed p95 latency above target for seven hours. Autoscaling thresholds were retuned.'
  ),
  (
    'inc_003',
    'Storage upload errors',
    'major',
    'resolved',
    now() - interval '19 days 6 hours',
    now() - interval '19 days 9 hours',
    3,
    array['Object Storage (R2)'],
    'Signed upload URLs expired early under load, rejecting a subset of media uploads. The signing window was extended and the worker retries added.'
  ),
  (
    'inc_004',
    'Scan worker memory pressure',
    'major',
    'investigating',
    now() - interval '4 hours',
    null,
    null,
    array['Scan Worker'],
    'Resident memory on the worker pool is trending upward since the model update. Monitoring is active while a candidate fix is validated.'
  ),
  (
    'inc_005',
    'Database connection pool exhaustion',
    'critical',
    'resolved',
    now() - interval '25 days 3 hours',
    now() - interval '25 days 5 hours',
    2,
    array['Postgres (Neon)'],
    'A runaway query pattern exhausted the connection pool, causing intermittent timeouts. The query was optimized and pool limits raised.'
  )
on conflict (id) do nothing;

-- Incident severity + status lookups are the hot path on the monitoring page.
create index if not exists admin_incidents_status_idx on public.admin_incidents (status);

-- =====================================================================
-- MIGRATION 0008 · 0008_audit_logs.sql
-- =====================================================================
-- Admin audit log. Written by backend services (service-role only) and read
-- by the admin Audit Logs endpoint (GET /admin/audit-logs). Seeded rows mirror
-- the frontend mockAuditEvents shape ({id, actor_email, action, severity,
-- resource_type, resource_id, created_at}) so the real path is immediately
-- useful; the seed uses relative timestamps computed at migration time.
--
-- RLS is enabled with no public policies: the Supabase service role bypasses
-- RLS, so only the backend can read/write the trail. The auth.audit_log_entries
-- table is left untouched — this is the application-level audit trail.

create table if not exists public.audit_logs (
  id text primary key default gen_random_uuid()::text,
  actor_email text not null,
  action text not null,
  severity text not null default 'low'
    check (severity in ('critical', 'high', 'medium', 'low')),
  entity_type text,
  entity_id text,
  details jsonb,
  created_at timestamptz not null default now()
);

alter table public.audit_logs enable row level security;

insert into public.audit_logs
  (id, actor_email, action, severity, entity_type, entity_id, created_at)
values
  ('audit_0001', 'founder.admin@provance.local', 'user.invited',   'medium', 'user', 'usr_002', now() - interval '0 hours'),
  ('audit_0002', 'aisha.khan@provance.local',   'scan.submitted',  'medium', 'scan', 'scan_0042', now() - interval '2 hours'),
  ('audit_0003', 'aisha.khan@provance.local',   'scan.completed',  'low',    'scan', 'scan_0042', now() - interval '4 hours'),
  ('audit_0004', 'founder.admin@provance.local', 'role.changed',   'high',   'role', 'role_analyst', now() - interval '8 hours'),
  ('audit_0005', 'david.okafor@provance.local', 'report.exported', 'low',   'report', 'rpt_0042', now() - interval '10 hours'),
  ('audit_0006', 'system',                      'scan.failed',     'high',   'scan', 'scan_0041', now() - interval '1 day 2 hours'),
  ('audit_0007', 'founder.admin@provance.local', 'waitlist.approved', 'medium', 'waitlist_application', 'wl_0017', now() - interval '1 day 8 hours'),
  ('audit_0008', 'lucia.mendes@provance.local', 'team.member_added', 'medium', 'team', 'team_legal', now() - interval '2 days 1 hour'),
  ('audit_0009', 'founder.admin@provance.local', 'feature_flag.toggled', 'high', 'feature_flag', 'ff_deep_scan', now() - interval '2 days 6 hours'),
  ('audit_0010', 'david.okafor@provance.local', 'api_key.created', 'medium', 'api_key', 'key_0012', now() - interval '3 days 3 hours'),
  ('audit_0011', 'founder.admin@provance.local', 'settings.updated', 'low',  'settings', 'retention', now() - interval '3 days 9 hours'),
  ('audit_0012', 'aisha.khan@provance.local',   'report.viewed',   'low',    'report', 'rpt_0038', now() - interval '4 days 2 hours'),
  ('audit_0013', 'lucia.mendes@provance.local', 'scan.submitted',  'medium', 'scan', 'scan_0039', now() - interval '5 days 5 hours'),
  ('audit_0014', 'founder.admin@provance.local', 'org.created',    'medium', 'organization', 'org_0001', now() - interval '6 days 1 hour'),
  ('audit_0015', 'system',                      'waitlist.rejected', 'high', 'waitlist_application', 'wl_0009', now() - interval '6 days 8 hours')
on conflict (id) do nothing;

-- The endpoint orders by created_at desc and the page filters by action /
-- severity / actor, so those are the hot paths.
create index if not exists audit_logs_created_at_idx on public.audit_logs (created_at desc);
create index if not exists audit_logs_action_idx on public.audit_logs (action);
create index if not exists audit_logs_severity_idx on public.audit_logs (severity);

-- =====================================================================
-- MIGRATION 0009 · 0009_scan_processing.sql
-- =====================================================================
-- 0009: scan processing metadata.
--
-- The mock scan rows the frontend consumes carry processing_mode, team_id, and
-- completed_at. The real scans table needs them so the API boundary can emit
-- the same row dialect for queue/ledger/report surfaces.

alter table public.scans
  add column if not exists processing_mode text not null default 'standard',
  add column if not exists team_id uuid,
  add column if not exists completed_at timestamptz;

-- The queue snapshot and admin analytics aggregate on status; keep the common
-- filters indexed.
create index if not exists scans_status_idx on public.scans (status);
create index if not exists scans_user_status_idx on public.scans (user_id, status);

-- =====================================================================
-- MIGRATION 0010 · 0010_user_sessions.sql
-- =====================================================================
-- User session ledger and per-user security settings.
--
-- Written by the backend security service (service-role only): every
-- sign-in / session refresh upserts a row keyed by (user_id, auth_session_id)
-- where auth_session_id is the `sid` claim of the Supabase access-token JWT
-- (the same id GoTrue uses in auth.sessions). Only a SHA-256 hash of the
-- refresh token is stored — never the raw token — so a DB leak cannot replay
-- a session credential; revocation goes through the GoTrue admin API by
-- session id instead.
--
-- RLS is enabled with no public policies: the service role bypasses RLS, so
-- only the backend can read/write the ledger. The anon/authenticated keys
-- cannot touch it.
--
-- The security endpoints degrade gracefully when this migration is not yet
-- applied (settings default, sessions render empty) so a fresh DB never
-- blocks sign-in — consistent with the billing plan fallback.

create table if not exists public.user_sessions (
  id text primary key default gen_random_uuid()::text,
  user_id uuid not null references auth.users (id) on delete cascade,
  -- The `sid` claim of the Supabase access token — the durable key that
  -- survives refresh-token rotation (a refresh keeps the same auth session).
  auth_session_id text not null,
  -- SHA-256 hex of the current refresh token. Used to clean the ledger row
  -- on sign-out, where only the cookie refresh token is available.
  refresh_token_hash text,
  device text not null default 'Unknown device',
  ip_address text,
  location text,
  created_at timestamptz not null default now(),
  last_active_at timestamptz not null default now(),
  unique (user_id, auth_session_id)
);

alter table public.user_sessions enable row level security;

create index if not exists user_sessions_user_last_active_idx
  on public.user_sessions (user_id, last_active_at desc);

-- Per-user sign-in controls (2FA flag, session timeout, notify preferences).
-- Mirrors the frontend mockSecuritySettings.signInControls contract.
create table if not exists public.user_security_settings (
  user_id uuid primary key references auth.users (id) on delete cascade,
  two_factor_enabled boolean not null default false,
  session_timeout_minutes integer not null default 60,
  notify_on_new_device boolean not null default false,
  notify_on_password_change boolean not null default false,
  updated_at timestamptz not null default now()
);

alter table public.user_security_settings enable row level security;

-- =====================================================================
-- MIGRATION 0011 · 0011_notifications.sql
-- =====================================================================
-- User-scoped notifications for the in-app bell and notification center.
--
-- Written by the backend notifications service (service-role only): rows are
-- keyed by user_id so every query is scoped to the signed-in user — the same
-- identity pattern as auth_audit_events (actor_email) but with a proper FK so
-- the dashboard + bell surfaces can join and filter by user.
--
-- The column set mirrors the frontend mockNotifications contract:
--   { id, category, title, description, read, link, created_at }
-- `read` is stored as is_read (read is a SQL-reserved-ish keyword in some
-- tooling) and mapped back in the service response.
--
-- RLS is enabled with no public policies: the service role bypasses RLS, so
-- only the backend can read/write. The anon/authenticated keys cannot touch
-- it — consistent with user_sessions and the other backend-owned tables.
--
-- The notifications endpoints degrade gracefully when this migration is not
-- yet applied (list returns empty, mark-read is a no-op) so a fresh DB never
-- blocks the app shell.

create table if not exists public.notifications (
  id text primary key default gen_random_uuid()::text,
  user_id uuid not null references auth.users (id) on delete cascade,
  category text not null default 'system',
  title text not null,
  description text,
  -- `read` is a keyword in a few SQL dialects; is_read is unambiguous.
  is_read boolean not null default false,
  -- Optional deep link (e.g. /app/reports/rpt_001) the bell/page navigate to.
  link text,
  created_at timestamptz not null default now()
);

alter table public.notifications enable row level security;

create index if not exists notifications_user_created_idx
  on public.notifications (user_id, created_at desc);

-- =====================================================================
-- MIGRATION 0012 · 0012_profiles_team.sql
-- =====================================================================
-- 0012: team assignment on the user directory (profiles).
--
-- The admin Users page renders each user's team badge and drives its team
-- filter from the user row's team_id (mirroring mockUsers.team_id). The
-- organization_members table is the relational source of truth for team
-- assignment, but the user-directory query path reads a flat profile row —
-- so profiles gains a denormalized team_id, backfilled from the first
-- active membership per user. New assignments are written by the org
-- service (updateMemberTeam) which keeps both columns in sync.

alter table public.profiles
  add column if not exists team_id uuid references public.teams(id) on delete set null;

create index if not exists profiles_team_id_idx on public.profiles (team_id);

-- Backfill: first active membership's team per user (created_at asc).
update public.profiles p
set team_id = m.team_id
from (
  select distinct on (user_id) user_id, team_id
  from public.organization_members
  where team_id is not null and status = 'active'
  order by user_id, created_at asc
) m
where m.user_id = p.user_id
  and p.team_id is null;

-- =====================================================================
-- MIGRATION 0013 · 0013_scan_dedup.sql
-- =====================================================================
-- 0013: scan deduplication.
--
-- Hash-based dedup: when a user submits a scan whose SHA-256 matches a prior
-- completed scan of theirs, the backend skips reprocessing and reuses the
-- prior result payload (see scans.service.ts submitScan). The hash is written
-- at submit time so the lookup is an indexed equality, not a payload scan.

alter table public.scans
  add column if not exists file_hash_sha256 text;

-- The dedup lookup filters by user + hash on completed rows only.
create index if not exists scans_user_hash_complete_idx
  on public.scans (user_id, file_hash_sha256)
  where status = 'complete';

-- =====================================================================
-- MIGRATION 0014 · 0014_crash_reports.sql
-- =====================================================================
-- 0014: client crash reports (pre-Sentry telemetry).
--
-- Backs POST /v1/telemetry/errors, where the frontend flushes its buffered
-- render-error records (src/lib/telemetry.js). client_id is the client's
-- `cr-…` record id and the primary key, so a retried flush upserts in place
-- instead of duplicating the same crash.

create table if not exists public.crash_reports (
  client_id text primary key,
  type text not null default 'render_error',
  message text not null default '',
  stack text,
  component_stack text,
  route text,
  user_agent text,
  user_id text,
  email text,
  meta jsonb not null default '{}',
  reported_at timestamptz,
  received_at timestamptz not null default now()
);

-- Triage ordering for a future admin crash surface (newest first).
create index if not exists crash_reports_reported_at_idx
  on public.crash_reports (reported_at desc);

-- =====================================================================
-- MIGRATION 0015 · 0015_invite_token_hash.sql
-- =====================================================================
-- 0015: org invite token hardening — store only a SHA-256 hash.
--
-- organization_invites.token previously held the raw invite token, so a
-- leaked invites table exposed usable acceptance tokens. The app now writes
-- token_hash (sha256 hex of the raw token) and matches acceptance by hash;
-- the raw token leaves the backend only in the inviteMember response, so it
-- travels via the share/email link and is never persisted.

alter table public.organization_invites
  add column if not exists token_hash text;

-- Backfill existing pending invites from their raw tokens so they stay
-- acceptable after this deploy (raw values are hashed in place and never
-- re-issued).
update public.organization_invites
set token_hash = encode(sha256(token::bytea), 'hex')
where token_hash is null and token is not null;

-- One accepted token per invite — partial so nulls never collide.
create unique index if not exists organization_invites_token_hash_idx
  on public.organization_invites (token_hash)
  where token_hash is not null;

-- Deprecate the raw column: neutralize the default + not-null so future rows
-- store no raw token bytes. The column itself is dropped in a follow-up
-- migration once this deploy is confirmed live.
alter table public.organization_invites
  alter column token drop default,
  alter column token drop not null;

-- =====================================================================
-- MIGRATION 0016 · 0016_role_scopes.sql
-- =====================================================================
-- Role scope grants. The RBAC role matrix (ADMIN_ROLES in the backend roles
-- module) declares the default scope grants as product config; this table
-- holds the *overrides* saved through PATCH /admin/roles/:roleId/scopes so
-- permission edits survive restarts. The list endpoint merges these rows over
-- the defaults (DB wins), which keeps new scope keys working the moment they
-- ship without a migration.
--
-- RLS is enabled with no public policies: the Supabase service role bypasses
-- RLS, so only the backend can read/write the grants (same as audit_logs).

create table if not exists public.role_scopes (
  role_id text not null,
  scope_key text not null,
  enabled boolean not null default true,
  updated_at timestamptz not null default now(),
  primary key (role_id, scope_key)
);

alter table public.role_scopes enable row level security;

create index if not exists role_scopes_role_idx on public.role_scopes (role_id);

-- =====================================================================
-- MIGRATION 0017 · 0017_user_sessions_seed.sql
-- =====================================================================
-- 0017: seed demo rows for the user_sessions ledger.
--
-- The security service writes ledger rows on every real sign-in, so the
-- table self-populates in normal use. This seed exists so a freshly-migrated
-- environment (or one where nobody has signed in yet) still renders the
-- Security page's "active sessions" surface with plausible data — the same
-- relative-timestamp pattern as the incidents (0007) and audit-log (0008)
-- seeds.
--
-- It targets the dev test account (founder.admin@provance.local, the owner
-- the seed-org script creates) and is a strict no-op otherwise: the DO block
-- bails when that auth user does not exist, so a production DB with no such
-- account is untouched, and it skips when the user already has ledger rows so
-- it never fights real sessions.
--
-- auth_session_id values are UUID-format demo ids, distinct from any real
-- GoTrue session id (real ones never collide with the '00000000-0000-…'
-- prefix). The refresh_token_hash columns are SHA-256 hashes of dummy refresh
-- values — never raw tokens, matching the ledger's at-rest doctrine. Note:
-- revoking a *demo* row via the Security page will fail the GoTrue admin call
-- (the fake session id does not exist there), so the UI surfaces an error
-- toast; the ledger row itself is only removed by the real sign-in/sign-out
-- lifecycle.

do $$
declare
  v_user_id uuid;
begin
  select id into v_user_id
  from auth.users
  where email = 'founder.admin@provance.local'
  limit 1;

  if v_user_id is not null and not exists (
    select 1 from public.user_sessions where user_id = v_user_id
  ) then
    insert into public.user_sessions
      (user_id, auth_session_id, refresh_token_hash, device, ip_address, location, created_at, last_active_at)
    values
      (
        v_user_id,
        '00000000-0000-4000-8000-00000000d001',
        encode(sha256('demo-refresh-current'::bytea), 'hex'),
        'Chrome 126 · macOS',
        '41.90.98.12',
        'Lagos, NG',
        now() - interval '2 hours',
        now() - interval '2 hours'
      ),
      (
        v_user_id,
        '00000000-0000-4000-8000-00000000d002',
        encode(sha256('demo-refresh-laptop'::bytea), 'hex'),
        'Firefox 128 · Windows',
        '102.89.33.7',
        'Abuja, NG',
        now() - interval '3 days',
        now() - interval '1 day'
      ),
      (
        v_user_id,
        '00000000-0000-4000-8000-00000000d003',
        encode(sha256('demo-refresh-mobile'::bytea), 'hex'),
        'Safari 17 · iOS',
        '154.120.70.4',
        'Nairobi, KE',
        now() - interval '6 days',
        now() - interval '5 days'
      );
  end if;
end $$;

-- =====================================================================
-- MIGRATION 0018 · 0018_better_auth.sql
-- =====================================================================
-- ===========================================================================
-- 0018_better_auth.sql — Better Auth schema (parallel auth provider)
--
-- Tables required by the Better Auth instance in
-- backend/src/auth/better-auth.config.ts, mounted at /api/auth.
--
--   core       → user, session, account, verification
--   twoFactor  → twoFactor (+ user.twoFactorEnabled)         [better-auth/plugins]
--   organization → organization, member, invitation, team,  [better-auth/plugins]
--                teamMember, role
--   apiKey     → apiKey                                      [@better-auth/api-key]
--
-- Field lists were extracted from the installed packages
-- (better-auth@1.6.26, @better-auth/api-key@1.6.26). Once DATABASE_URL is
-- set, reconcile with the canonical generator:
--
--   cd backend && npx @better-auth/cli generate --config src/auth/better-auth.config.ts
--
-- The generated SQL is the source of truth; this file is the reproducible
-- dashboard-SQL-editor equivalent (idempotent — safe to re-run).
--
-- NOTE: `user` is quoted because it is a reserved keyword in Postgres; the
-- better-auth pg adapter quotes identifiers the same way.
-- ===========================================================================

-- ---------------------------------------------------------------------------
-- Core: user
-- ---------------------------------------------------------------------------
create table if not exists "user" (
  id            text primary key,
  name          text not null,
  email         text not null unique,
  emailVerified boolean not null default false,
  image         text,
  createdAt     timestamptz not null,
  updatedAt     timestamptz not null,
  -- added by the twoFactor plugin
  twoFactorEnabled boolean
);

-- ---------------------------------------------------------------------------
-- Core: session
-- ---------------------------------------------------------------------------
create table if not exists "session" (
  id        text primary key,
  expiresAt timestamptz not null,
  token     text not null unique,
  createdAt timestamptz not null,
  updatedAt timestamptz not null,
  ipAddress text,
  userAgent text,
  userId    text not null references "user" (id) on delete cascade
);

create index if not exists session_user_id_idx on "session" (userId);

-- ---------------------------------------------------------------------------
-- Core: account
-- ---------------------------------------------------------------------------
create table if not exists "account" (
  id                    text primary key,
  accountId             text not null,
  providerId            text not null,
  userId                text not null references "user" (id) on delete cascade,
  accessToken           text,
  refreshToken          text,
  idToken               text,
  accessTokenExpiresAt  timestamptz,
  refreshTokenExpiresAt timestamptz,
  scope                 text,
  password              text,
  createdAt             timestamptz not null,
  updatedAt             timestamptz not null
);

create index if not exists account_user_id_idx on "account" (userId);

-- ---------------------------------------------------------------------------
-- Core: verification
-- ---------------------------------------------------------------------------
create table if not exists "verification" (
  id         text primary key,
  identifier text not null,
  value      text not null,
  expiresAt  timestamptz not null,
  createdAt  timestamptz,
  updatedAt  timestamptz
);

create index if not exists verification_identifier_idx on "verification" (identifier);

-- ---------------------------------------------------------------------------
-- twoFactor plugin
-- ---------------------------------------------------------------------------
create table if not exists "twoFactor" (
  id                      text primary key,
  userId                  text not null references "user" (id) on delete cascade,
  secret                  text not null,
  backupCodes             text,
  verified                boolean not null default false,
  failedVerificationCount integer not null default 0,
  lockedUntil             timestamptz
);

create unique index if not exists two_factor_user_id_idx on "twoFactor" (userId);

-- ---------------------------------------------------------------------------
-- organization plugin — organization
-- ---------------------------------------------------------------------------
create table if not exists "organization" (
  id        text primary key,
  name      text not null,
  slug      text not null unique,
  logo      text,
  metadata  jsonb,
  createdAt timestamptz not null
);

-- ---------------------------------------------------------------------------
-- organization plugin — team
-- ---------------------------------------------------------------------------
create table if not exists "team" (
  id             text primary key,
  name           text not null,
  organizationId text not null references "organization" (id) on delete cascade,
  createdAt      timestamptz not null,
  updatedAt      timestamptz
);

create index if not exists team_organization_id_idx on "team" (organizationId);

-- ---------------------------------------------------------------------------
-- organization plugin — member
-- ---------------------------------------------------------------------------
create table if not exists "member" (
  id             text primary key,
  organizationId text not null references "organization" (id) on delete cascade,
  userId         text not null references "user" (id) on delete cascade,
  role           text not null,
  createdAt      timestamptz not null default now()
);

create unique index if not exists member_org_user_idx on "member" (organizationId, userId);

-- ---------------------------------------------------------------------------
-- organization plugin — teamMember
-- ---------------------------------------------------------------------------
create table if not exists "teamMember" (
  id        text primary key,
  teamId    text not null references "team" (id) on delete cascade,
  userId    text not null references "user" (id) on delete cascade,
  createdAt timestamptz not null default now()
);

create unique index if not exists team_member_team_user_idx on "teamMember" (teamId, userId);

-- ---------------------------------------------------------------------------
-- organization plugin — invitation
-- ---------------------------------------------------------------------------
create table if not exists "invitation" (
  id             text primary key,
  organizationId text not null references "organization" (id) on delete cascade,
  email          text not null,
  role           text not null,
  status         text not null default 'pending'
                 check (status in ('pending', 'accepted', 'rejected', 'canceled')),
  teamId         text references "team" (id) on delete set null,
  inviterId      text not null,
  expiresAt      timestamptz not null,
  createdAt      timestamptz not null default now()
);

create index if not exists invitation_org_status_idx on "invitation" (organizationId, status);
create index if not exists invitation_email_idx on "invitation" (email);

-- ---------------------------------------------------------------------------
-- organization plugin — role (custom role → permission map)
-- ---------------------------------------------------------------------------
create table if not exists "role" (
  id             text primary key,
  organizationId text not null references "organization" (id) on delete cascade,
  role           text not null,
  permission     jsonb not null default '{}',
  createdAt      timestamptz not null default now(),
  updatedAt      timestamptz
);

create unique index if not exists role_org_role_idx on "role" (organizationId, role);

-- ---------------------------------------------------------------------------
-- apiKey plugin (@better-auth/api-key, references: 'user')
-- ---------------------------------------------------------------------------
create table if not exists "apiKey" (
  id                   text primary key,
  configId             text not null,
  name                 text,
  start                text,
  prefix               text,
  key                  text not null unique,
  -- polymorphic owner: the userId (references: 'user') or organizationId
  -- (references: 'organization'). No FK — the referenced entity varies.
  referenceId          text not null,
  refillInterval       integer,
  refillAmount         integer,
  lastRefillAt         timestamptz,
  enabled              boolean not null default true,
  rateLimitEnabled     boolean not null default false,
  rateLimitTimeWindow  integer,
  rateLimitMax         integer,
  requestCount         integer not null default 0,
  remaining            integer,
  lastRequest          timestamptz,
  expiresAt            timestamptz,
  createdAt            timestamptz not null,
  updatedAt            timestamptz not null,
  metadata             jsonb,
  permissions          jsonb
);

create index if not exists api_key_reference_id_idx on "apiKey" (referenceId);

-- =====================================================================
-- MIGRATION 0019 · 0019_scan_idempotency.sql
-- =====================================================================
-- 0019: scan initiate idempotency.
--
-- POST /scans accepts an Idempotency-Key header so a client retry (network
-- blip, double-submit) cannot create duplicate scan records. The key is
-- stored per user, and the partial unique index scopes the guarantee to the
-- awaiting_upload window — once a scan leaves that state (submitted,
-- completed, failed), the same key starts a fresh record instead of silently
-- reusing a submitted one.

alter table public.scans
  add column if not exists idempotency_key text;

create unique index if not exists scans_user_idempotency_awaiting_idx
  on public.scans (user_id, idempotency_key)
  where status = 'awaiting_upload';

-- =====================================================================
-- MIGRATION 0020 · 0020_api_usage.sql
-- =====================================================================
-- Per-user monthly API-call metering for the Billing page's apiCalls meter.
--
-- The billing service reads this table (service-role only) to report
-- apiCallsUsed/apiCallsLimit on GET /v1/billing; the per-plan call limit comes
-- from the plan catalog in backend/src/billing/billing.service.ts (not the
-- table) so limits stay code-configurable per plan.
--
-- One row per (user_id, period_month) — the calendar month in UTC, stored as
-- 'YYYY-MM'. A row is created lazily on first write; reads treat a missing row
-- as zero usage, so a fresh DB never breaks the billing payload.
--
-- RLS is enabled with no public policies: the service role bypasses RLS, so
-- only the backend can read/write. The anon/authenticated keys cannot touch
-- it — consistent with user_sessions and the other backend-owned tables.

create table if not exists public.api_usage (
  id text primary key default gen_random_uuid()::text,
  user_id uuid not null references auth.users (id) on delete cascade,
  -- 'YYYY-MM' (UTC) — the billing cycle the usage belongs to.
  period_month text not null,
  calls integer not null default 0,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (user_id, period_month)
);

alter table public.api_usage enable row level security;

create index if not exists api_usage_user_period_idx
  on public.api_usage (user_id, period_month);

-- =====================================================================
-- MIGRATION 0021 · 0021_scan_attempts.sql
-- =====================================================================
-- 0021: scan retry telemetry.
--
-- The worker (BullMQ path) retries a failing scan up to `attempts: 3` with
-- exponential backoff before landing it in the terminal 'failed' state. This
-- migration records how many attempts the worker actually burned so the admin
-- Jobs page can show operators the real retry count instead of a neutral
-- default: scans.attempts_made = attempts burned, scans.max_attempts = the
-- configured retry budget at enqueue time (3 for BullMQ, 1 for the inline
-- no-retry path).
--
-- Both columns default to a sane fallback so rows written before this
-- migration (or by paths that do not report attempts) render 1/3 rather than
-- null: a failed scan with no telemetry means "failed on its first attempt".

alter table public.scans
  add column if not exists attempts_made integer not null default 1;

alter table public.scans
  add column if not exists max_attempts integer not null default 3;

```

</details>

<!-- END FOUNDER CHECKLIST -->

## Why this runbook exists

- The project's migrations have historically been applied **manually via the
  dashboard SQL Editor** (pg-meta locks the migration runner), so there is no
  CLI `supabase db push` path to rely on.
- The configured backend project is
  `https://dmhrwdcuwtgscwlaagsa.supabase.co` (see `backend/.env.local`).
  As of **2026-08-17** (verified live by `npm run validate:migrations`)
  that project has **0001, 0002, 0003, 0004, 0006 applied** (`scans` with 4
  rows, `profiles` with 3, waitlist tables + 0003 admin_ops columns + 0006
  feature_flags) and is **missing 0005, 0007, 0008, 0009, 0010, 0011, 0012,
  0013, 0014, 0015, 0016, 0018, 0019, 0020, 0021** (all `PGRST205`/`42703`,
  plus the seed-only 0017). That state 503s `POST /v1/scans`, keeps
  `/v1/health/readiness` degraded, and fails the live
  `invite-accept.e2e-spec.ts` seed. The founder checklist at the top of this
  file is the one-paste fix.
- **Probe caveat:** verify live schema state with **non-head** selects (or
  `select('*')`) using the backend's supabase-js v2 — `head: true` / HEAD
  requests mask PostgREST error bodies (`PGRST205`/`42703`), so a v1-style
  client or `head: true` probe can report tables as present when they are
  not. The canonical check is `backend/scripts/probe-monitoring-tables.mjs`
  (which uses `select('*')` for incidents).
- **Automated guard:** `MigrationHealthService` (in `backend/src/health/`)
  diffs `supabase/migrations/` against the live schema with one non-head
  probe per migration — it logs every missing migration at startup and adds
  a `checks.migrations` entry to `GET /v1/health/readiness` (gating `ready`),
  so a half-migrated deployment is surfaced at boot and with one request.
  Keep `MIGRATION_PROBES` in sync when new migrations land; a file without a
  probe is warned about at boot (self-enforcing).
- Every file is **idempotent** (`if not exists`, `on conflict do nothing`,
  `create or replace`, `add column if not exists`), so re-running an already
  applied migration is safe. Applying the full 0003–0010 set in order is the
  intended way to converge a fresh or half-migrated project.
- The backend probes the schema live (no restart needed after applying), but
  PostgREST re-reads the schema on DDL — if a probe still reports a missing
  table seconds after a run, wait a few seconds and re-run the probe before
  assuming failure.

## Dependency order

Numeric order **is** the correct order. The only real dependencies:

| Migration | Depends on | Why |
| --------- | ---------- | --- |
| 0003 | 0001 | alters `waitlist_applications` + `access_invites` |
| 0004 | 0001 | trigger uses `public.set_updated_at()` (defined in 0001) |
| 0005 | 0001 | triggers use `set_updated_at()`; FKs to `auth.users` |
| 0009 | 0002 | alters `public.scans` (created in 0002) |
| 0010 | — | FK to `auth.users` only |

0006, 0007, 0008 are standalone (no project-schema deps).

## Applying a migration (general steps)

1. Open the Supabase dashboard → your project → **SQL Editor**.
2. Open the migration file in the repo (`supabase/migrations/<NN>_*.sql`) and
   copy its **entire contents**.
3. Paste into the editor, click **Run**.
4. Expect a green "Success" banner. If the editor reports an error, read the
   first error line (usually a missing dependency — apply the prerequisite
   migration first) and re-run.
5. Run that migration's verification check below, then move to the next file.

## Faster path: one-command applier (`apply:migrations`)

As of **2026-08-17** there is a CLI alternative that replaces the per-file
paste loop — no dashboard round-trip needed once `DATABASE_URL` is set:

```bash
cd backend
npm run apply:migrations            # apply every migration in numeric order
npm run apply:migrations -- --dry-run   # preview what WOULD run (no DB needed)
npm run apply:migrations -- --from=0009  # only 0009 and newer
```

- **Requires** `DATABASE_URL` in `backend/.env.local` (dashboard → **Connect**
  → **Session pooler** connection string, or a direct
  `db.<ref>.supabase.co:5432` URL). Until that's set, the SQL Editor paste
  loop above (or the combined block) is the only path.
- Reads `supabase/migrations/*.sql` in numeric order and executes each file
  as one multi-statement query via `pg` (simple-query mode), so `DO` blocks
  and multi-statement files run verbatim. Idempotent guards make the whole
  set safe to re-run.
- Stops at the **first failure** so dependencies stay ordered; exit code 1
  names the failing file and its first error line. Re-run after fixing.
- Prints the project ref from `SUPABASE_URL` so a wrong-project
  `DATABASE_URL` is visible before anything runs.
- Re-verify afterwards: `npm run validate:migrations` (expect
  `applied: 20 · missing: 0 · skipped: 1`) and
  `curl http://localhost:4000/v1/health/readiness` (expect `ready`).

---

## 0003 — `admin_ops.sql`

**Creates/alters:** adds `waitlist_applications.notes` and
`access_invites.invited_by`.
**Unlocks:** admin waitlist notes + invite attribution (`/v1/admin/waitlist`).

**Verify** (paste into SQL Editor — expect **2 rows**):

```sql
select column_name, table_name
from information_schema.columns
where table_schema = 'public'
  and (
    (table_name = 'waitlist_applications' and column_name = 'notes')
    or (table_name = 'access_invites' and column_name = 'invited_by')
  );
```

---

## 0004 — `profiles.sql`

**Creates:** `public.profiles` (PK `user_id` → `auth.users`), RLS owner
policies, `set_updated_at` trigger. Also ensures `pgcrypto`.
**Unlocks:** account API (`/v1/account/me`, profile update), admin users.

**Verify** (expect **1 row**):

```sql
select table_name from information_schema.tables
where table_schema = 'public' and table_name = 'profiles';
```

---

## 0005 — `organization.sql`

**Creates:** `organizations`, `teams`, `organization_members`,
`organization_invites` + RLS policies + triggers.
**Unlocks:** org API (`GET /v1/organization`, invites, member role/team,
DELETE), team scoping on scans, admin org views.

**Verify** (expect **4 rows**):

```sql
select table_name from information_schema.tables
where table_schema = 'public'
  and table_name in ('organizations', 'teams', 'organization_members', 'organization_invites');
```

---

## 0006 — `feature_flags.sql`

**Creates:** `public.feature_flags` + 10 seed rows.
**Unlocks:** feature-flag API + admin page.

**Verify** (expect **10**):

```sql
select count(*) from public.feature_flags;
```

---

## 0007 — `incidents.sql`

**Creates:** `public.admin_incidents` + 5 seed rows (relative timestamps).
**Unlocks:** admin monitoring incidents section (`GET /v1/admin/monitoring`
degrades gracefully when missing, but the section stays empty).

**Verify** (expect **5**):

```sql
select count(*) from public.admin_incidents;
```

---

## 0008 — `audit_logs.sql`

**Creates:** `public.audit_logs` + 15 seed rows + indexes.
**Unlocks:** admin audit logs (`GET /v1/admin/audit-logs`).

**Verify** (expect **15**):

```sql
select count(*) from public.audit_logs;
```

---

## 0009 — `scan_processing.sql`

**Alters:** adds `scans.processing_mode`, `scans.team_id`,
`scans.completed_at` + status indexes.
**Unlocks:** **the scan round-trip** — without it `POST /v1/scans` returns
`503 "migration 0009 not applied"`. This is the hard gate for
initiate → upload → submit → worker → complete → report.

**Verify** (expect **3 rows**):

```sql
select column_name from information_schema.columns
where table_schema = 'public' and table_name = 'scans'
  and column_name in ('processing_mode', 'team_id', 'completed_at');
```

---

## 0010 — `user_sessions.sql`

**Creates:** `public.user_sessions` (session ledger, refresh-token hash only,
RLS with no public policies) + `public.user_security_settings`.
**Unlocks:** active-sessions surface (`GET/DELETE /v1/security/sessions`),
sign-in controls; second hard gate for the readiness endpoint.

**Verify** (expect **2 rows**):

```sql
select table_name from information_schema.tables
where table_schema = 'public'
  and table_name in ('user_sessions', 'user_security_settings');
```

---

## Post-apply verification

### 1. One-shot object check (paste once, expect the noted counts)

```sql
select '0003' m, 'notes + invited_by cols' obj, count(*) found
from information_schema.columns
where table_schema = 'public'
  and (
    (table_name = 'waitlist_applications' and column_name = 'notes')
    or (table_name = 'access_invites' and column_name = 'invited_by'))
union all
select '0004', 'profiles', count(*) from information_schema.tables
  where table_schema = 'public' and table_name = 'profiles'
union all
select '0005', 'org tables', count(*) from information_schema.tables
  where table_schema = 'public'
  and table_name in ('organizations', 'teams', 'organization_members', 'organization_invites')
union all
select '0006', 'feature_flags rows', count(*) from public.feature_flags
union all
select '0007', 'incidents rows', count(*) from public.admin_incidents
union all
select '0008', 'audit_logs rows', count(*) from public.audit_logs
union all
select '0009', 'scan processing cols', count(*) from information_schema.columns
  where table_schema = 'public' and table_name = 'scans'
  and column_name in ('processing_mode', 'team_id', 'completed_at')
union all
select '0010', 'sessions tables', count(*) from information_schema.tables
  where table_schema = 'public'
  and table_name in ('user_sessions', 'user_security_settings')
union all
select '0011', 'notifications table', count(*) from information_schema.tables
  where table_schema = 'public' and table_name = 'notifications'
union all
select '0012', 'profiles.team_id col', count(*) from information_schema.columns
  where table_schema = 'public' and table_name = 'profiles' and column_name = 'team_id'
union all
select '0013', 'scans.file_hash_sha256 col', count(*) from information_schema.columns
  where table_schema = 'public' and table_name = 'scans' and column_name = 'file_hash_sha256'
union all
select '0014', 'crash_reports table', count(*) from information_schema.tables
  where table_schema = 'public' and table_name = 'crash_reports'
union all
select '0015', 'invites.token_hash col', count(*) from information_schema.columns
  where table_schema = 'public' and table_name = 'organization_invites'
  and column_name = 'token_hash'
union all
select '0016', 'role_scopes table', count(*) from information_schema.tables
  where table_schema = 'public' and table_name = 'role_scopes'
union all
select '0017', 'sessions seed rows (dev account)', count(*) from public.user_sessions
  where user_id = (select id from auth.users
                   where email = 'founder.admin@provance.local' limit 1)
union all
select '0018', 'better-auth tables', count(*) from information_schema.tables
  where table_schema = 'public'
  and table_name in ('user', 'session', 'account', 'verification', 'twoFactor',
    'organization', 'team', 'member', 'teamMember', 'invitation', 'role', 'apiKey')
union all
select '0019', 'scans.idempotency_key col', count(*) from information_schema.columns
  where table_schema = 'public' and table_name = 'scans' and column_name = 'idempotency_key'
union all
select '0020', 'api_usage table', count(*) from information_schema.tables
  where table_schema = 'public' and table_name = 'api_usage'
union all
select '0021', 'scans attempts cols', count(*) from information_schema.columns
  where table_schema = 'public' and table_name = 'scans'
  and column_name in ('attempts_made', 'max_attempts')
order by m;
```

Expected `found` per migration: 0003 → 2 · 0004 → 1 · 0005 → 4 · 0006 → 10 ·
0007 → 5 · 0008 → 15 · 0009 → 3 · 0010 → 2 · 0011 → 1 · 0012 → 1 · 0013 → 1 ·
0014 → 1 · 0015 → 1 · 0016 → 1 · 0017 → 3 · 0018 → 12 · 0019 → 1 · 0020 → 1 ·
0021 → 2.

(`0017 → 3` is conditional: the seed only inserts when the dev test account
`founder.admin@provance.local` exists in `auth.users` — expect 0 in a
project without that account. `0018 → 12` counts the twelve Better Auth
core/plugin tables; this row is informational since the live auth flow
still runs on GoTrue behind `USE_BETTER_AUTH`.)

### 2. Readiness endpoint (backend must be running on :4000)

```bash
curl http://localhost:4000/v1/health/readiness
```

Acceptance: `"status": "ready"` requires ALL of:

- `checks.scansSchema.ready = true` — flips only when **0009** lands
  (`processing_mode`/`team_id`/`completed_at` on `scans`).
- `checks.userSessions.ready = true` — flips only when **0010** lands
  (`user_sessions` + `user_security_settings`).
- `checks.migrations.ready = true` — the `MigrationHealthService` diff gate:
  it lists every `supabase/migrations/*.sql` file whose probe still fails,
  and it also gates `ready`. **Applying 0005–0010 alone leaves `status`
  `degraded`** because 0011–0021 remain unapplied — to converge to `ready`
  in one paste, use the **founder checklist** at the top of this file
  (click-to-open links per migration, or the embedded full combined block —
  all idempotent, numeric order).

The `queue` check is informational (BullMQ configured vs inline fallback) and
does not gate `ready`.

### 3. One-command schema check (from a shell)

The canonical pre-walk gate — probes the SAME `MIGRATION_PROBES` list the
readiness `checks.migrations` check uses (imported from
`src/health/migration-health.service.ts`), so the script and the health
endpoint can never disagree:

```bash
cd backend && npm run build && npm run validate:migrations
```

Prints a **project banner** (the Supabase project ref this env probes, plus a
direct SQL-editor link), every migration's status (`OK`/`MISSING`/`SKIP`), an
applied/missing summary, and — on failure — the exact
`MISSING MIGRATIONS: <n> (<file>), …` list plus a **project/env mismatch
check**, exiting non-zero. A `42703` (column missing) or `PGRST205` (table
missing) pinpoints which migration is still outstanding. Equivalent to the
service-role REST probes below, minus the SQL editor.

**Using it to diagnose a project/env mismatch before any live walk:** the
banner names the project this env actually probes. When migrations were
applied in the dashboard but the check still reports them missing, compare
that ref with the project id in the SQL Editor's browser URL bar — they must
match. If they don't, the paste went to a different project than
`backend/.env.local` points at (the banner's dashboard link opens the SQL
editor for exactly the probed project). The applied set is the fingerprint:
this project shows `0001, 0002, 0003, 0004, 0006` applied, so a dashboard
project with a different applied set is not the one the env probes.

### 4. Admin surface walk (after 0008 + 0009 land)

Once `audit_logs` (0008) and `scans.processing_mode` (0009) are applied, the
admin jobs surface is verifiable end to end with one command (backend must be
running):

```bash
cd backend && npm run validate:admin-jobs
```

Seeds one synthetic `failed` scan, then walks the Admin Jobs contract live:
`GET /admin/jobs` envelope, `?status=` server-side filter, pagination
(disjoint pages + exact total), `POST /admin/jobs/:id/retry` (row flips to
`queued`), and `GET /admin/audit-logs?actor=…&action=scan.retried` showing
`severity: medium` with the admin actor. The synthetic scan is deleted on
cleanup; the audit rows are intentionally left. Uses the documented seed
account `founder.admin@provance.local` (override with `ADMIN_WALK_EMAIL` /
`ADMIN_WALK_PASSWORD`) and creates it via the GoTrue admin API if it does
not exist yet. The walk scripts resolve the running backend by probing
`/v1/health` for `service=provance-backend` (a foreign `PORT` env var from
the dev shell is ignored).

### 5. Org-admin session revocation walk (after 0005 + 0010 land)

Once `organizations` (0005) and `user_sessions` (0010) are applied, the org
member-sessions surface is verifiable end to end with one command:

```bash
cd backend && npm run validate:org-revoke
```

Signs in the allowlisted org admin, creates a throwaway member, signs it in
twice with different User-Agents (two real devices → two `user_sessions`
rows), seeds one org (admin = owner, member = member), then walks
`GET /organization/members/:id/sessions` (both rows, team + `isNewDevice`),
`DELETE /organization/members/:id/sessions/:sid` (ledger drops to one), and
the two-device proof (revoked token 401s on `/auth/me`, the survivor 200s).
Member user + seeded org are always deleted; the admin account stays.
Re-runs are safe — stale walk orgs and member users are purged first.

---

## Troubleshooting

| Symptom | Cause | Fix |
| ------- | ----- | --- |
| `PGRST205` / "Could not find the table in the schema cache" | table not created | re-run that migration; if just applied, wait a few seconds (PostgREST schema reload) and re-probe |
| `42703` / "column X does not exist" | column not added (e.g. 0009) | re-run the ALTER migration |
| `503 "migration 0009 not applied"` on `POST /v1/scans` | 0009 missing | apply 0009 |
| SQL editor error on 0004/0005 | missing `set_updated_at()` (0001) or ran out of order | apply 0001 first, then re-run |
| SQL editor error on 0009 | `public.scans` missing (0002 not applied) | apply 0002 first |
| Migration applied but a *value* looks off (0 rows in a seeded table) | `on conflict do nothing` + an earlier partial run | re-run the file; seeds are upsert-style |
| Applied migrations in the dashboard SQL editor, but `npm run validate:migrations` still reports them missing | **project/env mismatch** — the paste went to a different Supabase project than the one `backend/.env.local` probes | run `cd backend && npm run build && npm run validate:migrations` and compare the printed project ref with the project id in your SQL Editor URL bar (`supabase.com/dashboard/project/<ref>/…`); re-paste the combined block into the ref the command names (the banner prints a direct editor link) |

Notes:

- All probes above use the **service role key** (RLS bypassed). The dashboard
  SQL editor runs as `postgres`, which also bypasses RLS — the two views agree.
- The backend reads the schema live; no backend restart is required after
  applying. If the running backend predates the schema change and a probe
  disagrees with the SQL check, trust the SQL check.
- Do **not** hand-edit already-applied migrations (0001/0002) — that creates
  drift with the remote project. New schema changes go in new numbered files.
