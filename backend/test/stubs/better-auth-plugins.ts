/**
 * better-auth-plugins.ts — CJS stub for `better-auth/plugins` (see
 * better-auth.ts). Plugin factories return inert config; nothing in the e2e
 * graph enables them.
 */
export function twoFactor(): unknown {
  return {};
}

export function organization(): unknown {
  return {};
}
