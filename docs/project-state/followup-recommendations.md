# Follow-Up Task Log

Last updated: 2026-08-11

## Purpose

This is the **running log of next-step recommendations** made at the end of
each completed task. It exists so nothing we suggest gets lost: when the
current task load is done, the Founder points back at this file and we
continue working from it.

Rules of use:

- After every task, append the recommendations we make (the "what's next"
  suggestions) as new rows here.
- Update the status of a row when it is picked up, done, or explicitly
  declined/deferred.
- This file complements `PHASE_TASK_LIST.md` (phase checklist) — it captures
  *emergent* follow-ups, not the phase backlog.

Status values: `Open` · `In Progress` · `Done` · `Deferred` · `Declined`

## Log

| Date | Source task | Recommendation | Status |
| --- | --- | --- | --- |
| 2026-08-17 | Pre-walk project guard | `validate:scan-roundtrip` now fails fast (exit 2) with project ref + applied-set fingerprint before any upload when migrations aren't converged (verified live: `dmhrwdcuwtgscwlaagsa` applied 5 · fingerprint `f687d713` · missing 15). Open: (a) once migrations land, re-run the round-trip to see the guard PASS line + the walk continue; (b) consider the same guard on `validate-admin-jobs.mjs` / `validate-live-surfaces.mjs` so every live walk shares the one-command wrong-project diagnostic | Open |
| 2026-08-17 | Sample Report redesign | Landing Sample Report + `/sample-report` now render the full branded verification-report document (`SampleReportDocument`) — the export-PDF design with every detail group. Open items: (a) **Done 2026-08-17** — cover now mirrors the pdfkit header exactly: full-bleed ink band (`#23201A`) with uppercase PROVANCE wordmark (parchment `#F7F4ED`, bold sans, wide tracking) over the VERIFICATION REPORT label (muted gold `#C9C2B4`), and the sentence-case eyebrow replaced by the PDF's OVERALL VERDICT banner (tone rule + label) beneath which the verdict headline sits; (b) consider generating the sample PDF server-side via `generateReportPdf` on the real `report-document` model so the downloadable sample matches the actual export byte-for-byte (today it's the browser print path) — still open | Open |
| 2026-08-17 | Sample report cover parity | `SampleReportDocument` cover now mirrors the pdfkit export header (ink band + PROVANCE / VERIFICATION REPORT wordmark + OVERALL VERDICT banner with tone rule). Open: (a) **Done 2026-08-17** — `src/components/SampleReport.test.jsx` (10 tests) pins the report contract: the full content-model walk now renders `SampleReportDocument` directly (the `/sample-report` surface — cover wordmark + verdict, executive summary, media/metadata, risk, timeline, version info, AI signals, provenance, frame/model/cross-validation, scope, custody, findings, next steps, appendix, reviewer notes, disclaimer + metrics grid), and a new compact-teaser suite asserts the landing section shows the seal/verdict/metrics/signals and **does not** render the deep sections; (b) generate the sample PDF server-side via `generateReportPdf` on the real `report-document` model so the downloadable sample matches the actual export byte-for-byte instead of the browser print path — still open | Open |
| 2026-08-17 | Landing Sample Report — compact redesign (Founder) | The landing Sample Report section is now a short, compact verification summary instead of the full document: a circular **Verified with Provance** seal (SVG circular text + charcoal check core) overlapping the card's top-right, the pdfkit-style ink band (PROVANCE / VERIFICATION REPORT — SAMPLE), the verdict headline with tone rule + three headline metric pills (confidence / authenticity / risk), the sample media frame with a "Sample media" tag, the top three AI-detection signal rows, a report-identity footer (filename + "See the full report" link), and the Download Sample PDF / View Full Sample Report CTAs. The full document remains at `/sample-report` and `/sample-report/print` (untouched). Verified: vitest **603/603 → 607/607** (10 report tests), responsive **260/260** clean at 375–1280, audit:a11y **34/34** clean, build clean, lint 0 errors. Open: the seal's circular text is SVG textPath — confirm the tracking/overlap looks right at the printed size; optionally extend the same seal to the full report cover | Open |
| 2026-08-17 | Landing polish — navbar width + bigger report card (Founder) | Two Founder-driven refinements: (1) **Navbar** pill widened `max-w-6xl → max-w-7xl` with `px-5 md:px-8` + `gap-6` and nav link spacing `gap-7 → gap-8`, CTA buttons `ml-2 px-6` — real air between logo / menu / CTAs. (2) **Landing Sample Report card** expanded `max-w-3xl → max-w-5xl` and made more substantial: the **seal clip is fixed** (the circular Verified with Provance stamp was being trimmed by `InteractivePanel`'s `overflow-hidden` — it now sits outside the tilt panel as an absolute sibling overlapping the card's top-right, geometrically verified fully inside the section's clip region); new **"What the full report includes"** grid (AI detection signals, manipulation indicators, metadata & provenance, chain of custody, frame analysis, timeline & methodology — real section names from the full document, kept distinct from the test's absent-list); new **"Choose your report depth"** row (Quick / Standard / Deep with mono tags + one-liners, Standard featured as the default the sample shows, tying the teaser to the real `processingMode` enum); footer now carries filename + report id + media type. Verified: vitest **10/10** report tests, build clean, lint 0, responsive **260/260**, audit:a11y **34/34** | Open |
| 2026-08-17 | Accessibility audit (app + admin) | New repeatable gate **`npm run audit:a11y`** (`scripts/audit-a11y.mjs`) — boots vite mock, signs in as the seeded admin, walks all /app + /app/admin routes (plus landing surfaces) and fails on structural a11y issues: interactive elements with no accessible name, `<img>` without alt, form controls with no label/aria, and keyboard-Tab stops with no visible focus indicator (real `:focus-visible` via Playwright Tab cycling; native date/time widgets exempted — Chromium focuses inside their shadow DOM). **34/34 routes structurally clean.** Fixes landed: `--color-charcoal-light` darkened `#7d8797 → #5f6b7c` (label text on parchment/white-warm now ≥4.5:1 — the systemic contrast issue; token doc updated) with the dark-surface counterpart fixed (`TransparencyFooter` labels `charcoal-light → stone` on charcoal so they stay ≥4.5:1); notification count badge `bg-rose-500 → bg-rose-600` (white 10px text ≈4.8:1). Contrast is advisory by design (hex tokens measured exactly; Tailwind v4 oklch palette + alpha backgrounds fall through to the ancestor walk). **Known advisory left**: the amber timeline timestamps (`--color-amber` #b7791f on amber/10-over-white ≈3.3–3.6:1) — a brand-accent tradeoff; decide whether to darken the amber token or accept for decorative mono timestamps | Open |
| 2026-08-17 | Supabase credentials setup | **Credentials wired + stack live**: new-format keys (`sb_publishable_` / `sb_secret_`) in both env files, `SUPABASE_JWKS_URL` added, backend restarted on :4000 (readiness `checks.supabase.ready: true`), BullMQ worker up (queue `scan-processing`, concurrency 4), frontend dev on :4400 → `http://localhost:4000/v1`. **Only remaining blocker**: migrations 5/19 (missing 0005, 0007–0016, 0018–0021) — apply via `DATABASE_URL` in `backend/.env.local` + `npm run apply:migrations -- --verify`, or the dashboard SQL Editor paste. Worker runs with inline `PORT=4000` (its ConfigModule requires PORT from process env). Once migrations land, run the full live walk (validate:migrations → readiness → scan round-trip) | Open |
| 2026-08-17 | Verification Reports polish | Report surfaces fixed (confidence scale 0–1 everywhere + `formatPct` 100% clamp, seed scans carry the full verdict/report/signal payload, bell/avatar popovers anchor under their icons and fit the viewport, video preview renders). Open items: (a) **PDF export** — real mode already downloads the branded A4 pdfkit file (`GET /reports/:id/pdf`), but mock mode still routes to the print dialog; decide whether to bring the branded generator into mock mode or keep the print path as an intentional dev-only fallback; (b) **stale localStorage seed** — `mockScanStore` persists to localStorage, so browsers with an old pre-fix seed keep stale rows until cleared; a store schema-version bump would force a reseed; (c) seed scans have no `asset_preview_url` (real mode does) — add mock preview URLs if the demo should show media thumbnails/video | Open |
| 2026-08-17 | find-skills discovery | Curated skill installs for the Founder (skills.sh search verified — counts as of 2026-08-17): **vitest** `antfu/skills@vitest` (32.3K — by Vitest's creator; frontend gate runs 592 vitest tests), **Upstash Redis** `upstash/skills@upstash-redis-js` (5.6K official; BullMQ runs on Upstash here), **a11y** `addyosmani/web-quality-skills@accessibility` (45K; ~50 routes never a11y-audited), **NestJS** `kadajett/agent-nestjs-skills@nestjs-best-practices` (25K, complements custom provance-nestjs), **GH Actions** `callstackincubator/agent-skills@github-actions` (6.6K). Alternates: `redis/agent-skills@redis-development` (3.2K), `supabase/supabase@vitest` (2.2K). None installed yet — awaiting Founder approval (`npx skills add <owner/repo@skill> -g -y`) | Open |
| 2026-08-17 | frontend-design pass (landing) | ~~Landed the **Examination** instrument world on the landing~~ — **REVERTED same day at Founder request**: Hero back to the parchment/serif version and fonts back to Plus Jakarta Sans/Manrope; all landing files byte-identical to the approved `b88e703` high-end pass (verified with empty `git diff b88e703`). Open items that survive the revert: (a) the public pages (product, methodology, pricing, security, docs, benchmark, resources, waitlist, sign-in, reset-password) still need the design/copy review the Founder flagged earlier — proceed on the `b88e703` voice; (b) decide whether any premium sans should extend into the app/admin shells (currently Inter, Operate register — deliberate) | Closed |
| 2026-08-17 | improve-codebase-architecture review | Architecture review surfaced four deepening candidates (report filed to the OS temp dir): **01** `api.js` is a 988-line shallow dispatch ladder (70 inline USE_MOCK branches) — replace with a Backend seam (MockBackend / HttpBackend / BetterAuthBackend) selected once at boot (Strong); **02** the mock backend (mockApi/mockData/mockNoise, 4.3k lines) becomes an adapter behind that same seam with parity suites as contract tests (Strong); **03** collapse the twin polling hooks `useResource`/`useMockData` into one engine with a loader seam — **Done 2026-08-17** (see the dedicated row below); **04** give the Report concept one owning module (model + render + export) instead of six re-derivations (Speculative). Top recommendation: start with 01 — 02 are a consequence of it. 01/02/04 still open | Open |
| 2026-08-11 | Live walk tooling (migrations pending) | **Apply channel is ready in two ways**: (a) paste `.freebuff/combined-0005-0020.sql` in the dashboard SQL Editor, or (b) paste the Supabase connection string into `DATABASE_URL` in `backend/.env.local` and run `node .freebuff/apply-migrations.mjs --yes` — then re-run the full sequence `validate:migrations` → `validate:live-surfaces` → `validate-scan-roundtrip` → `validate:admin-jobs` and confirm readiness flips to ready — **state 2026-08-17**: still 5/20 applied (15 missing incl. 0009 hard gate); `DATABASE_URL` line exists but is EMPTY in `backend/.env.local`; fresh paste block written to `.freebuff/combined-missing.sql` (853 lines, 15 migrations in dependency order, now including 0021 — the newer `validate:migrations --paste-file` emit supersedes the older combined-0005-0020.sql); backend up on :4000 with REDIS_URL set (BullMQ path) but the `dist/worker` process is NOT running, so the round-trip's queued→processing→complete chain needs the worker started (`npm run start:worker`) once migrations land — **apply channel enhanced 2026-08-17**: `apply:migrations` now supports `--verify` (applies then auto-runs validate:migrations, failing on any still-missing); the only missing input is the `DATABASE_URL` value itself, which must be pasted into `backend/.env.local` by the Founder — **round-trip strengthened 2026-08-17**: `validate-scan-roundtrip` now also polls the BullMQ job (1s cadence vs the row's 5s) and asserts the job is observed, was claimed `active` (worker path, not inline), and its terminal state matches the row; skipped with a note when REDIS_URL is unset | Open |
| 2026-08-11 | Live walk tooling (migrations pending) | The pre-paste baseline is captured (scan initiate 503 on 0019; notifications + analytics BLOCKED on 0011 + 0009; 5 applied · 14 missing) — after the apply, the same walks must flip green with no code changes | Open |
| 2026-08-11 | Live walk tooling (migrations pending) | Once migrations land, extend `validate-live-surfaces.mjs` (or the runbook) with the `?team=` scoped analytics leg so the admin team filter is live-verified against real `scans.team_id` aggregation, not just the mock join | Open |
| 2026-08-11 | USE_MOCK env flip (real-by-default) | Decide the Vercel demo intent: the deployed site is now real-mode by default, so it will render degraded error states on every surface until the `combined-0005-0020.sql` paste lands — if the founder wants the live demo to keep showing mock data meanwhile, set `VITE_USE_MOCK=true` in the Vercel env (production build honors it) | Open |
| 2026-08-11 | USE_MOCK env flip (real-by-default) | Add a vitest run with `VITE_USE_MOCK=false` to the CI matrix (or a dedicated real-branch suite) so the real API paths get automated coverage instead of only live walks — today no test exercises the real branches | Open |
| 2026-08-11 | USE_MOCK env flip (real-by-default) | Update the ADR 004 ratification note and any remaining docs that still say "set `USE_MOCK = false` in api.js" — the flip mechanism is now env-driven (sweep done in this task; SCAN_UPLOAD_CONTRACT + status docs updated) | Done |
| 2026-08-11 | Live cookie CI gate | Founder setup step: add the `SUPABASE_URL`, `SUPABASE_ANON_KEY`, and `SUPABASE_SERVICE_ROLE_KEY` secrets (values from `backend/.env.local`) at GitHub → Settings → Secrets and variables → Actions, then trigger `cookie-live.yml` manually once to confirm the gate passes on the branch — until then every main push shows a red X by design (fail-closed) | Open |
| 2026-08-11 | Live replay verification | The live `refresh_token_rejected` read remains gated: replays 401 (17/17 walk), but `GET /v1/admin/audit-logs` 503s ("Failed to fetch audit logs.") because `audit_logs` (0008) is still unapplied — paste `combined-0005-0020.sql` and the same walk's audit read flips live | Open |
| 2026-08-11 | Refresh-replay lockout | Consider applying the same failure-triggered lockout to `POST /auth/sign-in` (credential-stuffing bursts), and make the lockout state distributed (Redis) once multiple backend replicas run — today it's per-process in-memory — **sign-in half Done 2026-08-17**: `SignInLockoutInterceptor` (5×401 in 60s → 429 + one `signin_lockout` high row; success clears; `SIGNIN_LOCKOUT_ENABLED=false` e2e escape hatch), 8 unit tests. **Redis-distributed state still Open for BOTH lockouts once replicas run** | Done |
| 2026-08-11 | Refresh-replay lockout | Surface the `refresh_lockout` event on the Admin Audit Logs page's action filter (the severity tone renders already; the filter list may need the new action) | Open |
| 2026-08-11 | Session revocations in admin trail | Once migration 0008 (audit_logs) lands, re-run the session-lifecycle walk to confirm `session.revoked` rows appear in `GET /admin/audit-logs` live, not just in the mocked e2e | Open |
| 2026-08-11 | Live cookie walk | Wire `validate-refresh-cookie.mjs` into package.json as `validate:refresh-cookie` (it exists but is only runnable via `node scripts/…`), so the live walk is one command like the other validate scripts — **Done 2026-08-17** (`npm run validate:refresh-cookie`) | Done |
| 2026-08-11 | Live cookie walk | After migration 0008 (audit_logs) lands, re-run the walk to capture the `refresh_token_rejected` reuse-detection rows live — the walk skips that read today with an explicit note | Open |
| 2026-08-11 | Cookie-migration coverage doc | Wire a transactional alert (email/Slack) when `refresh_token_rejected.reuse_suspected` is true — the audit + e2e assertion shipped, but a real out-of-band alert for suspected token theft is still open | Open |
| 2026-08-11 | Auth cookie e2e slice | Extend the spec with a secure-deployment variant (AUTH_COOKIE_SECURE=true) asserting the `__Host-provance_refresh` cookie name + `Secure` flag, since only the plain local name is covered today | Open |
| 2026-08-11 | Auth cookie e2e slice | Add a guarded-surface leg: sign in, then GET /v1/auth/me with the access token (SupabaseAuthGuard overridden like the security spec) to prove the full session round-trip past the cookie boundary | Open |
| 2026-08-11 | Readiness surfaced live | The migrations gate runs 20 sequential REST probes so the first readiness call takes ~15s — either parallelize the probes or note the timeout in the runbook so a slow first call isn't mistaken for a hang | Open |
| 2026-08-11 | One-paste schema convergence | After pasting `combined-0005-0020.sql`, run every live walk in sequence (readiness → `validate:admin-jobs` → `validate:org-revoke` → `validate:scan-roundtrip`) to confirm each surface flips real, since the single paste unblocks all of them at once — **emitter Done 2026-08-17**: `validate:migrations` now auto-emits the paste block itself (`--paste-file=<path>` writes it, `--no-emit` silences it), so the hand-assembled `.freebuff/combined-0005-0020.sql` is reproducible on demand (byte-parity verified); the post-paste walk chain is the remaining half | Open |
| 2026-08-11 | One-paste schema convergence | Migrate the stale `.freebuff/probe_migrations.mjs` (0001–0010, soft "table present" fallback) onto the canonical `migration-health.service.ts` probe list or delete it, so scratch and canonical probes can't disagree (0009 already showed that false-positive) | Open |
| 2026-08-11 | Org session revocations audited | Add `session_revoked` mock parity to the self-service `mockRevokeSession` (Security page) — the real backend writes it to the feed but the mock still doesn't, so mock mode under-demos the account tab — **Done 2026-08-17**: `mockRevokeSession` prepends a `session.revoked` row (severity from the shared map, `details.session_id`) | Done |
| 2026-08-11 | Org session revocations audited | Render `details.revoked` (and member context) in the Admin Audit Logs event drawer so the persisted count is visible, not just stored — **Done 2026-08-17**: the drawer renders structured `details` (revoked counts, session ids, lockout ip/failures, reasons) with `break-words` hardening | Done |
| 2026-08-11 | Org session revocations audited | Extend `validate-org-session-revoke.mjs` to assert the `member_sessions_revoked` row (with the exact count) appears in `GET /admin/audit-logs` once migrations 0005 + 0010 land | Open |
| 2026-08-10 | Push milestone to `dev/backend-integration-milestone` | Merge the branch into main (builds green, 330 backend / 449 frontend) so Vercel picks up the latest for live testing — Founder decision, test first | Open |
| 2026-08-10 | New-device sign-in detection | Wire the `[mock-email]` new-device alert to a real transactional-email provider once one is selected, so the security alert actually reaches the user out-of-band | Open |
| 2026-08-10 | Billing meters real data | Apply migration `0020_api_usage.sql` in the Supabase dashboard and seed a couple of `api_usage` rows, then verify GET /v1/billing renders real storage + API meters end-to-end | Open |
| 2026-08-10 | Dashboard quota warning chip | Add a matching quota warning to the Uploads page (the surface where the user will actually hit the limit), reusing `scanQuotaPct` so both surfaces agree — **Done 2026-08-17**: chip extracted to shared `src/components/ScanQuotaWarningChip.jsx`, Uploads page fetches `getBilling` and renders it under the header | Done |
| 2026-08-10 | Projected usage + overage estimate | Wire the overage estimate into a real payment processor when Stripe lands, so `overageCostUsd` becomes a charge rather than an estimate | Open |
| 2026-08-10 | Billing meters real data | Wire API-call counting into the backend (increment `api_usage.calls` on authenticated API requests) so the meter moves with real traffic, not just seeded rows | Open |
| 2026-08-10 | New-device sign-in detection | Add a live two-device validation: sign in from two IPs/devices against a real Supabase project and confirm the audit event + notification land and refresh does not re-trigger | Open |
| 2026-08-10 | Push milestone | Next implementable backlog slice: fuller authorization model beyond allowlists and route guards (scopes enforcement across the API) | Open |
| 2026-08-10 | Push milestone | Build the admin search and filter model across waitlist, users, scans, and reports so admin views share one filterable table pattern | Open |
| 2026-08-10 | Push milestone | Build report share links (mock-backed) with an expiry surface, replacing the null `share_url` in the report payload | Open |
| 2026-08-10 | Better Auth Option A (USE_BETTER_AUTH mount) | Wire the org/api-key client surfaces (`organizationClient` / api-key client) or the twoFactor enrollment UI the security page's 2FA toggle needs | Open |
| 2026-08-10 | Better Auth Option A | Apply migration `0018_better_auth.sql` in the Supabase dashboard and set `USE_BETTER_AUTH=true` + `DATABASE_URL` in `backend/.env.local` to light up the enabled path, then walk sign-up/sign-in live | Open |
| 2026-08-10 | Scan idempotency (0019) | Apply `0019_scan_idempotency.sql` in the Supabase dashboard SQL editor (alongside pending migrations) to activate the dedupe round-trip | Open |
| 2026-08-10 | Scan upload + queue round-trip | Provision an Upstash Redis instance, wire `REDIS_URL` into `backend/.env.local`, run `start:worker`, and verify jobs process through BullMQ instead of inline fallback | Open |
| 2026-08-10 | Live two-device session walk | After migration 0010 lands: sign in twice as the dev test account, list both sessions, revoke one via the API, and confirm the revoked token stops working | Open |
| 2026-08-10 | Frontend gates | Enable GitHub Actions on the repo (if disabled, exact Settings steps) so the CI gate runs on pushes; confirm it passes on the branch | Open |
| 2026-08-10 | Auth provider decision brief | Founder sign-off gate: pick GoTrue (stay) vs Better Auth (migrate now, recommended) vs hybrid, with a re-evaluation date — see `AUTH_PROVIDER_DECISION.md` | Open |
| 2026-08-10 | Retention policy baseline | Archival enforcement job (Phase 5 backlog): move aged `result_payload`/media to cold storage and prune audit/crash tables per `RETENTION_POLICY.md` | Open |
| 2026-08-10 | Pre-processing content gate | Malware scanning before beta (vendor decision) and worker error alerting (alerting channel decision) — explicitly deferred in `PHASE_TASK_LIST.md` | Deferred |
| 2026-08-10 | Backend /admin sweep | Audit done: `/admin/jobs`, `/admin/reports`, `/admin/settings` are built (derived from scans/profiles/flags/audit, matching mock shapes). **Remaining gap: `/admin/roles` has no controller route** — `getAdminRoles` → `GET /admin/roles` 404s in real mode; build the RolesController (list roles + scopes, PATCH scopes with Owner-guard, PATCH member role with audit) — **Done 2026-08-17**: `backend/src/roles/` (RolesController at `/admin/roles`: GET list, PATCH `:roleId/scopes`, PATCH `members/:memberId`), `api.js` real-path branches wired, RolesPage consumes them | Done |
| 2026-08-10 | Live e2e validation | Flip `USE_MOCK` off against a local Supabase + backend and walk the full user flow (sign in, upload, queue, report) to catch contract mismatches — **sign-in → reload → dashboard + httpOnly-cookie flow now verified live (commit `4a8de1c`); upload/queue/report legs still blocked on the pending migration set** | In Progress |
| 2026-08-10 | PostHog/Sentry approval | Set up Sentry (errors) + PostHog (product analytics) baseline ahead of beta — needs project credentials | Open |
| 2026-08-10 | Transactional email | Select a transactional email provider (invite/reset delivery) — decision deferred | Deferred |
| 2026-08-10 | Live scan-flow walk (migration probe) | Apply the pending migration set (0005, 0007, 0008, 0009, 0010–0016, 0018–0020 — confirmed missing by the new `MigrationHealthService` startup diff; 0001, 0002, 0003, 0004, 0006 are applied) via `DATABASE_URL` in `backend/.env.local` or dashboard SQL-editor pastes (paste block at `.freebuff/combined-0005-0009.sql`), then re-run the live scan round-trip; `POST /v1/scans` currently 503s and `invite-accept.e2e-spec.ts` fails on the missing org tables | Open |
| 2026-08-10 | MigrationHealthService (startup + readiness diff) | Restart part done — fresh builds have been booted repeatedly since (deploy check `4a8de1c`, live walks on :4100). **Remaining: the CI smoke step** that fails the build when `/v1/health/readiness` reports missing migrations | In Progress |
| 2026-08-10 | Auth controller cookie-flow coverage (`ec6d0ce`) | Live cookie-session walk against the real Supabase project — sign-in sets the HttpOnly cookie, the refresh token is stripped from the body, and a fresh page load restores the session via the silent cookie refresh with an empty localStorage — **done in the deploy check (commit `4a8de1c`)** | Done |
| 2026-08-10 | Refresh-token reuse detection (`adaae84`) | `session_revoked` audit recording is shipped (security.service.ts writes it on revocation; the security e2e asserts it). **Remaining: severity-map parity** — add `session_revoked: 'high'` to backend `audit-severity.ts` + frontend `AUDIT_SEVERITY_BY_ACTION` so the Admin Audit Logs page renders revocations with the right tone — **Done 2026-08-17**: `audit-severity.ts` carries `session.revoked: high` + `session_revoked: medium`; the frontend `mockData` map mirrors both | Done |
| 2026-08-10 | Refresh-token reuse detection (`adaae84`) | Throttle `POST /auth/refresh` so repeated rejected refresh tokens (replay attacks) trigger a short lockout + a high-severity audit event, protecting the new rejection trail from noise | Open |
| 2026-08-10 | Refresh-token reuse detection (`adaae84`) | Live walk of the replay path: sign in, refresh to rotate, replay the old token, and confirm the 401 plus the `refresh_token_rejected` row on `GET /admin/audit-logs` | Open |
| 2026-08-10 | Deploy check — httpOnly-cookie flow (`4a8de1c`) | Automate the cookie contract check as a CI gate: boot the real backend and assert the sign-in `Set-Cookie` carries `HttpOnly` + `SameSite` and the body omits `refreshToken`, so the migration can't silently regress on deploy | Open |
| 2026-08-10 | Deploy check — httpOnly-cookie flow (`4a8de1c`) | Flip the frontend to real mode by default (env-driven `USE_MOCK` with a dev fallback) so every future slice is validated against the live API, not the mocks — **done**: `USE_MOCK` is now `VITE_USE_MOCK`-driven in `src/lib/api.js` (real by default in prod builds, mock fallback in dev, explicit override both ways) | Done |
| 2026-08-10 | Deploy check — httpOnly-cookie flow (`4a8de1c`) | Apply the pending migrations (0009 + 0010/0011 at minimum) and re-run the live walk — scan upload round-trip, notifications, and admin analytics parity are still 503/404 in real mode | Open |
| 2026-08-10 | Responsive pass — tablet/desktop audit (`53d9a8e`) | Turn the responsive audit into a repeatable npm script (`audit:responsive`) that fails CI on any page-level overflow or clipped element at 768/1280, making the pass a permanent gate — **done**: `scripts/audit-responsive.mjs` + CI job boots vite in mock mode, signs in as the seed admin, walks 52 routes × 2 viewports in headless Chromium, fails on page-overflow/clipped in-flow elements (fixed/absolute + scroll containers exempt, overflow:hidden NOT exempt). It immediately caught a real 768px bug (public Navbar desktop links overflowed at `md:`) — fixed by moving the desktop nav to `lg:` | Done |
| 2026-08-11 | audit:responsive gate | New pages must be added to the route inventory in `scripts/audit-responsive.mjs` (PUBLIC/APP/ADMIN_ROUTES) to stay under the gate — worth a line in the PR checklist or a code comment pointer | Open |
| 2026-08-11 | audit:responsive gate | Extend `VIEWPORTS` in the script (one-line change) to 640px + 1024px once the 768/1280 gate is stable, turning the older 640/1024 follow-up into a config tweak | Open |
| 2026-08-10 | Responsive pass — tablet/desktop audit (`53d9a8e`) | Extend the `overflow-hidden` → `overflow-x-auto` sweep to the public pages (Sample Report, ProductShowcase, landing sections) and the print views at tablet width — **done**: audit found no table wrappers on these surfaces (all `overflow-hidden` are legitimate media/animation/decorative clips), so instead the sweep hardened the actual clip risk — long unbroken backend strings (URLs/hashes/IDs) in value cells + timeline `1fr` tracks, proven by injection (718→1475px before, stays 718 after `break-words`/`min-w-0` hardening in SampleReport/SampleReportDocument/AppReportPrintPage) | Done |
| 2026-08-11 | Overflow-hidden sweep (long-string hardening) | Make the long-string injection a permanent leg of `audit:responsive`: after the layout probes, inject a hostile long URL into the report value cells and assert the paper frame still has no overflow — closing the clip-regression loop the same way the route probes close viewport overflow | Open |
| 2026-08-10 | Responsive pass — tablet/desktop audit (`53d9a8e`) | Run the responsive audit at 640px (small phone) and 1024px (small laptop) too, fixing anything those widths surface beyond the 768/1280 pair — **done**: `VIEWPORTS` now covers 640/768/1024/1280 (208 audits); the only new failure was the Admin Monitoring Table-stats card clipping at 1024 (`overflow-hidden` → `overflow-x-auto`, the `53d9a8e` pattern); 640 surfaced nothing | Done |
| 2026-08-10 | Landing mobile spot-check (no commit — all clean) | Decide ProductShowcase's fate like forensic/: wire it into the landing page (e.g. under the Hero or between Use Cases and Pricing) or archive it — it is currently unreferenced dead code — **done**: wired `<ProductShowcase />` into HomePage between Use Cases and Pricing (reuse, not archive — it's a finished 767-line mock-data-driven interactive demo; verified rendering + auto-play + 208/208 audit clean) | Done |
| 2026-08-11 | ProductShowcase dead-code decision | `TrustBar.jsx` is also unreferenced (same check found zero imports) — decide its fate the same way: wire it into the landing (e.g. under the Hero as a logos/social-proof strip) or archive it under `src/archive/` — **done**: wired `<TrustBar />` into HomePage directly under the Hero (reuse, not archive — it is a finished benchmark-claims panel that converts the Provance-1000 claims into social proof and CTAs to the live `/benchmark` page; verified rendering at 375/768 + stats grid + lint + grid guard). ProductShowcase was already wired (row above) — both dead-code decisions are now closed | Done |
| 2026-08-10 | Landing mobile spot-check | Run the same mobile overflow probes on the remaining public pages (docs, resources, benchmark, security, waitlist, sign-in, reset-password, accept-invite) at 375px — **done**: 375×812 added as `phone-375` to `audit:responsive` (52 routes × 5 viewports); real fixes: `/docs` API-example header spans (long URL text, `break-words`), admin overview + roles/audit-logs activity feeds (`ActivityRow` actor email → `break-words`; `kwame.boateng@independent-research.africa` was 210px+ of unbroken text in a narrow column), activity + audit-logs resource chips (`min-w-0 max-w-full` + `truncate` on the id — `waitlist_application_0007` blew the chip to 256px), audit-logs filter selects (`max-w-full` — native selects size to their longest option), and the report detail `text-3xl` filename header (`min-w-0 break-words` — `IMG_20260715_143022.jpg` overflowed +52px). Subset walk over every previously-failing route: 90/90 clean at 375/640/768/1024/1280 | Done |
| 2026-08-10 | Landing mobile spot-check (no commit — all clean) | Mobile touch audit of the Hero: verify tap targets are ≥44px and there are no hover-only interactions at phone width | Open |
| 2026-08-10 | Mobile-first grid guard (`bcd8ca8`) | Extend `gridClassGuard` to enforce base `flex`/`block` equivalents for responsive display utilities (`lg:flex`, `md:grid`) so mobile-first intent is explicit everywhere, not just grids — **done**: new `findBaseDisplayViolation` rule flags any responsive display-ON utility (`lg:flex`, `md:grid`, `xl:block`, …) lacking an explicit base display token (`flex`/`block`/`grid`/`hidden`/…); only 2 repo-wide violations existed (the app + admin shell layouts) → now `block lg:grid lg:grid-cols-[…]`; 7 new unit tests (20 total), repo-wide scan clean, audit 155/155 at all five viewports | Done |
| 2026-08-14 | gridClassGuard CI gate | **Done — repo-wide sweep is now an explicit CI step**: `scripts/grid-guard.mjs` (`npm run guard:grid`) runs in the CI frontend job on every push/PR alongside vitest, scanning src/ for responsive `grid-cols-*` without a base `grid-cols-1` and for stale allowlist entries (fail-closed, file:line output) | Done |
| 2026-08-14 | LivePollIndicator rollout | **Done — extracted to the ui kit and wired everywhere polling happens**: `LivePollIndicator` moved to `src/components/ui/LivePollIndicator.jsx` (barrel-exported) and now renders on the Queue page (queue-posture card, `live = queueNeedsPolling || hasActiveScanWork`) and the report detail pane (`detailLive = scanNeedsPolling(selectedScan)`), alongside the dashboard's ledger + queue-posture cards. Live probe 4/4 (queue shows it, queued detail shows it, completed detail hides it, dashboard still 2). **Docs standardized 2026-08-17**: full primitive header (usage, gating contract, a11y, barrel rule); all 6 consumers verified barrel-imported | Done |
| 2026-08-17 | LivePollIndicator extraction (docs pass) | The sibling request from the original extraction task is still open: a manual "refresh now" affordance beside the indicator (tap-to-refresh icon forcing a poll tick) so users don't wait out the 5s cadence — the ui primitive takes no props today, so the affordance needs a small optional `onRefresh`-style extension or a separate button in the Card `actions` slot | Open |
| 2026-08-14 | LivePollIndicator rollout | Sweep the remaining polling surfaces (History page, uploads page, admin monitoring if any of them run 5s poll loops) for the same indicator so every auto-refreshing surface is consistent — **Done 2026-08-17** (see the LivePollIndicator sweep row below) | Done |
| 2026-08-10 | Mobile-first grid guard (`bcd8ca8`) | Wire the `gridClassGuard` sweep into the CI workflow so the repo-wide scan runs on every push/PR alongside the vitest suite | Open |
| 2026-08-10 | Mobile-first grid guard (`bcd8ca8`) | Re-run the landing + workspace overflow probes with the new explicit `grid-cols-1` bases at 375/768/1280 to confirm the sweep is visually inert (zero layout change) — **done**: overflow probe 215/215 PASS at 375/768/1280 (and 640/1024); pixel-diff of the pre-sweep commit (53d9a8e) vs post-sweep (bcd8ca8) worktrees shows the landing **byte-identical (0.000%)** at all three widths; the only workspace diffs were on data-driven pages (reports list/detail, sample-report) proven to be mock-timing noise by a same-code double-capture (same magnitude without any sweep), with sub-0.01% animation-frame variance elsewhere | Done |
| 2026-08-10 | Dashboard live-refresh indicator | Extend the same live indicator to the Queue page and the report detail pane (which already poll a single scan via `scanNeedsPolling`), so every surface that tracks worker progress shows it — **Done 2026-08-17** (LivePollIndicator rollout row) | Done |
| 2026-08-10 | Dashboard live-refresh indicator | Add a manual "refresh now" affordance next to the indicator (a tap-to-refresh icon) so users can force a poll tick without waiting for the 5s cadence — **Done 2026-08-17**: `LivePollIndicator` gained the optional `onRefresh` affordance; `useResource`/`useMockData` both expose silent `refresh()` (in-place swap, no loading flash, bypasses pollWhen — never use reload/refetch for this); wired on all six live surfaces (dashboard ledger + queue posture, Queue, History, report detail, Uploads status card, admin Monitoring). 8 new tests, headless-verified, audit subset 40/40 | Done |
| 2026-08-17 | Refresh-now affordance rollout | The affordance is currently gated with the indicator (only while polling is active) — an idle-but-stale surface (e.g. a settled dashboard ledger) has no refresh path today. If founder wants one, render a small refresh-only button even when `live` is false (outside the pill, `reload()`-free) | Open |
| 2026-08-10 | Report detail live-completion | Add the same live indicator (pulsing dot + auto-refreshing) to the report detail pane while it polls a pending scan, so the pending → completed flip is visually announced there too | Open |
| 2026-08-10 | Report detail live-completion | Consider exposing the mock worker's step durations via a dev seam (?worker=fast|slow) so the pending → completed flip can be demoed at a controllable pace in screenshots | Open |
| 2026-08-10 | useResource tab-hidden pause (poll verification) | Extend the pause to the Page Lifecycle API: also gate on `pagehide`/`freeze` (Safari bfcache), so a backgrounded tab cannot fire one last poll after the document freezes | Open |
| 2026-08-10 | useResource tab-hidden pause (poll verification) | Once migrations 0009/0019 land, re-run the live walk and verify the visibility pause in a real browser tab switch (background the tab mid-upload, confirm zero network calls, resume on return) - the curl-level pause is proven, the browser-level leg is not | Open |
| 2026-08-10 | scanPresentation import-parity guard | Extend the same import-parity pattern to the other shared modules (chartGeometry, mockData/mockApi surfaces, ui barrel exports) so every consolidation target has a drift guard | Open |
| 2026-08-10 | scanPresentation import-parity guard | Wire the parity scan into the CI workflow beside the gridClassGuard repo-wide scan so both run on every push/PR | Open |
| 2026-08-10 | Real scan upload + queue round-trip (verified complete) | Apply the scan-pipeline migrations (0009 + 0019) and run the live walk: initiate -> signed-URL upload -> submit -> worker complete, watching the dashboard 5s poll land queued/processing/complete against real rows (the existing deploy-check row tracks the same blocker) | Open |
| 2026-08-10 | Real scan upload + queue round-trip (verified complete) | Stand up the real worker against Upstash Redis (REDIS_URL + start:worker) and confirm a submitted scan processes through BullMQ jobs (worker log lines ready/completed) instead of the inline fallback | Open |
| 2026-08-10 | Security page two-step revoke | Add the same two-step confirm to the Organization page member-remove and API-key delete actions, so every destructive action in the app shares the armed-confirm pattern — **Done**: both pages ship the armed two-step (verified 2026-08-17); the click-away/Escape reset now matches too (see the Armed-revoke click-away row) | Done |
| 2026-08-10 | Security page two-step revoke | Consider a click-away / Escape reset for the armed confirm state (currently only Cancel / revoke-start reset it) | Open |
| 2026-08-10 | Security e2e settings/password slice | Apply migration 0005 on the live project so the invite-accept live e2e suite passes again (its 2 failures are the standing live-DB blocker, not code) — **state 2026-08-17**: founder reports pasting the 0005+0015 block, but validate:migrations + direct REST probes on project dmhrwdcuwtgscwlaagsa still show 15 missing (organizations.id → PGRST205, organization_invites.token_hash → PGRST205, scans.processing_mode → 42703) and DATABASE_URL is empty; PROVANCE_LIVE_E2E=1 run confirms 2 failed (both the invite-accept seed: 'org tables are missing — apply 0005_organization.sql'), 76 pass — the paste landed on a different project or didn't execute; re-check the SQL Editor browser URL bar reads project/dmhrwdcuwtgscwlaagsa | Open |
| 2026-08-10 | Security e2e settings/password slice | Backfill the changePassword revoke-everything-else behavior into the mock (mockChangePassword) so the Security page demo matches the real contract end-to-end | Open |
| 2026-08-10 | Session-ledger round-trip e2e | Extend the same round-trip to the refresh path: POST /v1/auth/refresh should upsert (not duplicate) the ledger row for the same sid and keep isCurrent stable across rotation — worth an e2e assertion now that the sign-in leg is covered | Open |
| 2026-08-10 | Session-ledger round-trip e2e | The stateful mock upserts by user_id when the payload carries no id — a second sign-in with a different sid would replace the first row. Fix the mock to key upserts by the conflict columns (user_id, auth_session_id) for realism | Open |

| 2026-08-10 | Session lifecycle validation | The combined SQL paste block must include migration 0010 (user_sessions) — the live project genuinely lacks the table (PGRST205 confirmed directly, not a stale cache); re-run the two-device ledger walk once applied | Open |
| 2026-08-10 | Session lifecycle validation | Once 0010 lands, re-verify isCurrent live: the session_id claim fix means the ledger should now record + match real GoTrue sessions (decodeJwtPayloadSid reads session_id with sid fallback) | Open |
| 2026-08-10 | Session lifecycle validation | The backend revoke path (DELETE /auth/v1/admin/sessions/:id) 404s on GoTrue v2.195.0 and /admin/v1 403s — pick the GoTrue-version-appropriate admin session-delete (or document logout scope=local as the working revoke) and make revokeLedgerRow match | Open |
| 2026-08-10 | Session lifecycle validation | Access-token behavior confirmed empirically (revocation kills both refresh and access), but only via logout scope=local — once a working admin session-delete exists, repeat the walk with the real DELETE to confirm identical behavior | Open |

| 2026-08-10 | Scan round-trip validation script | Apply the combined migration block INCLUDING 0019 (idempotency_key column) to the live project, then run PORT=4100 node backend/scripts/validate-scan-roundtrip.mjs — initiate currently 503s with the exact missing-migration hint | Open |
| 2026-08-10 | Scan round-trip validation script | The readiness scansSchema probe reports 0009 columns but the first failing insert is idempotency_key (0019) — have the probe check the idempotency_key column too so the health signal matches the first failing query — **Done 2026-08-17**: migration 0019 is probed via `scans.idempotency_key` in migration-health.service.ts | Done |

| 2026-08-10 | Refresh-cookie validation | Apply migration 0008 (audit_logs) so the walk FINAL check (reuse rows with reuse_suspected) reads live instead of noting the missing table — replays already 401, the audit trail is the only unreadable leg | Open |
| 2026-08-10 | Refresh-cookie validation | GoTrue reuse grace interval measured ~20s on v2.195.0 (replays inside return 200) — consider surfacing the reuse-interval constant in the deploy contract doc so ops know a stolen-token replay window exists by design | Open |

| 2026-08-10 | AccountController HTTP spec | Consider the same HTTP-layer treatment for the roles controller (list roles, PATCH scopes Owner-guard, PATCH member role) — it is the last admin surface without a controller-level spec | Open |
| 2026-08-10 | AccountController HTTP spec | The stringy-boolean coercion (emailNotifications: "yes" → true) is a real implicit-conversion contract worth a note in API_DESIGN_STANDARDS.md under DTO validation gotchas | Open |

| 2026-08-10 | ParseIntStrictPipe sweep | Verify the frontend never sends garbage page/pageSize params that would now 400 — grep the api.js real-path callers for templated query strings that could produce NaN | Done |
| 2026-08-10 | ParseIntStrictPipe sweep | Consider ParseFloatStrictPipe analog for any future decimal query params, and note the strict-int contract in API_DESIGN_STANDARDS.md (garbage page values are 400s, not defaults) | Open |

| 2026-08-10 | SecurityController HTTP spec | Decide whether PATCH /security/settings should reject unknown keys with 400 (currently a silent no-op — controls unchanged, still 200) so client typos surface instead of silently doing nothing | Open |
| 2026-08-10 | SecurityController HTTP spec | The stateful in-memory Supabase mock is now duplicated between backend/test/security.e2e-spec.ts and this controller spec — extract it into a shared test helper module so the two cannot drift | Open |

| 2026-08-10 | Dashboard notification feed click contract | Add a View all affordance to the dashboard feed body (it currently caps at unread.slice(0, 4) with no link to /app/notifications) — **Done 2026-08-17**: "View all N unread notifications" link under the preview | Done |
| 2026-08-10 | Dashboard notification feed click contract | The optimistic readIds set resets on remount and the bell keeps its own count (documented as per-instance state) — consider lifting unread state into a shared context so the badge and feed sync instantly without refetch | Open |

| 2026-08-10 | Migration apply prep | User pastes .freebuff/combined-0005-0020.sql in the dashboard SQL Editor, then re-run the one-shot REST probes + readiness check to confirm scansSchema/userSessions/migrations all flip ready (wait a few seconds for the PostgREST schema reload first) | Open |
| 2026-08-10 | Migration apply prep | Once the full set is applied, re-run PORT=4100 node backend/scripts/validate-scan-roundtrip.mjs and the invite-accept live e2e — the standing 503/PGRST205 blockers should clear | Open |

| 2026-08-10 | validate:migrations script | After the user applies the combined migration block, cd backend && npm run build && npm run validate:migrations should print ALL OK and exit 0 — the single re-check command (currently reports 14 missing, exit 1) | Open |
| 2026-08-10 | validate:migrations script | Add a --json flag to validate-migrations.mjs so CI can gate on the missing list without parsing the table output | Open |

| 2026-08-10 | Full live walk (queue live) | Queue side verified live: BullMQ worker ready on scan-processing (concurrency 4, Upstash REDIS_URL); backend :4200 + worker left running. The ONLY remaining gate is the schema: validate:migrations still reports 14 missing (0005, 0007-0016, 0018-0020). Paste .freebuff/combined-0005-0020.sql in the SQL Editor, then run PORT=4200 node backend/scripts/validate-scan-roundtrip.mjs | Open |


| 2026-08-10 | Worker retry hardening | Once migrations land and the queue is live, prove the retry/backoff in production: enqueue a scan whose asset download fails, watch BullMQ log attempt 1-3 with exponential backoff, and confirm the row lands failed with the reason after the final attempt (the e2e locks the semantics; this is the live confirmation) | Open |
| 2026-08-10 | Worker retry hardening | Write a scan_failed event to audit_logs from markScanFailed so worker-side terminal failures show up in the Admin Audit Logs page (currently only admin retry/fail actions are audited) — **done 2026-08-17**: `markScanFailed` now writes a best-effort `scan.failed` audit row (owner email via profiles, `system` fallback; entity `scan`/scanId, `{ failure_reason }`); 4 new unit tests, backend jest 433/433, e2e 76 pass | Done |
| 2026-08-10 | Worker retry hardening | Consider surfacing attemptsMade/failedReason from the BullMQ job in the admin Jobs page payload so operators can see how many retries a failing scan burned — **done 2026-08-17**: new 0021_scan_attempts.sql (scans.attempts_made/max_attempts, defaults 1/3) + probe; worker's final-attempt 'failed' handler passes job.attemptsMade/opts.attempts into markScanFailed, which persists them (inline path records 1/1); admin toJobView surfaces real attempts (floor 1 keeps the mock dialect) + max_attempts; retryJob resets attempts_made to 0; mockAdminJobs gains max_attempts for shape parity; unit tests for persistence + audit details; backend jest 442/442, e2e 76 pass / 2 skip | Done |
| 2026-08-17 | Worker retry telemetry | The Admin Jobs page still renders only the numeric attempts readout — surface max_attempts in the job detail (e.g. "3 of 3 attempts") so operators see the retry budget alongside the burn count | Open |
| 2026-08-17 | Worker retry telemetry | Live confirmation once 0021 lands in the Supabase project: enqueue a failing scan through the real queue, confirm the admin payload shows attempts 3/3 + the failure reason, and that a manual retry resets the counter to 0 | Open |
| 2026-08-17 | Queue skill audit (flag-only pass) | **D1 — skill snippets stale vs code**: the skill's Worker section shows `markScanFailed(scanId, error.message)` and the inline path shows `markScanFailed(scan.id, reason)` — both now pass a third `{ attemptsMade, maxAttempts }` arg (0021 telemetry). Update SKILL.md's worker + inline snippets and add a hard rule that the attempts telemetry rides through markScanFailed | Open |
| 2026-08-17 | Queue skill audit (flag-only pass) | **D2 — contract doc missing 0021**: SCAN_UPLOAD_CONTRACT.md's schema table + the "must be applied" note name only 0002 + 0009, but markScanFailed now WRITES attempts_made/max_attempts (0021) — a live project without 0021 503s on the failure path (hint names 0021). Add 0021 to the schema table + required-migrations note — **done 2026-08-17**: schema table now lists file_hash_sha256 (0013), idempotency_key (0019), attempts_made/max_attempts (0021), and the required-migrations note names 0013/0019/0021 alongside 0002/0009 | Done |
| 2026-08-17 | Supabase skill activation (RLS/Storage audit) | Applied the supabase agent-skill (v0.1.2) to the scans contract: added an RLS/Data-API/key-posture section to SCAN_UPLOAD_CONTRACT.md — (1) the app never depends on table RLS (service-role admin client does all reads/writes; frontend's only direct Supabase surface is the signed-URL upload); (2) anon-key-only posture, service key never in public clients (VITE_SUPABASE_ANON_KEY only); (3) the Apr-2026 Data API auto-exposure breaking change — scans has RLS policies but NO GRANTs, intentional today, but any future client-side reads need exposure + GRANT first; (4) policy shape matches the recommended pattern (TO authenticated, UPDATE with USING + WITH CHECK); (5) no storage.objects RLS policies by design — signed-token access only, plus the upsert-needs-INSERT+SELECT+UPDATE trap documented for future direct reads — **done 2026-08-17** (commit 48cc5ad); inventory documented in README | Done |
| 2026-08-17 | Queue skill audit (flag-only pass) | **D3 — retry shape asserted nowhere automated**: the skill claims the e2e "hard-code the retry shape," but both scans e2e specs mock `enqueueScanProcessing` as a bare jest.fn and only assert it was called with scanId — the options object (attempts 3 / backoff 1s / removeOn 100 / jobId) is never asserted. The shape lives only in queue.service.ts + a verbatim duplicate in verify-bullmq.mjs. A change to attempts/backoff would pass the whole suite. Add an e2e assertion on the enqueue options object (or export the options builder) | Open |
| 2026-08-17 | Queue skill audit (flag-only pass) | **D4 — unit layer never exercises the enqueue branch**: scans.service.spec.ts passes `undefined as never` for QueueService and has no submitScan test — the skill's "keep the queue mocking consistent between unit and e2e" convention isn't true in the unit spec. Add a unit submitScan test with a mocked `isConfigured()=true` asserting enqueueScanProcessing is called (and `=false` covering the inline branch) | Open |


| 2026-08-10 | Schema-state mismatch check | Live run confirms the diagnostic works — project banner names dmhrwdcuwtgscwlaagsa. Once the user pastes the combined block into THAT project, this same command is the re-check: expect all-OK + exit 0, and the mismatch hint disappears | Open |
| 2026-08-10 | Schema-state mismatch check | Consider adding a --ref-only flag (or a validate:project npm script) that prints just the probed ref + dashboard link for CI scripts that need the project identity without the full table | Open |
| 2026-08-11 | Admin Jobs deep links | Make ?page= URL-backed too (or solve the two-single-key atomic-update constraint) so a paginated filtered view like ?status=failed&page=2 is fully shareable, mirroring the ?team= + ?from=/?to= pattern | Open |
| 2026-08-11 | Admin Jobs deep links | Extend the server-side status/page forwarding treatment to the admin Users page (and confirm Audit Logs forwards status already) so role/status deep links resolve server-side like /app/admin/jobs | Open |
| 2026-08-11 | Admin Jobs deep links | The useQueryParam absent-key loop fix is DOM-tested; do a quick manual dev pass on /app/admin/jobs?status=failed (and a ?team= surface) to confirm the visual state and URL bar stay canonical in the browser | Open |
| 2026-08-11 | Admin jobs controller spec | Extend the same HTTP-layer spec treatment to the remaining paginated admin routes (users, audit-logs, reports, organizations) — they share the strict-pipe query pattern and the guard chain, but only jobs has route-level coverage today — **done 2026-08-17**: new `AdminController paginated admin routes (HTTP layer)` describe block (12 tests) locks route paths/verbs, strict→DefaultValuePipe→ParseIntPipe wiring (reports 1|20, audit-logs 1|100, users 1|20), raw team/filter passthrough, malformed-number 400s, envelope pass-through, and guard 401/403 on the new routes; backend jest 454/454, e2e 76 pass / 2 skip | Done |
| 2026-08-11 | Admin jobs controller spec | The listJobs envelope now matches (totalPages added to the mock) — confirm the e2e scans/admin specs assert totalPages too so the parity is locked on both sides of the test suite | Open |
| 2026-08-11 | Admin jobs live walk | BLOCKED on migrations 0008 + 0009 (audit_logs, scans.processing_mode) — paste .freebuff/combined-0005-0020.sql in the dashboard SQL editor of dmhrwdcuwtgscwlaagsa, then run cd backend && npm run validate:admin-jobs to execute the prepared walk (filter/paginate/retry/audit-row checks) | Open |
| 2026-08-11 | Walk script PORT hardening | The injected-PORT hazard is fixed for validate-scan-roundtrip / validate-refresh-cookie / validate-admin-jobs — the remaining script (validate-session-lifecycle.mjs) already resolves via .env.local only, but verify-bullmq.mjs and parity-monitoring.mjs read ports from env too; audit them for the same class of env-var ambiguity | Open |
| 2026-08-11 | Admin jobs live walk | Once the walk passes, extend it (or add a sibling script) to also verify the fail path: POST /admin/jobs/:id/fail writes scan.failed with severity + reason, and that a completed job rejects both retry and fail with 400 | Open |
| 2026-08-11 | Live e2e opt-in gate | Once migrations land, run the live suite to completion with PROVANCE_LIVE_E2E=1 npm run test:e2e — the invite-accept round trip + forged-token rejection close the last live-DB verification gap | Open |
| 2026-08-11 | Live e2e opt-in gate | Centralize the hermetical defense: add a jest-e2e setupFiles hook that blanks SUPABASE_URL/ANON/SERVICE_ROLE unless PROVANCE_LIVE_E2E=1, replacing the per-spec delete-key pattern in security/organization e2e specs | Open |
| 2026-08-11 | Live e2e opt-in gate | Document the PROVANCE_LIVE_E2E=1 trigger in docs/engineering (e2e run instructions) so the live-suite opt-in is discoverable for operators | Open |
| 2026-08-11 | New-device trust signal | Fingerprint consistency: the sign-in detection (isNewDeviceCombo) uses the (device, ip) combo while the per-session badge uses the device label only — decide whether they should share one fingerprint so the sign-in alert and the ledger badge always agree | Open |
| 2026-08-11 | New-device trust signal | Expose each device's firstSeenAt in SessionView so the badge tooltip can render "First seen Jul 17" instead of a static explanation, and the Security page could group sessions by device | Open |
| 2026-08-11 | New-device trust signal | A manual dev pass: load /app/account (Security tab) and the org member-sessions drawer to confirm the badge renders next to This device / team tags in both surfaces at desktop and mobile widths | Open |
| 2026-08-11 | Org-admin revoke walk | BLOCKED on migrations 0005 + 0010 — paste .freebuff/combined-0005-0010.sql in the SQL Editor of dmhrwdcuwtgscwlaagsa (or set DATABASE_URL in backend/.env.local and the pg client applies them), then run cd backend && npm run validate:org-revoke for the two-device walk | Open |
| 2026-08-11 | Org-admin revoke walk | If DATABASE_URL is pasted, add a tiny node scripts/apply-migrations.mjs (pg client, reads the migration dir in numeric order) so future migration applies are one command instead of dashboard pastes | Open |
| 2026-08-11 | 375px audit leg | The audit is flaky on data pages that mutate in-session state: the same route (e.g. /app/admin/audit-logs) can fail one walk and pass the next because feeds accumulate live-prepended events across the walk, changing which rows render. Make the audit deterministic: freeze the mock feed (or reset session state between routes) so a route that fails once always fails until fixed | Open |
| 2026-08-11 | 375px audit leg | The full 260-check walk exceeds 10 min in this dev environment (cold vite + slow transforms); the subset walk over previously-failing routes is the fast loop, but the full walk should be re-run once (locally or via the CI responsive job) to confirm the whole inventory at 375 after this round of fixes — **public subset re-verified 2026-08-14**: docs/resources/benchmark/security/waitlist/signin/reset-password(+confirm)/accept-invite (+ /app/docs, /app/security, /app/admin/waitlist) = 60/60 clean at 375/640/768/1024/1280 | Open |
| 2026-08-14 | Landing touch/pointer audit | Consider promoting the tap-target probe into a permanent gate: extend `audit:responsive` (or add a small `audit:touch` script) to flag interactive elements with min target < 44×44 at 375px — same fail-closed pattern as the overflow probe, guarding the WCAG 2.5.5 floor from regressions | Open |
| 2026-08-14 | Landing touch/pointer audit | The remaining sub-44 target is the inline "See the full pricing model" sentence link (WCAG-exempt) — if the founder wants it larger anyway, convert it to a standalone button-style CTA below the sentence; otherwise leave as-is | Open |
| 2026-08-14 | Live-site 404 fix + analytics + 404 page | The legacy `api/` folder (Hono server, superseded by `backend/`) sits at the Vercel function root — decide its fate: delete it (it's dead code and Vercel may try to bundle `api/server.js` as a serverless function) or move it under `backend/` if any of its routes are still wanted | Open |
| 2026-08-14 | Live-site 404 fix + analytics + 404 page | After the next main deploy, confirm Vercel Web Analytics data starts flowing in the project dashboard, and consider enabling Speed Insights (`@vercel/speed-insights`) for the same zero-config win | Open |
| 2026-08-14 | Live-site 404 fix + analytics + 404 page | Add a 404-route check to `audit:responsive`'s route inventory (e.g. a synthetic `/definitely-missing` path) so the custom 404 page stays under the layout gate like every other route | Open |
| 2026-08-14 | Armed-confirm rollout | The armed-confirm pattern now covers session revoke, member remove, and API key revoke — sweep remaining destructive actions (webhook delete, admin user/org removal, invite cancel, feature-flag toggles if considered destructive) and convert any still single-click | Open |
| 2026-08-14 | Armed-confirm rollout | Extract the armed-confirm state machine (armingId + busyId + click handler) into a shared `useArmedAction` hook so future destructive actions get the exact same contract instead of re-implementing it per page | Open |
| 2026-08-17 | Import-parity guard extension | The guard core (`src/lib/importParity.js`) is now generic — the same three-line config can pin any future consolidation target (e.g. `mockApi.js`, `mockData.js`, `api.js`) without new machinery; add parity tests for those modules when they next get consolidated — **Done 2026-08-17**: `apiParity.test.js` (79 exports) + `mockApiParity.test.js` (69) + `mockDataParity.test.js` (40), all on the generic core with the surface pinned in declaration order (ESM namespace order — note: plain-node `Object.keys` sorts, vitest does not); 9 new tests, vitest 583/583 | Done |
| 2026-08-17 | Import-parity guard extension | The scanPresentation guard still walks at runtime with a fresh regex per file — fine for a unit suite, but if the repo grows past ~1k files, consider pre-walking once and caching the import list per file keyed by mtime | Open |
| 2026-08-17 | Live migration check | 14 migrations missing on the live project (`dmhrwdcuwtgscwlaagsa`): 0005, 0007–0016, 0018–0020. Paste block regenerated + parity-verified at `.freebuff/combined-0005-0020.sql` — **awaiting the founder's paste in the SQL Editor, then rerun `validate-migrations` + `validate-scan-roundtrip` + readiness**. Infra staged 2026-08-17: backend :4000 up, BullMQ worker ready, fresh Upstash Redis (expires 2026-08-19) — walk blocked at initiate (503, migration 0019) until the paste lands | Open |
| 2026-08-17 | Live walk staging | The fresh Upstash instance expires **2026-08-19** — if the founder's paste lands after that, re-provision (`POST https://upstash.com/start-redis`) and re-patch `REDIS_URL` before the walk | Open |
| 2026-08-17 | Live walk staging — migration applier | **Built** `backend/scripts/apply-migrations.mjs` + `npm run apply:migrations` (numeric-order multi-statement applier via `pg`, `--dry-run`/`--from` flags, stops on first failure, idempotent IF-NOT-EXISTS guards make re-runs safe). Dry-run verified (20 files). **Founder authorized direct apply 2026-08-17 — still blocked ONLY on the connection string**: `DATABASE_URL` is empty in `backend/.env.local` (grep-confirmed no string exists anywhere in the repo; re-probed `validate:migrations` → still 5 applied / 14 missing). The instant the founder pastes the Supabase → Connect → Session pooler string, the chain is: `npm run apply:migrations` → `validate:migrations` (expect 19/20, 0017 skip) → readiness ready → `validate:live-surfaces` → `validate:scan-roundtrip` → `validate:admin-jobs` → `validate:refresh-cookie` → `validate:org-revoke` → `validate:monitoring-parity` → `validate:sessions` → `validate:bullmq` | Open |
| 2026-08-17 | Armed-revoke click-away/Escape reset | The same armed-confirm pattern exists on the Organization page (member Remove) and API Keys page (Revoke) — apply the identical `data-armed-*` click-away + Escape disarm there so every destructive action shares the reset semantics, not just session revoke — **Done 2026-08-17**: `data-armed-member-row` (Org member remove) + `data-armed-revoke-row` (API key revoke table row), document `pointerdown`/`Escape` disarm in both | Done |
| 2026-08-17 | Armed-revoke click-away/Escape reset | The `useArmedAction` extraction follow-up is now more compelling: three pages hand-roll the same arming/busy/click-away state — a shared hook would carry the document-listener reset once instead of three copies | Open |
| 2026-08-17 | Skills install | Official `supabase/agent-skills` collection installed globally (supabase + supabase-postgres-best-practices v-tracked). Next gap to close from the skills audit: a Provance-specific NestJS skill (drafted from the repo's module conventions + API_DESIGN_STANDARDS) and a BullMQ/Redis queue skill from SCAN_UPLOAD_CONTRACT.md — both encodable with `skill-creator` — **done 2026-08-17**: both Provance skills drafted + installed byte-identical to `~/.agents/skills/`, registered via `skills list -g`; full inventory in README | Done |
| 2026-08-17 | BullMQ/Redis queue skill | **Drafted** `docs/skills/provance-bullmq-redis-queue/SKILL.md` (205 lines: architecture map, job contract, retry invariant, inline fallback, connection gotchas, verification/testing, hard rules) — **done 2026-08-17**: installed to `~/.agents/skills/provance-bullmq-redis-queue/` (byte-identical), registers in `skills list -g`; validated on the retry-telemetry slice + audit (D1–D4 rows below remain Open as the flagged fixes) | Done |
| 2026-08-17 | Live BullMQ round-trip | **Done** — real BullMQ path verified against Upstash Redis: worker claimed an enqueued scan (`delayed → active → failed` at queue level, row `queued → processing → failed`), retries live (`attempt 2 of 3`). **Next:** the app-path round-trip still needs migrations 0009/0019 applied (paste block `.freebuff/combined-0005-0020.sql` or `DATABASE_URL` + `npm run apply:migrations`) — after that, re-run `validate-scan-roundtrip` for the full upload → queue → report walk | Open |
| 2026-08-17 | NestJS skill | **Drafted** `docs/skills/provance-nestjs/SKILL.md` (204 lines: global wiring, module/controller conventions, guards+throttle, error + pagination envelopes, DTO/Swagger, service rules, 3-layer test pattern, hard rules) — **done 2026-08-17**: installed to `~/.agents/skills/provance-nestjs/` (byte-identical), both Provance skills register in `skills list -g` (CLI v1.5.22) | Done |
| 2026-08-17 | mockChangePassword revoke-everything-else | **Done** — mock now revokes every other session + writes session.revoked/password_changed audit rows; page reloads after change; preview-verified (4→1 sessions, feed ordering). Natural follow-ups: (1) the real backend already does this — the remaining gap is the org/API-key armed-confirm parity from the earlier row; (2) consider persisting mock security mutations to localStorage so they survive full reloads like the session does, if demo walks ever need cross-reload continuity | Open |
| 2026-08-17 | Invite-accept live e2e gate | Hermetic e2e green (**76 pass / 2 skip**). The live invite-accept suite needs **0005 + 0015** applied (seed inserts `token_hash`). Targeted block ready at `.freebuff/combined-0005-0015.sql` (parity-verified) — **awaiting founder paste in the SQL Editor for `dmhrwdcuwtgscwlaagsa`, or DATABASE_URL in `backend/.env.local`**, then re-run `PROVANCE_LIVE_E2E=1 npx jest --config ./test/jest-e2e.json` and `validate:migrations` (expect 16/20 after the pair, or 20/20 after the full block) | Open |
| 2026-08-17 | Component-reachability guard | **Done** — `componentReferences.test.js` enforces transitive reachability for every non-ui component under `src/components/` (TrustBar-class dead code now fails CI with the component path). All 36 components live today; a future sweep is unnecessary. Next-scale idea: extend the same reachability idea to the whole src tree (any unreferenced module, not just components) if the repo keeps growing | Open |
| 2026-08-17 | ProductShowcase tone fix (live walk) | **Done** — full cycle scroll-verified in the preview (queued → signals resolve → report-ready → verdict chip + per-signal findings all render); walk surfaced a tone bug ("incomplete" matched the ok-regex → green + 75% agreement + 1 flagged). Fixed via extracted `showcaseTone.js` matcher + 11 tests (agreement 50%, flagged 2, amber tone verified live). Natural follow-ups: (1) the showcase's 62% generative-fingerprint signal and the 78/100 authenticity score are hardcoded demo values — a variant that derives them from the fixture signals would keep the demo honest as thresholds change; (2) consider a small caption/aria-reader pass on the animated signal rows for screen-reader users, since the demo is timing-driven | Open |
| 2026-08-17 | Report-surface long-string hardening | **Done** — extended the `min-w-0`/`break-words` treatment to every backend-string surface on the workspace report detail (`AppReportsPage`: meta values, list filename, verdict label, dedup banner ids, signal/finding text) and the admin Reports drawer (`ReportsPage`: summary card ids, dl rows, signal labels + finding badges). Responsive audit subset **20/20** clean at 375–1280. Natural next: the same sweep for the report **print view** (`AppReportPrintPage`) if long payload strings ever render there | Open |
| 2026-08-17 | Full dashboard/admin/report review + roadmap | **Drafted** — `docs/engineering/DASHBOARD_ADMIN_REPORT_ROADMAP.md` (founder review draft). Audit findings: dashboard + 12 admin pages are feature-complete with backend parity; the report gap is real (mock/dev mode exports via **browser print dialog**; the branded server PDF only exists in real mode); the pipeline is **image-only** (backend 400s `mediaType !== 'image'`); public API is roadmap-only. 5 phases proposed: (1) branded PDF single-path export (client-side gen in mock, upgraded pdfkit branding w/ logo/cover/QR/signature), (2) video+audio verification (ffmpeg frame pipeline), (3) public verification API + report share tokens, (4) dashboard value features (media donut, WoW deltas, queue controls, onboarding checklist), (5) admin depth (status page, incident UI, seat/plan management). **Awaiting founder phase approval** — recommended start: Phase 1 | Open |
| 2026-08-17 | LivePollIndicator sweep | **Done** — History (existing 5s poll + Card `actions` indicator, `hasActiveScanWork`-gated), Uploads (new `getScan(activeScanId)` poll with `scanNeedsPolling` gate + indicator in the status card), and admin Monitoring (new silent 5s poll via `useMockData` `pollMs` option + indicator in the queue-health header). `useMockData` gained an optional silent-poll `options` arg (no loading flash, last-known-good on failure) with a 3-test suite. Headless-verified on all three surfaces; audit subset 15/15; vitest 563/563. Natural follow-ups: (1) the monitoring page could surface a last-checked timestamp so viewers see how fresh the silent feed is; (2) `pollWhen` is now supported by both `useResource` and `useMockData` — a future sweep could assert both hooks share the same poll semantics with a parity test — **Done 2026-08-17**: `src/lib/pollParity.test.jsx` (3 tests: held-while-gated, flip-to-hold, no-pollMs). Remaining from this row: (1) the monitoring page's last-checked timestamp — still Open | Done |
| 2026-08-17 | CRLF shebang hazard | **Done** — with Windows `core.autocrlf=true`, `scripts/*.mjs` shebangs check out CRLF and vite 8/rolldown's transform fails when the script is imported (`trello.test.mjs` lost 22 tests to "Invalid or unexpected token"). Fixed repo-level: `.gitattributes` forces `eol=lf` on `scripts/*.mjs`; both branches at `885e971` with 560/560 green. **Note for future**: any NEW shebang'd script added under scripts/ is covered by the rule — but a shebang'd .mjs added anywhere else would hit the same bug; keep shebangs LF-only | Done |
| 2026-08-17 | Armed-confirm test flake + merge to main | **Done** — pre-merge gate caught a flaky `appSecurityArmedReset` suite (~1-in-5 arm clicks never registered under load; `fireEvent.click` raced React's commit). Rewrote clicks with `userEvent` + deterministic waits: 12/12 green, full suite 560/560, lint/build clean. Founder approved the merge — `dev/backend-integration-milestone` merged into `main` (404 fix, Vercel analytics, armed confirms, CI gates now live on Vercel) | Done |
| 2026-08-17 | Mobile touch audit (Hero + landing) | **Done** — measured every interactive landing element at 375×720: hamburger 44×44, mobile menu links fixed from ~24px to exactly 44px (`min-h-11`, matching the footer pattern), hero CTAs 55px, accordion/detail summaries and ProductShowcase signal buttons all ≥44px. `InteractivePanel` tilt already touch-safe (fine-pointer + reduced-motion gated); **zero** `group-hover`/hover-only content patterns on the landing. Natural follow-ups: (1) the Hero's infinite pulse blob and ProductShowcase's timed demo run regardless of `prefers-reduced-motion` — gating the auto-run and pulse behind a reduced-motion check would finish the a11y story; (2) extend the same 44px measurement to the workspace (`/app/*`) nav + admin surfaces as a one-off probe — **(1) Done 2026-08-17**: Hero pulse renders static under reduced motion; ProductShowcase skips auto-play (manual Run demo / Replay still works). **(2) workspace 44px probe still Open** | Done |
| 2026-08-17 | Post-paste verification pipeline (staged, blocked) | **Staged & waiting on founder paste.** Re-probed `dmhrwdcuwtgscwlaagsa` — still **14 missing** (0005, 0007–0016, 0018–0020); `DATABASE_URL` still empty, so the SQL Editor remains the only apply path. `.freebuff/combined-0005-0020.sql` (905 lines) **byte-parity re-verified** against all 14 source migrations (PARITY OK). The moment it's applied I run: `validate:migrations` (expect 19/20, 0017 seed-skip) → readiness flips ready → `validate:live-surfaces` → `validate:scan-roundtrip` (wired as `validate:scan-roundtrip` in commit `e2dcacf`) → `validate:admin-jobs` → `validate:refresh-cookie`. Alternatively, paste the Session pooler `DATABASE_URL` into `backend/.env.local` and I run `npm run apply:migrations` myself | Open |
| 2026-08-17 | Follow-up log triage + closed gap batch | **Done** — audited the whole log against the code and flipped 9 verified-stale rows to `Done` (roles controller, severity parity, mock parity, cookie CI gate, idempotency-key probe, polling-sweep rows, armed confirms, refresh-cookie wiring). Founder-blocked rows kept Open with current notes. **New code shipped**: shared `ScanQuotaWarningChip` on Uploads, dashboard notifications View all, armed click-away/Escape on Org + API Keys, Audit Logs details drawer + lockout tone, `SignInLockoutInterceptor` (sign-in credential-stuffing lockout, 8 tests), reduced-motion gating (Hero pulse + ProductShowcase auto-play), `validate:refresh-cookie` script, `pollParity.test.jsx`. Gates: backend jest 441/441, e2e 76 pass / 2 skip, vitest 566/566, lint 0, build clean, audit 155/155 | Done |
| 2026-08-17 | Sign-in lockout + e2e PORT quirk | **Environment hazard found**: this dev shell exports `PORT=0`, which trips `env.validation` ("PORT must be a positive integer") on every e2e AppModule boot — the hermetic e2e suite is green only when PORT is unset. Pin `PORT` (and the other volatile vars) in a jest-e2e `setupFiles` hook so the suite is immune to the parent shell, replacing the per-spec delete-key pattern | Open |
| 2026-08-17 | Sign-in lockout | `SIGNIN_LOCKOUT_*` env keys (threshold 5 / window 60s / duration 300s / enabled) aren't in `backend/.env.example` (the file is tool-blocked from editing) — add them next time .env.example is touched by hand, alongside the refresh lockout's keys | Open |
| 2026-08-17 | Sign-in lockout | The lockout state is per-process in-memory for BOTH sign-in and refresh — once multiple backend replicas run, make the `RefreshLockoutTracker` state distributed (Redis) so a stuffing burst can't race replica failover | Open |
| 2026-08-17 | Shared quota chip | `ScanQuotaWarningChip` is now shared (dashboard + Uploads) but router-coupled (react-router `Link`) — if it ever needs to render outside the router tree (emails, docs), pass the href as a prop or split the presentational shell from the link | Open |
| 2026-08-17 | Armed-confirm rollout | `useArmedAction` extraction is now a 4-page duplication (session revoke, member remove, API key revoke, + the click-away/Escape reset in each) — one shared hook would carry the document-listener reset once instead of four copies | Open |
| 2026-08-17 | Monitoring freshness | The monitoring page's silent 5s poll (LivePollIndicator sweep) still lacks a "last checked" timestamp so viewers can see how fresh the feed is | Open |
| 2026-08-17 | Workspace tap targets | Extend the 44px tap-target probe to the workspace (`/app/*`) nav + admin surfaces (the landing audit covered the public nav only) | Open |
| 2026-08-17 | Founder migration checklist | `MIGRATION_RUNBOOK.md` now opens with a generated founder checklist (`validate:migrations --runbook`): per-migration click-to-open SQL Editor links + the full combined block. **Next**: after the block lands on `dmhrwdcuwtgscwlaagsa`, re-run `validate:migrations` and regenerate the checklist — rows auto-flip to applied as the section shrinks; confirm the `?query=` prefill is honored by the dashboard once (it degrades to a blank editor on the right project if not) | Open |
| 2026-08-17 | Live walk remains blocked | Schema still 5/20 on `dmhrwdcuwtgscwlaagsa` (re-verified live 2026-08-17 — direct REST probes + readiness agree: 0009/0010 hard gates missing, applied-set fingerprint unchanged). `DATABASE_URL` still empty. Unblock: re-paste `.freebuff/combined-missing.sql` into the SQL Editor of **project/dmhrwdcuwtgscwlaagsa** (check the browser URL bar — the paste is landing on a different project), or set `DATABASE_URL` + `npm run apply:migrations -- --verify`. Then run readiness → `validate:admin-jobs` → `validate:org-revoke` → `validate-scan-roundtrip` (job-level BullMQ watcher included; worker must be running) → `GET /v1/reports/:id/pdf` | Open |
| 2026-08-17 | Contract doc evidence | `SCAN_UPLOAD_CONTRACT.md` now carries the verified-live BullMQ evidence (concurrency 4 boot line ×183, attempt 2-of-3 retry observed, both validator commands). **Next**: once migrations land, re-run `validate:bullmq` + `validate:scan-roundtrip` and update the "no completed job observed" note with the first `Completed scan job <id>` + `complete` row transition — the doc's happy-path evidence is the last gap | Open |
| 2026-08-17 | Full live scan walk (retry) | Re-probed before the walk after user reported migrations applied — **still 5/20** on `dmhrwdcuwtgscwlaagsa`: direct probes + readiness agree (organizations 404, user_sessions 404, audit_logs 404, processing_mode 42703). The combined block (`.freebuff/combined-missing.sql` or the runbook checklist links) did not land on this project. Full round-trip blocked until it does | Open |
| 2026-08-17 | High-end design pass (landing) | Applied the vanguard-architect premium language to the landing surface: Plus Jakarta Sans, fluid-island floating nav + staggered mobile menu, double-bezel cards (WhyProvance/HowItWorks/UseCases/SampleReport/Pricing), button-in-button CTAs, luxe cubic-bezier motion, film grain. 260/260 responsive clean. **Extended 2026-08-17 to the remaining public pages** — `/docs`, `/resources`, `/benchmark`, `/security`, `/waitlist`, `/signin`, `/reset-password` (+ `/reset-password/confirm`) all converted to the same language: double-bezel shells (`bezel-shell`/`bezel-core`, dark variants on charcoal sections — docs code panels, security retention/enterprise cards, benchmark comparison bars + catalog panels), the LUXE cubic-bezier motion with blur fade-up on every card grid, eyebrow-pill section headers, and the landing heading scale (`lg:text-[3.4rem]`) on section h2s. `PageHero` (shared by all 8 pages) upgraded to LUXE easing + blur fade-up + `xl:text-[4.4rem]` headline scale so every public hero matches the landing. Verified: vitest **593/593**, lint 0 errors, build clean, `guard:grid` clean, responsive audit subset **55/55** clean at 375–1280, and DOM probes confirmed the bezel shells on /docs (7+2), /signin (access card + form), /security (3 light + 9 dark + audit mockup). **Remaining open**: the app/admin shells are still on Inter (Operate register by design) — decide whether the premium sans should extend there too | Open |
| 2026-08-17 | Landing cleanup (Founder) | Removed `<TrustBar />` (Open Benchmark · Published) + `<ProductShowcase />` (Live Product Preview) from HomePage and the hero "Image-first early access" pill; both components archived under `src/archive/` (forensic precedent, imports repointed). **Public-pages copy audit Done 2026-08-17** — `docs/engineering/PUBLIC_COPY_OVERCLAIM_AUDIT.md` sweeps /docs, /resources, /benchmark, /security, /waitlist, /signin, /reset-password against the real backend. Verdict: **/docs is critical** (fabricates a live `POST /v1/verify` API — no such route; verdict `ai_generated`, heatmaps, webhooks, SDKs don't exist); **/security is high** (mockup shows SAML + retention + audit-export that don't exist; "Compliance Ready"/"Built for the enterprise" titles overclaim); **/benchmark low** (numbers trace to the shipped V0.1 report — two wording nits only); resources/waitlist/signin/reset are clean. Every flagged line has a recommended rewrite in the doc. **/docs rewrite Done 2026-08-17** (see next row). **/security + /benchmark rewrites Done 2026-08-17** (see the copy-honesty pass row) — the full audit is now applied | Open |
| 2026-08-17 | Copy-honesty pass (Founder-approved) | Implemented every remaining rewrite from `docs/engineering/PUBLIC_COPY_OVERCLAIM_AUDIT.md` (the /docs half already landed). **/security**: audit-log mockup now shows only real events — `verdict:suspicious confidence:0.69` (real display verdict + classifier confidence range) instead of `ai_generated 0.947`, `endpoint:/v1/scans status:201` instead of `webhook-001 /v1/verify`, `action:settings.updated` instead of `update_retention 90d→365d`, `method:email_password` instead of `method:saml`, and a real report id (`PRV-7F2C9A1E`); "Export & Integration" → "Export & Integration (planned)"; "Compliance Ready" → "Compliance Direction"; the Data Retention column items re-framed as roadmap ("Retention windows are on the roadmap — the policy will be published before rollout", "Deletion rules will ship…", "…are planned"); hero "Built for the enterprise." → "Enterprise-grade trust, phased in.". **/benchmark**: "held Trust-Weighted Accuracy at 1.00 on the gold subset" → "scored Trust-Weighted Accuracy of 1.00 on the V0.1 gold subset (100 assets) — a research benchmark, not a live-product guarantee"; "protecting newsrooms from citing false results" → "the design goal for newsroom review workflows". Verified: build clean, lint 0 errors, responsive **15/15** on /security+/benchmark routes at 375–1280, zero fabricated refs (`saml/update_retention/webhook-001//v1/verify/ai_generated`) remain on /security, no tests reference the changed strings | Open |
| 2026-08-17 | /docs real API contract | `src/pages/DocsPage.jsx` now documents something true: the fabricated `POST /v1/verify` reference (fake `ai_generated` verdict, `include_heatmaps`/`heatmap_url`, callback webhooks, rate tiers) is replaced by the real contract from `backend/src/scans` — **POST /v1/scans** (InitiateScanDto: originalFilename/mimeType/fileSizeBytes/mediaType=image/optional processingMode quick|standard|deep, + optional Idempotency-Key) → 201 `{scanId, status:'awaiting_upload', bucket, path, token, signedUrl}`; then PUT to signedUrl, **POST /v1/scans/:id/submit** → 202 `{scanId, status:'queued'}`; then poll **GET /v1/scans/:id** → `{scan}` with real status enum (`awaiting_upload · queued · processing · completed · failed`), real verdict enum (`likely_authentic · inconclusive · suspicious` → display `authentic · inconclusive · suspicious`), and an honest `result_payload` subset (verdict class/confidence/signals/report/metadata — every key traced to `buildVerdict`/`buildAnalysisResultPayload`). The fabricated "Webhooks" section became "Verification Lifecycle" (reserve→upload→submit, BullMQ queue + retries + 402 quota gate, poll-the-scan as today's async mechanism, SDKs/tiers marked **planned**). Page copy: "Verify media in three lines" → "A three-step verification flow". Verified: build clean, lint 0 errors, responsive **10/10** on /docs + /app/docs at 375–1280, audit:a11y clean, live DOM probe confirms the enums render and zero fabricated refs remain (the only 'heatmaps' mention is the "no heatmaps ship yet" disclaimer). Open: extend `audit:a11y`'s inventory to include the public /docs route (today it's app+admin focused) | Open |
| 2026-08-17 | RolesController HTTP spec | `roles.controller.spec.ts` (16 tests) now locks the roles surface at the HTTP layer: route metadata, real SupabaseAuthGuard+AdminGuard, scopes PATCH with Owner-role 403, member reassign routing (not shadowed by `:roleId/scopes`), DTO 400s, and the 30/60s throttle. **Next**: the `members/:memberId` route shadows `:roleId/scopes` only when memberId === 'scopes' (a member literally named 'scopes' can't be reassigned) — worth a route-order comment or a reorder to `members/:memberId` first if that edge ever matters | Open |
| 2026-08-17 | Migration-convergence CI gate | `npm run check:migrations` (backend CI step) runs the real applier `--dry-run` and fails if the migration file set/order drifts from the runbook manifest (`MIGRATION_RUNBOOK.md` `BEGIN/END MIGRATION MANIFEST` markers). `--fix` re-embeds the manifest; `--runbook` regen emits it too. **Extended 2026-08-17**: the combined paste block is now content-verified too — every `-- MIGRATION nnnn · <file>.sql` banner must match its filename prefix AND the SQL under it must byte-match (CR/trailing-whitespace normalized) the on-disk file, so a content-level mislabel or swap fails CI (verified: real runbook passes 16/16; a probe runbook with a mislabeled banner + swapped 0016 line exits 1 with both caught). New `--runbook <path>` flag for testing. **Extended 2026-08-17 (validate:migrations)**: the checker's manifest is now ALSO the cross-check source for the live walk — `validate:migrations` parses the same manifest block and warns on every run when the runbook's `expect applied: N` hardcode or the live probe contradicts the documented expectation (verified live: manifest expects 20, probe reports 5 → WARN). The `--runbook` generator's `expect applied: N · skipped: M` line is now derived from MIGRATION_PROBES instead of hardcoded `20 · 1`, so a new migration can't leave a stale expectation in the doc. **Next**: after the live paste lands, regenerate the runbook so the `applied: 20` expectation matches the converged state | Open |
| 2026-08-17 | Manifest-vs-live test | The `validate:migrations` manifest cross-check was validated ad hoc (live probe + isolated parse-helper simulation). If the parse helpers ever gain another rule, a small `node:test` suite around `parseManifestFiles`/`parseRunbookExpectedApplied` would lock the stale-hardcode and over-applied branches in CI — same reasoning as the banner/content checker row | Open |
| 2026-08-17 | Banner/content drift test | **Done 2026-08-17** — the gate is now regression-proof: the parse/diff logic was extracted into `backend/src/health/migration-convergence.ts` (pure functions, filesystem injected as a `readFile` callback) with an 18-test jest spec (`migration-convergence.spec.ts`, notifications.service.spec-style fixtures) covering the manifest parse, build↔parse round-trip, set/order diff, and the full banner matrix (pass, number-prefix mismatch, content swap, missing file, CRLF-noise tolerance, empty fence). `check-migration-convergence.mjs` is now a thin IO shell importing the module from `dist/` (CI already builds first). Verified: 488/488 jest, positive/negative/--fix paths + CI-style invocation. The ad-hoc negative probe is retired — the cases it checked are now tests | Done |
| 2026-08-17 | Convergence gate build dependency | `check:migrations` now requires `npm run build` (dist helpers) — CI already builds first, but a local `check:migrations` on a stale dist runs stale logic. The script prints a friendly "run npm run build first" error on missing dist; the failure mode to watch is a **stale** dist that exists but predates a helper change. If that ever bites locally, consider a source mtime-vs-dist check in the script's preamble | Open |
| 2026-08-17 | Retry-path verification | `validate:bullmq` now proves the retry contract live (8/10: active→delayed→active→delayed→active→failed, attemptsMade 1→2→3, row failed + download reason). The 2 remaining checks need migrations **0021** (attempts_made/max_attempts on the row) + **0008** (scan.failed audit row) — once those land, re-run and expect 10/10 | Open |
| 2026-08-17 | Stale worker process | The running worker(s) logged `marked failed after retries` — a format that exists nowhere in current src/dist — so at least one worker process is running pre-0021 code in memory. After migrations land, **restart every worker from the current build** before relying on attempts telemetry (a fresh worker with 0021 applied makes the verify assertions write real numbers) | Open |
| 2026-08-17 | Mock security persistence | Mock revokes + password-change audit rows now persist via `provance.mock.securityMutations.v1` (delta replay, not full-state) — the Security page, Activity feed, and org session drawer restore the revoked set across reloads. **Next**: consider persisting the sign-in control toggles (2FA/timeout/notify) the same way if a demo wants those settings to stick; currently they re-seed fresh by design | Open |
| 2026-08-17 | Change-password three-way parity | All three legs now pinned: mock (`changePasswordContract.test.js`), better-auth (`betterChangePasswordContract.test.js`), GoTrue (backend security e2e — now with the HTTP-layer "exactly one ledger row" readback). **Remaining asymmetry**: the mock writes `session.revoked`/`password_changed` audit rows, but the better-auth backend path (BetterAuthController proxy) does not write them — when the better-auth backend slice lands, add the same SecurityService.recordAdminTrail writes so the Activity feed matches in better-auth mode too | Open |
| 2026-08-17 | Premium layer token doc | **Done** — `docs/design/PREMIUM_LAYER_TOKENS.md` is the canonical reference for the premium design layer (typography, palette, luxe ease + `LUXE` constant, double-bezel shell/core, grain, spacing rhythm, primitives, new-surface checklist), cross-checked against `src/index.css` and wired into the master index. **Next**: the `LUXE = [0.32, 0.72, 0, 1]` framer constant is duplicated in 12+ components and `--ease-luxe` used 33× — a shared `src/lib/motion.js` export (or a framer `transition` helper) would let the doc and the code reference one easing source instead of re-declaring it; also the stale `docs/design/DESIGN_SPEC.md` (Instrument Serif values) should either be archived or rewritten against this layer | Open |
| 2026-08-17 | CONTEXT.md glossary | **Done** — seeded `CONTEXT.md` (repo root) with the domain vocabulary (Scan, Verdict, Report, Queue, Audit, Session Ledger, Organization, Waitlist, Migration) grounded in shipped code, plus five vocabulary rules; wired into the master index. **Next**: (1) create `docs/adr/` and record ADR-001 = "repo migrations are the schema source of truth; live Supabase is verified against them" (the first decision the glossary should pin); (2) when the architecture-review skill runs, reference CONTEXT.md instead of re-deriving terms — if any term drifts, update the glossary in the same turn as the code change | Open |
| 2026-08-17 | Candidate 03 — collapse useResource/useMockData | **Done** — `useResource` is now the single polling engine (one implementation of load/reload/poll/refresh, visibility pause, in-flight guards, last-known-good); `useMockData` is a thin adapter over it via a loader seam (params from a ref at call time, `() => promise` wrapper) + vocabulary map (`status`→`loading`, `reload`→`refetch` with `keepDataOnReload`, `refresh` passthrough, `errorMessage` option). 6 mock consumers + 48 real-mode consumers untouched. `pollParity.test.jsx` kept unchanged as the drift guard — it now exercises one engine through two adapters. 4 new seam tests (params-destructuring defaults, params pass-through, refetch-keeps-data, refetch last-known-good on reject — the last is a deliberate upgrade: the old mock blanked data on refetch failure). vitest 597/597, build clean, lint 0. **Next**: candidate **01** (Backend seam over the api.js ladder) is the natural follow-on — 02 rides on it, and once both land the poll-engine collapse can extend to a single `useBackend`-style entry if desired | Open |
| 2026-08-17 | Usage-unit billing (VUs) | **Proposed** — `docs/engineering/USAGE_CREDITS_PROPOSAL.md` swaps the flat scan-count quota for a metered VU ledger (quick 1 / standard 10 / deep 100 VU per scan, deduct-on-complete, failed = 0, rollover ≤1×, top-up packs, hard-stop 402 default, API volume ladder + per-key spend caps, enterprise committed blocks). 5 open decisions await the Founder (unit name, Pro allowance, overage default, rollover, failed-scan charging). **Next when approved**: rollout step 1 = ledger + metering backend slice (VU cost table by depth, deduction on completion in the worker, `GET /v1/billing` gains unitsUsed/unitsLimit, dashboard chip + projection switch to VUs) — no payment code required | Open |
| 2026-08-17 | A11y audit re-run (btn cascade + probe) | **Done** — re-ran `audit:a11y` (34/34 clean). Two real fixes: (1) `.btn-*` moved into `@layer components` in `src/index.css` — they were unlayered, so in Tailwind v4 unlayered CSS beat layered utilities and every utility override on a button silently died (Footer/About `Request Demo` dark-glass treatment rendered as a white pill with 1:1 charcoal text; Navbar `px-6 py-2.5` never applied). (2) `audit-a11y.mjs` contrast sampler now composites alpha backgrounds and skips unparseable oklch own-bgs instead of fabricating false 1:1 advisories — advisory count went 43 → **0**. **Next**: consider wiring `audit:a11y` into the CI workflow alongside `guard:grid` + `audit:responsive` so the structural gate runs on every push | Open |
| 2026-08-17 | Sample Report section responsive re-verify | **Done** — `audit:responsive` 260/260 clean at 375/640/768/1024/1280 after the btn cascade fix, covering `/` (landing section), `/sample-report`, and the print page. DOM probe at 375: grids collapse to single column, 3-metric row stays 3-across, seal full 74px, zero overflow. No code changes required — the mobile stack height (~2.8k px vs ~1.8k at 1280) is the expected `grid-cols-1` collapse, not clipping. **Next**: nothing pending for this surface; the standing CI-wiring item (audit:responsive/a11y/grid in workflow) remains | Open |
| 2026-08-17 | A11y audit re-verify | **Done** — re-ran `npm run audit:a11y` after the responsive + cascade work: 34/34 routes structurally clean, zero contrast advisories. No regressions from subsequent landing changes; no code changes needed this pass. The CI-wiring follow-up (run audit:a11y + audit:responsive + guard:grid on every push) remains the standing next step | Open |
| 2026-08-17 | VU per-package math + monetization strategy | **Done** — proposal §3 now shows what each package buys per month (Starter 10k/1k/100, Pro 100k/10k/1k, Team 300k/30k/3k at quick/std/deep) and §9 documents the founder's hook-now-tighten-later play: keep current rates during growth, raise per-scan VU cost over time (10× illustrative dial) once authority/top-ups/abuse-data triggers hit. Pro allowance stays 100,000 for now. **Next**: when the growth milestones land, revisit the dial deliberately (never silently); the ledger records the applied rate per scan so tighten is a config change, not a migration | Open |
| 2026-08-17 | Verified seal on full report cover + PDF | **Done** — `VerifiedSeal` extracted to a shared component (`src/components/VerifiedSeal.jsx`, useId collision-safe ids, size as caller prop) and mounted on the SampleReportDocument ink band, so `/sample-report` and `/sample-report/print` carry the same stamp as the landing card. The pdfkit export (`report-pdf.ts`) now renders the same seal on the cover via a per-glyph `drawCircularText` helper (no textOnPath in pdfkit 0.19): parchment disc + amber ring, circular phrase, charcoal core, gold check — geometry verified against pdfkit's transform matrix (glyph tops outward, clockwise reading). Tests: PDF spec asserts the phrase decodes from the content stream; document-cover test asserts the seal renders. **Next**: consider stamping the seal onto the first page of the print CSS (`@page` header) if browser-print exports should match the pdfkit cover exactly, and mirror it into the real `AppReportPrintPage` report surface once that exists | Open |
| 2026-08-17 | Seal ring seam fix | **Done** — `VerifiedSeal` SVG circular text was 272.75px vs a 207.37px path (76%), so the browser clipped the doubled phrase mid-string leaving a visible seam. Added `textLength={2π·33}` + `startOffset=0` to the textPath so the phrase tiles the full ring; probe confirms last glyph ends −179° with the first starting 180° (seam ~1°). PDF export unchanged (already a full 360° ring). **Next**: none — the ring is now continuous on all three surfaces | Open |
